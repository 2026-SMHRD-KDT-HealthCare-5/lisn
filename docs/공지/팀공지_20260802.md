# 팀 공지 초안 — 2026.08.02

> 카카오톡에 그대로 붙여넣을 수 있게 쓴 것입니다. 마크다운 기호는 넣지 않았습니다.
> 이전 공지는 [`팀공지_20260801.md`](팀공지_20260801.md).

---

## 1) 짧은 버전 — 이것만 보내도 됩니다

```
[귀기울임 개발 공지 8/2]

■ 각자 해야 할 것
1. git pull 받으세요.
2. 저장소 폴더 경로에 한글이 있으면 옮기세요. C:\LISN 처럼 영어로요.
   한글 경로면 flutter analyze 가 그냥 죽습니다(분석 서버 종료).
   급하면 dart analyze 로 우회는 됩니다. 결과는 같습니다.
3. 관리자 화면을 확인하려면 아래 한 줄로 데모 데이터를 넣으세요.
   psql -U postgres -d lisn -f db/seed_demo_persona.sql

■ 새로 된 것
- 관리자 웹 대상자 조회에 이름/이메일 검색이 생겼습니다.
- 위기 사건 이력 화면을 볼 수 있게 데모 페르소나를 넣었습니다.
  14일치 라이프로그와 판정 기록이 같이 들어갑니다.
- 화면 다시 불러올 때 내용이 사라졌다 돌아오던 것 고쳤습니다.
  앱 홈/라이프로그/리포트/설정, 관리자 웹 대상자 조회 전부요.

■ 주의
- 데모 페르소나는 제가 만들어낸 데이터입니다. 발표 자료에 성능 근거로
  쓰면 안 됩니다. 화면에 seed-demo-v0 로 표시됩니다.
- 관리자 권한은 DB 의 role 을 봅니다. 승격하면 API 는 바로 열리는데
  관리자 웹은 다시 로그인해야 합니다.
```

---

## 2) 긴 버전 — 배경까지 설명해야 할 때

```
[귀기울임 개발 공지 8/2]

1. 저장소 경로에 한글 쓰지 마세요
flutter analyze 가 한글 경로에서 죽습니다. 분석 서버가 메시지를 잘라먹고
그냥 종료됩니다(exit 255). 바탕 화면 아래에 뒀다가 겪었고, C:\LISN 으로
옮기니 바로 정상이 됐습니다.
안드로이드 빌드도 한글 경로를 막는 검사가 있어서 플래그로 꺼둔 상태입니다.
=> 각자 폴더를 C:\LISN 같은 영어 경로로 옮겨주세요.
   당장 못 옮기면 dart analyze 를 쓰시면 됩니다. 같은 규칙, 같은 결과입니다.

2. 관리자 화면 확인용 데모 데이터
위기 사건 이력 화면은 CRITICAL 판정 기록을 읽습니다. 그런데 라이프로그를
14일 실제로 쌓지 않으면 화면이 계속 비어 있어서 확인할 수가 없었습니다.
데모 페르소나 한 명을 넣는 SQL 을 만들었습니다.

  psql -U postgres -d lisn -f db/seed_demo_persona.sql

14일 동안 수면이 421분에서 171분으로, 걸음이 7420에서 390으로 줄고
판정도 안정 -> 주의 -> 심각으로 같이 내려갑니다.
"라이프로그로 정서를 읽는다"는 설명이 화면에서 그대로 보이도록 짰습니다.

※ 만들어낸 데이터입니다. 화면에 seed-demo-v0 로 표시되니 실제 판정과
  구분됩니다. 발표 자료에 성능 근거로 쓰면 안 됩니다.
지울 때는 이 한 줄입니다.
  DELETE FROM USERS WHERE email = 'demo.crisis@lisn-test.example';

3. 관리자 웹에 대상자 검색이 생겼습니다
이름이나 이메일 일부만 쳐도 찾아집니다. 위험도 필터와 같이 걸립니다.
연락처로는 검색이 안 됩니다. 암호화해서 저장하기 때문에 같은 번호라도
암호문이 매번 달라서 부분 일치가 원리상 안 됩니다. 의도한 겁니다.

4. 화면이 튀던 것 고쳤습니다
버튼 누르거나 검색할 때마다 화면이 크게 움직인다는 지적이 있었습니다.
재보니 다시 불러올 때 내용을 통째로 비우고 로딩 표시를 그리고 있었습니다.
이제 이전 내용을 두고 흐리게만 처리합니다.
앱 홈/라이프로그/리포트/설정, 관리자 웹 대상자 조회 전부 같은 규칙입니다.
=> 화면 새로 만드실 때도 같은 규칙으로 해주세요. frontend/app/README.md 에
   적어뒀습니다.

5. 관리자 권한 관련해서 정정
전에 "role 은 토큰에 박히니까 승격 후 재로그인해야 한다"고 안내했는데
절반만 맞았습니다. 서버는 DB 의 role 을 보기 때문에 승격하면 API 는
기존 토큰으로도 바로 열립니다.
재로그인이 필요한 건 관리자 웹 화면 쪽입니다. 로그인할 때 권한을 보고
세션을 저장할지 정하기 때문입니다.
=> 403 이 계속 나면 토큰이 아니라 DB 의 role 을 확인하세요.

남은 큰 작업은 Health Connect 실기기 연동입니다.
화면설계서 디자인 작업(신규 6장, 콜아웃 재배치)도 그대로 남아 있습니다.
```

---

## 3) 보내기 전에 확인할 것

- **경로 이동 안내가 가장 중요합니다.** 함은선 님이 Flutter 작업을 하고 있어
  한글 경로면 `flutter analyze` 로 계속 막힙니다.
- 데모 데이터는 **각자 로컬 DB** 에 넣는 것입니다. 공용 DB 가 없으니 혼동 없습니다.
- 이 문안에는 키가 들어 있지 않습니다. 그대로 복사해도 안전합니다.

## 4) 공지에 넣지 않은 것 (팀장만 알면 되는 내용)

| 항목 | 상태 |
|---|---|
| 개발 DB 계정 누적 | 테스트 계정이 32개 쌓여 관제 대시보드 분포가 희석돼 보입니다. 시연 전에 정리 필요 |
| `MLCM_501` ❸ | 대상자 상세 API 는 있는데 관리자 웹 화면 연결이 남았습니다 |
| 화면설계서 `SD-A④` | 검색 UI 를 화면설명에 넣을지. 5줄이 이미 꽉 차 와이어프레임 주석을 권합니다 |
| OpenAI 키 | 조직 설정의 **Disable user API keys** 가 아직 켜져 있습니다. 시연 전에 풀어야 합니다 |
| `MUSIC`·`FOOD` 콘텐츠 | 팀이 직접 조사하기로 한 항목. 진행 상황 확인 필요 |
