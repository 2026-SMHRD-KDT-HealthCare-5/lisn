# 산출물 문서 개정 체크리스트

> **최종 갱신** 2026.08.03 · **작성자** 이응균(PM) · **목표 완료일** 2026.08.11
> **대상** 01~05 HWP 5종 + 발표자료·화면설계서 PPTX 2종
> **현행성 점검** 2026.08.03 · 완료 항목은 작업이력으로 이동했고 현재 남은 범위만 기재
>
> ## ⚠ 2026.08.03 — 범위가 다시 열렸습니다
>
> 7/29 에 시작한 개정이 `SD-B①` 까지 **전부 끝났습니다**(아래 2절 참조).
> 그런데 끝난 03 을 기준으로 01 을 대조하니 **01 이 03 과 반대 결론을
> 들고 있습니다.** 01 은 아직 F1 80% 를 목표로 걸고 있고, 03 은 그 목표에
> 도달하지 못해 채택하지 않았다고 적혀 있습니다.
>
> **개정 26건이 새로 생겼습니다** → [`기획서_개정안.md`](../결정/기획서_개정안.md)
> 같은 개정으로 「선제 접촉」 기능을 함께 세웁니다. 모순을 푸는 방법이 곧
> 차별점을 세우는 방법이라 둘을 나눌 수 없습니다.
>
> **이 문서에는 아직 반영되지 않은 항목만 남깁니다.**
> 완료된 결정과 그 근거 → [`작업이력.md`](작업이력.md) · 남은 일 → [`SESSION-HANDOFF.md`](../SESSION-HANDOFF.md)

---

## 1. 진행 현황

| 문서 | 남은 항목 | 담당 |
|---|---|---|
| **발표자료 PPTX** | **6 — `PL-20`~`PL-24`** ⭐⭐ **가장 급함** | 이응균 |
| **01 기획서** | **13 — `PL-01`~`PL-13`** ⭐⭐ | 이응균 |
| **02 요구사항정의서** | **7 — `PL-14`~`PL-19` · `PL-25`(음성 입력 제거)** ⭐⭐ | 이응균 |
| 03 빅데이터분석정의서 | **1 — 모델링 목표에 「개인 내 변화 탐지」 반영** | 김건영 |
| 04·05 | **1 — 알림 수신 동의 + 접촉 이력** (`fcm_token` 은 이미 있음) | 윤일준 |
| 화면설계서 PPTX | **1 — `PL-26` `MAIN_LIFELOG_01` ❺ 문안 1줄.** 신규 화면 **0장** | 함은선 |

> ### ⚠ 발표자료를 문서보다 먼저 하세요
>
> 「핵심 성과 지표」 슬라이드에 **`85% 위험도 분류 정확도` · `0.80 위험 단계
> F1-Score`** 가 KPI 타일로 박혀 있습니다. 03 이 "도달하지 못해 채택하지
> 않음"이라고 기록한 그 수치입니다. **문서는 펼쳐 봐야 걸리지만 이건 화면에
> 그대로 뜹니다.**
>
> PPTX 는 `tools/patch-pptx-text.ps1` 로 고치세요. PowerPoint 로 다시 저장하면
> 임베드 폰트가 풀립니다(5.99MB → 305KB 실제 발생).

> **화면설계서가 안 걸립니다.** 알림 토글은 `MAIN_SETTING_01` ❷ 에 이미
> 명세돼 있고, 선제 발화는 기존 챗봇 화면 안에서 일어납니다. 8일 남은
> 상황에서 이게 가장 큰 이점입니다.
>
> **FCM 도 신규가 아닙니다.** `NFR-DV-002`·`MLCM_200` 7단계·`MLCM_400`
> 5단계가 이미 FCM 을 요구하고 `fcm_token` 컬럼도 04·05 에 있습니다.
> 구현이 없을 뿐입니다.
>
> ⚠ **문안·근거는 전부 [`기획서_개정안.md`](../결정/기획서_개정안.md) 에
> 있습니다.** 여기에 복사하지 마세요 — 정본이 둘이 되면 반드시 어긋납니다.

### 🎉 HWP 5종 + 발표자료 개정 완료 (2026.07.31)

**7/29 에 시작한 산출물 문서 개정이 끝났습니다.** 마지막으로 닫힌 3건은 `02 FR-DP-001` · `SD-E1`(04·05) · `05-N③` 이고, 반영 검증까지 마쳤습니다.

**앱 push 서술이 01·02·03·05·발표자료 전 산출물에서 일치합니다.** 안건 1-1 이 완전히 닫혔습니다.

> 개정 항목별 근거와 검증 결과, 그리고 과정에서 나온 **감사 패턴 4가지·비고 작성 원칙**은 전부 [`작업이력.md`](작업이력.md) 2026.07.31 (14차) 절로 옮겼습니다. **다음 문서를 검수할 때 그 절부터 읽으세요.**

---

## 2. 이전 범위 — 전부 종료 (2026.08.03 확인)

**`SD-B①` 은 이미 03 에 반영돼 있었습니다.** 체크리스트가 뒤처져 있었던
것이고, 아래 교체 ①·②·③ 문안이 모두 추출본에 들어 있습니다.

| 확인한 것 | 위치 |
|---|---|
| 교체① 학습 방법 (ROC-AUC 0.528 / 0.479~0.540) | `03` 추출본 179행 |
| 교체② 모델링 목표 (「감정 코드는 … 규칙 기반으로 산출」) | `03` 추출본 177행 |
| 교체③ 6필드 계약 · `model_version` 구분 | `03` 추출본 183~184행 |

> **아래 본문은 근거 기록으로 남깁니다.** 새 범위는 1절과
> [`기획서_개정안.md`](../결정/기획서_개정안.md) 을 보세요.

<details>
<summary>SD-B① 원문 (반영 완료)</summary>

> ### ✅ 화면설계서 PPTX 는 2026.08.02 에 끝났습니다 (15장 → 21장)
>
> 신규 6장 추가 · 문안 교체 · 시안 15장 갱신 · 콜아웃 재배치가 전부 반영됐고
> PowerPoint 로 열어 확인했습니다. 과정과 근거는
> [`작업이력.md`](작업이력.md) 40·41차, 배치 기록은
> [`화면설계서_슬라이드_매칭.md`](../가이드/화면설계서_슬라이드_매칭.md) 에 있습니다.

### ✅ 화면설계서 문안 교체 5건 — 전부 반영 완료 (2026.08.02)

**본문은 들어냈습니다.** 반영이 끝난 문안을 여기 두면 아래 `SD-B①` 이 묻힙니다.
무엇을 왜 고쳤는지는 [`작업이력.md`](작업이력.md) 40차에 있고, 실제 문구는
`docs/extracted/` 추출본이 정본입니다.

| 항목 | 무엇을 |
|---|---|
| `SD-A③` | `MAIN_JOIN_01` 민감정보 동의 항목 분리 (`05-K` 연동) |
| `SD-A④` | `ADMIN_DASH_01` 대상자 검색 추가 · 콜아웃 5 → 7개 |
| `SD-A⑤` ⭐ | `MAIN_HOME_01` ❶ **감정 이름 노출 제거** — 「위기」·「절망」을 헤드라인에서 뺌 |
| `SD-A⑥` | 대화 성격 선택 단일화 — `MAIN_SETTING_01` ❸ 삭제, `MAIN_CHAT_01` ❹ 배지 |
| `SD-A⑦` | `MAIN_CHAT_02` ❸ 음성 입력 표기 제거 |

> ⚠ `SD-A⑤` 의 근거(진단 금지 · 위기 화면에 감정 이름을 크게 띄우지 않는다)는
> **되돌리면 안 되는 결정**입니다 → [`학습자료.md`](../학습자료.md) 9장

### SD-B①. `03` 4항 학습 방법 — 실측 결과 반영 **[교체]** ⭐ — 심사 방어 직결

**문서는 학습을 수행한다고 되어 있는데 실제로는 하지 않기로 확정했습니다.**
근거가 두 데이터셋 실측으로 확보됐으니 문서를 현재 상태에 맞춥니다.

> **왜 지금 고치나** — 「모델을 만든다」고 적힌 문서를 들고 「안 만들었다」고
> 발표하면 그 자체가 지적 사항이 됩니다. 반대로 **「재봤더니 안 돼서 넣지
> 않았다」는 판단은 근거만 있으면 강점**입니다.

---

#### 교체 ① — 「학습 방법」 칸

**개정 전**

```
- PMData 의 정상 패턴 시퀀스로 LSTM Autoencoder 를 학습시켜 재구성 오차 기반
  anomaly_score 를 추출한 뒤, 이를 GLOBEM 의 14일 집계 라이프로그 피처 및
  BDI-II 우울 척도 라벨과 결합하여 LightGBM 2단계(Two-Stage) Ensemble
  지도학습을 수행
- EMOTIONS 9개 클래스 중 CRISIS/DESPAIR 등 고위험 클래스는 발생 빈도가 낮아
  SMOTE(Synthetic Minority Over-sampling) 또는 class_weight 조정을 통해
  소수 클래스 학습 편향을 보정함
- 동일 사용자의 시계열 데이터가 Train/Test에 동시 포함되지 않도록
  사용자(user_id) 단위 Group K-Fold Cross-Validation을 적용함
```

**개정 후**

```
- 사용자(user_id) 단위 Group K-Fold Cross-Validation 으로 학습 타당성을
  사전 검증한 결과, 확보 가능한 공개 데이터로는 목표 성능에 도달하지 못하여
  본 과제에서는 모델 학습을 수행하지 않고 규칙 기반 판정을 적용한다.
- GLOBEM 공개 샘플(참가자 40명)의 우울 이진 라벨 기준 ROC-AUC 0.528,
  95% 신뢰구간 0.476~0.579 로 무작위 분류와 구분되지 않았다.
- LifeSnaps(참가자 63명, 감정 라벨 7종)로 재검증한 결과도 ROC-AUC
  0.479~0.540 로 동일하였으며, 특히 SAD(0.485)·TENSE/ANXIOUS(0.497)는
  0.5 미만이었다. 참가자 수와 피처 수를 늘려도 결과가 변하지 않아
  표본 부족이 아닌 신호 자체의 한계로 판단한다.
- 무작위 분할 시 동일 참가자가 학습·평가에 함께 포함되어 ROC-AUC 가
  0.9 이상으로 과대평가되므로, 참가자 단위 분할을 반드시 유지한다.
- 모델 교체 시 추론 서버의 반환 6필드 계약은 그대로 유지하며,
  model_version 값으로 규칙 판정 여부를 구분한다.
```

#### 교체 ② — 「모델링 목표」 칸

| 항목 | 내용 |
|---|---|
| 개정 전 | `다변량 시계열 라이프로그의 비정상 패턴 조기 탐지 및 9개 감정/3단계 위험도 다중 클래스 분류 정확도 확보` |
| 개정 후 | `다변량 시계열 라이프로그의 비정상 패턴 조기 탐지 및 3단계 위험도 판정. 감정 코드는 확보 가능한 데이터에 라벨이 없어 규칙 기반으로 산출한다` |

#### 교체 ③ — 「학습 모델」 칸

| 항목 | 내용 |
|---|---|
| 개정 전 | `- LSTM Autoencoder (비지도 시계열 이상 감지) + LightGBM Classifier (지도 정서 위험도 분류)` |
| 개정 후 | `- 목표 구조: LSTM Autoencoder(비지도 이상 감지) + LightGBM Classifier(지도 위험도 분류)`<br>`- 현재 적용: 개인별 기준값 대비 편차 규칙 판정(model_version = rule-placeholder-v0)` |

---

#### ⚠ 02 와의 불일치도 함께 정리해야 합니다

| 문서 | 현재 문장 |
|---|---|
| 02 `MLCM_210` 3단계 | 9개 감정 코드(EMOTIONS) 중 하나를 `emotion_id` 로 **분류** |
| 03 학습 방법 | GLOBEM 의 **BDI-II 우울 척도 라벨**로 지도학습 |

**BDI-II 로 학습하면 우울 이진 분류가 나오지 실제로 9종 감정이 나오지
않습니다.** 두 문장은 처음부터 서로 맞지 않았습니다.

02 는 **목표 설계로 유지**하고 03 에 위 실측을 적어 「목표는 9종, 현재는
규칙 3종, 이유는 라벨 부재」가 문서상 드러나게 하는 것을 권합니다.
02 까지 고치면 유스케이스 정의가 흔들려 04·05 로 번집니다.

> 실측 원본 — [`ai/README.md`](../../ai/README.md) ·
> [`감정라벨_데이터_후보_20260802.md`](../검증/감정라벨_데이터_후보_20260802.md)
> 재현 — `python ai/train/train_stage2.py` · `python ai/train/eval_lifesnaps.py`

</details>

## 3. 표기 규칙

- **[교체]** 해당 문장/항목을 통째로 바꿈 · **[추가]** 기존 내용 뒤에 덧붙임 · **[확인]** 사실관계 확인 후 판단
- ⭐ 심사 방어에 직결 · ⭐⭐ 그중에서도 우선

> ### ⚠ PPTX 를 저장할 때
> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.99MB → 305KB, 폰트 6개 → 0개로 실제 발생). `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.
>
> 텍스트만 바꾸는 것이라면 `tools/patch-pptx-text.ps1` 처럼 **XML 을 직접 고치는 편이 안전합니다.** 서식을 건드리지 않아 폰트가 풀리지 않습니다.

> ### ⚠ 붙여넣을 때마다 확인하세요
> 실제로 다섯 번 다 사고가 났습니다.
> 1. **마크다운 기호** — `**굵게**` 의 별표, `` `코드` `` 의 백틱이 딸려 들어갑니다
> 2. **스마트 따옴표** — 한글이 `'FRIEND'` 를 `'FRIEND’` 로 바꿉니다. **SQL 이 실행되지 않습니다**
> 3. **고칠 때 한 글자 더 지우기** — 따옴표를 고치다 옆의 정상 따옴표까지 지워 `IN ('FRIEND,` 가 된 적이 있습니다
> 4. **앞뒤 줄까지 붙여넣기** — 코드 블록이 위치를 잡으려고 **앞뒤 줄을 함께 보여줄 때가 있습니다.** 그 줄들은 이미 문서에 있으니 빼고 붙여넣으세요
> 5. **글자 조각이 남기** — 비고를 고치다 `heart_rate` 의 조각인 `eart_rat` 가 남은 적이 있습니다. **고친 칸은 끝까지 읽으세요**

> ### ⚠ 축약 표기를 그대로 옮기지 마세요
> 표 안에서 자리를 아끼려고 `단위 + DEFAULT 0` 처럼 줄여 쓴 것이 문서에 그대로 들어간 적이 있습니다. 실제로 들어갈 값은 `단위: m, DEFAULT 0` 이었습니다.
>
> **작성 측 규칙** — 표의 「교체」 칸에는 **문서에 들어갈 문자열을 그대로** 적는다. 설명이 필요하면 표 밖에 쓴다.

> ### ⚠ 반영 여부를 문구 검색으로만 판정하지 마세요
> **grep 0건이 미반영을 뜻하지 않습니다.** `01-G`·`01-H②` 가 처음 0건으로 나왔는데 실제로는 반영돼 있었고 **줄바꿈이 문구를 쪼갠 것**이었습니다(`앱에서 최소 15분` / `간격 전송`). 반드시 절 전체를 읽어 확인하세요.
>
> **반대로 검색이 오탐을 낼 수도 있습니다.** 01 의 `혈압` 1건은 활용방안이 아니라 **시장 현황의 통계 인용**(한국미디어패널조사)이라 문제없었습니다.

---

## 4. 문서를 고친 뒤

1. **HWP 를 고쳤으면 PDF 로 다시 내보냅니다.** `tools/doc2txt.py` 는 HWP 를 읽지 않아서, PDF 를 새로 뽑지 않으면 추출본이 옛 내용 그대로입니다
2. `python tools/doc2txt.py` 로 재추출합니다
3. **원본과 추출본을 같은 커밋에** 넣습니다. 그래야 버전 간 diff 가 보입니다

> 실제로 PDF 는 최신본인데 추출본은 이전 판인 커밋이 나온 적이 있습니다(`7922122`). 재추출을 건너뛰면 다음 사람이 옛 내용을 근거로 판단하게 됩니다.
