# 산출물 문서 개정 체크리스트

> **최종 갱신** 2026.08.04 · **작성자** 이응균(PM) · **목표 완료일** 2026.08.11
> **대상** 기획서~테이블명세서 HWP 5종 + 화면설계서 PPTX
> (**발표자료 PPTX 는 범위 밖입니다** — 아래 1절)
> **현행성 점검** 2026.08.04 · 완료 항목은 작업이력으로 이동했고 현재 남은 범위만 기재
>
> ## ⚠ 2026.08.03 — 범위가 다시 열렸습니다
>
> 7/29 에 시작한 개정이 `SD-B①` 까지 **전부 끝났습니다**(아래 2절 참조).
> 그런데 끝난 빅데이터분석정의서를 기준으로 기획서를 대조하니 **기획서가 빅데이터분석정의서와 반대 결론을
> 들고 있습니다.** 기획서는 아직 F1 80% 를 목표로 걸고 있고, 빅데이터분석정의서는 그 목표에
> 도달하지 못해 채택하지 않았다고 적혀 있습니다.
>
> **개정 27건 중 12건이 반영됐습니다** → [`기획서_개정안.md`](../결정/기획서_개정안.md)
> 같은 개정으로 「선제 접촉」 기능을 함께 세웁니다. 모순을 푸는 방법이 곧
> 차별점을 세우는 방법이라 둘을 나눌 수 없습니다.
>
> **이 문서에는 아직 반영되지 않은 항목만 남깁니다.**
> 완료된 결정과 그 근거 → [`작업이력.md`](작업이력.md) · 남은 일 → [`SESSION-HANDOFF.md`](../SESSION-HANDOFF.md)

---

## 1. 진행 현황

| 문서 | 남은 항목 | 담당 |
|---|---|---|
| **요구사항정의서** | **11 — `PL-14`~`PL-19` · `PL-25` · `PL-27` · `PL-28`~`PL-30`** ⭐⭐ **가장 급함** | 이응균 |
| 기획서 | **3 — `PL-07`·`PL-08`·`PL-09`** (12건 반영 완료 ✅) | 이응균 |
| 빅데이터분석정의서 | **1 — 모델링 목표에 「개인 내 변화 탐지」 반영** | 김건영 |
| 데이터베이스요구사항분석서·테이블명세서 | **1 — 알림 수신 동의 + 접촉 이력** (`fcm_token` 은 이미 있음) | 윤일준 |
| 화면설계서 PPTX | **1 — `PL-26` `MAIN_LIFELOG_01` ❺ 문안 1줄** | 함은선 |
| ~~발표자료 PPTX~~ | **범위 밖 — 8/28 준비 시점으로 이월** (`PL-20`~`PL-24`) | 이응균 |

> ### ✅ 기획서 12건 반영 (2026.08.03)
>
> `PL-01`~`PL-06` · `PL-10`~`PL-13`. **F1 80% 목표가 기획서에서 사라졌고
> `OUTREACH` 가 대응표에 들어갔습니다.** `Recall 90%` 는 측정 가능하고 빅데이터분석정의서와
> 일치하므로 그대로 뒀습니다.
>
> `PL-07`·`PL-08` 은 **주 문장이 반영되고 짝이 되는 줄만 남았습니다** —
> 1장 주요 기술 요약과 PMData 줄입니다. 한 문서 안에서 앞뒤가 갈린 상태라
> 우선순위가 낮지 않습니다.

> ### ⚠ 세지 말고 돌리세요
>
> ```bash
> python tools/check_docs.py
> ```
>
> 2026.08.04 실측 — **모델명 27건**(요구사항정의서가 15 · 기획서가 7 · 발표자료 2 · 그 외 3) ·
> **성능목표 4건**(요구사항정의서가 2 · 발표자료 2).
> **이 문서에 적힌 숫자는 적는 순간부터 틀리기 시작합니다.**
>
> ⚠ **검사기는 0 이 되지 않습니다.** 발표자료 몫 4건이 이월분이라 계속 잡힙니다
> (아래). **요구사항정의서와 기획서만 보세요** — 그쪽이 0 이 되면 이번 범위는 끝입니다.

> ### 발표자료 PPTX 는 이번 범위에서 뺐습니다 (2026.08.04 · PM 결정)
>
> `프로젝트_기획서_발표자료_귀기울임.pptx` 는 **기획서 발표용이고 그 발표는
> 이미 끝났습니다.** 지난 문서를 소급해 고칠 이유가 없어 `PL-20`~`PL-24` 를
> **8/11 마감 범위에서 뺍니다.**
>
> **폐기가 아니라 이월입니다.** 최종 발표는 **8/28** 이고 아직 착수하지
> 않았습니다. 그 자료를 **이 PPTX 에서 파생시킨다면 아래가 그대로 따라옵니다.**
>
> - 「핵심 성과 지표」 슬라이드의 **`85% 위험도 분류 정확도` · `0.80 위험 단계
>   F1-Score`** KPI 타일 — 빅데이터분석정의서가 "도달하지 못해 채택하지 않음"으로 기록한
>   수치입니다. **문서는 펼쳐 봐야 걸리지만 이건 화면에 그대로 뜹니다**
> - Tech Specs 의 `LSTM Autoencoder + LightGBM` 표기
> - 교체 문안은 [`기획서_개정안.md`](../결정/기획서_개정안.md) `PL-20` 에
>   **확정본으로 있습니다.** 8/28 자료를 새로 만들더라도 여기부터 읽으세요
>
> 고칠 때는 `tools/patch-pptx-text.ps1` 을 쓰세요. PowerPoint 로 다시 저장하면
> 임베드 폰트가 풀립니다(5.99MB → 305KB 실제 발생).

> **화면설계서가 안 걸립니다.** 알림 토글은 `MAIN_SETTING_01` ❷ 에 이미
> 명세돼 있고, 선제 발화는 기존 챗봇 화면 안에서 일어납니다. 8일 남은
> 상황에서 이게 가장 큰 이점입니다.
>
> **FCM 도 신규가 아닙니다.** `NFR-DV-002`·`MLCM_200` 7단계·`MLCM_400`
> 5단계가 이미 FCM 을 요구하고 `fcm_token` 컬럼도 데이터베이스요구사항분석서·테이블명세서에 있습니다.
> 구현이 없을 뿐입니다.
>
> ⚠ **문안·근거는 전부 [`기획서_개정안.md`](../결정/기획서_개정안.md) 에
> 있습니다.** 여기에 복사하지 마세요 — 정본이 둘이 되면 반드시 어긋납니다.

## 2. 이전 범위 — 전부 종료 (2026.08.03 확인)

7/29 에 시작한 산출물 개정이 **전부 끝났습니다.** 마지막까지 남아 있던
`SD-B①`(빅데이터분석정의서 학습 방법 교체)도 이미 반영돼 있었고, 체크리스트가 뒤처져
있었던 것입니다.

| 확인한 것 | 위치 |
|---|---|
| 학습 방법 (ROC-AUC 0.528 / 0.479~0.540) | 빅데이터분석정의서 추출본 179행 |
| 모델링 목표 (「감정 코드는 … 규칙 기반으로 산출」) | 빅데이터분석정의서 추출본 177행 |
| 6필드 계약 · `model_version` 구분 | 빅데이터분석정의서 추출본 183~184행 |

**본문은 들어냈습니다.** 개정 문안과 근거는
[`작업이력.md`](작업이력.md) 에 있고, 실제 문구는 `docs/extracted/`
추출본이 정본입니다. 새 범위는 1절과
[`기획서_개정안.md`](../결정/기획서_개정안.md) 을 보세요.

> ⚠ **요구사항정의서 `MLCM_210` 3단계와 빅데이터분석정의서 학습 방법이 처음부터 서로 맞지 않았습니다.**
> 요구사항정의서는 「9개 감정 코드 분류」, 빅데이터분석정의서는 「BDI-II 우울 척도 라벨로 지도학습」
> 이었습니다. BDI-II 로 학습하면 우울 이진 분류가 나오지 9종이 나오지
> 않습니다. **이번 개정(`PL-01`·`PL-16`~`PL-18`)이 이 불일치까지 함께
> 정리합니다.**

## 3. 표기 규칙

- **[교체]** 해당 문장/항목을 통째로 바꿈 · **[추가]** 기존 내용 뒤에 덧붙임 · **[확인]** 사실관계 확인 후 판단
- ⭐ 심사 방어에 직결 · ⭐⭐ 그중에서도 우선

> ### ⚠ PPTX 를 저장할 때
> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.99MB → 305KB, 폰트 6개 → 0개로 실제 발생). `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.
>
> 텍스트만 바꾸는 것이라면 `tools/patch-pptx-text.ps1` 처럼 **XML 을 직접 고치는 편이 안전합니다.** 서식을 건드리지 않아 폰트가 풀리지 않습니다.

> ### ⚠ 붙여넣을 때마다 확인하세요
> 실제로 다섯 번 다 사고가 났습니다.
> 1. **마크다운 기호** — `**굵게**` 의 별표, `` `코드` `` 의 백틱이 딸려 들어갑니다
> 2. **스마트 따옴표** — 한글이 `'FRIEND'` 를 `'FRIEND’` 로 바꿉니다. **SQL 이 실행되지 않습니다**
> 3. **고칠 때 한 글자 더 지우기** — 따옴표를 고치다 옆의 정상 따옴표까지 지워 `IN ('FRIEND,` 가 된 적이 있습니다
> 4. **앞뒤 줄까지 붙여넣기** — 코드 블록이 위치를 잡으려고 **앞뒤 줄을 함께 보여줄 때가 있습니다.** 그 줄들은 이미 문서에 있으니 빼고 붙여넣으세요
> 5. **글자 조각이 남기** — 비고를 고치다 `heart_rate` 의 조각인 `eart_rat` 가 남은 적이 있습니다. **고친 칸은 끝까지 읽으세요**

> ### ⚠ 축약 표기를 그대로 옮기지 마세요
> 표 안에서 자리를 아끼려고 `단위 + DEFAULT 0` 처럼 줄여 쓴 것이 문서에 그대로 들어간 적이 있습니다. 실제로 들어갈 값은 `단위: m, DEFAULT 0` 이었습니다.
>
> **작성 측 규칙** — 표의 「교체」 칸에는 **문서에 들어갈 문자열을 그대로** 적는다. 설명이 필요하면 표 밖에 쓴다.

> ### ⚠ 반영 여부를 문구 검색으로만 판정하지 마세요
> **grep 0건이 미반영을 뜻하지 않습니다.** `01-G`·`01-H②` 가 처음 0건으로 나왔는데 실제로는 반영돼 있었고 **줄바꿈이 문구를 쪼갠 것**이었습니다(`앱에서 최소 15분` / `간격 전송`). 반드시 절 전체를 읽어 확인하세요.
>
> **반대로 검색이 오탐을 낼 수도 있습니다.** 기획서의 `혈압` 1건은 활용방안이 아니라 **시장 현황의 통계 인용**(한국미디어패널조사)이라 문제없었습니다.

---

## 4. 문서를 고친 뒤

1. **HWP 를 고쳤으면 PDF 로 다시 내보냅니다.** `tools/doc2txt.py` 는 HWP 를 읽지 않아서, PDF 를 새로 뽑지 않으면 추출본이 옛 내용 그대로입니다
2. `python tools/doc2txt.py` 로 재추출합니다
3. **원본과 추출본을 같은 커밋에** 넣습니다. 그래야 버전 간 diff 가 보입니다

> 실제로 PDF 는 최신본인데 추출본은 이전 판인 커밋이 나온 적이 있습니다(`7922122`). 재추출을 건너뛰면 다음 사람이 옛 내용을 근거로 판단하게 됩니다.
