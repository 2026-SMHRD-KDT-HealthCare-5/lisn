# SW활용 결과보고서 — 채워 넣을 내용 (초안)

> 대상 양식: `2. [KDT][헬스케어 5회차][SW활용 결과보고서_팀명(팀장명)양식.hwp`
> 작성 근거: 저장소 코드·실측 문서 (2026.08.03 기준)
>
> **`( 비움 )` 은 지금 채울 수 없는 칸입니다.** 그대로 두고 나중에 넣으세요.
> 아래 「비워둔 칸」에 무엇을 넣어야 하는지만 적어뒀습니다.

---

# 1쪽 — 본문

## 과정명

```
( 비움 )
```

## 프로젝트명

```
귀기울임(LISN) — 멀티모달 라이프로그 감정 분석 기반 맞춤형 LLM 케어 및 모니터링 시스템
```

## 팀원

```
이응균(팀장), 김건영, 윤일준, 함은선
```

## 사용 서비스 / 사용 기간

```
사용 서비스 : OpenAI API (Chat Completions · gpt-5.6)
사용 기간   : ( 비움 )
```

---

## 사용 목적

```
스마트워치에서 수집한 라이프로그로 정서적 위험 징후를 조기에 포착하고,
페르소나 기반 대화로 정서 케어를 제공하는 서비스입니다. OpenAI API 는
이 서비스에서 네 가지 일을 맡습니다.

1) 페르소나 기반 정서 케어 대화
   같은 사용자라도 원하는 대화 방식이 다릅니다. 공감받고 싶은 사람과
   해결책을 원하는 사람에게 같은 말투로 답하면 오히려 거리감이 생깁니다.
   시스템 프롬프트로 F형(따스한 공감형)과 T형(현실적인 조언형) 두 성격을
   주입해 사용자가 고르게 했습니다.

2) 위기 문맥 판정 (2단계 위기 탐지의 2차)
   "죽고 싶다" 같은 직접 표현은 키워드로 잡히지만, "강아지 사료를 넉넉히
   사뒀어요" 같은 신변 정리 표현은 사전으로 잡을 수 없습니다. 문맥을 읽는
   판정이 필요하고, 이 부분을 LLM 이 맡습니다.

3) 대화 세션 요약
   사용자가 자신의 정서 변화를 되짚어 볼 수 있도록 종료된 대화를 2~3문장으로
   요약합니다.

4) 홈 화면 일일 상태 요약
   수면·걸음·심박 같은 수치를 그대로 보여주지 않고, 관찰된 패턴을 사람의
   말로 옮겨 전달합니다.

※ 본 서비스는 정신건강을 진단하지 않습니다. 위험 징후의 가능성을 조기에
   포착해 상담 연계를 돕는 모니터링 보조 도구입니다.
```

---

## 적용 방법

### 1. 핵심기술 내용

```
① Structured Outputs — 위기 판정 결과의 스키마 보장
   위기 판정은 안전에 직결되므로 응답 형식이 흔들리면 안 됩니다.
   beta.chat.completions.parse() 에 Pydantic 스키마를 넘겨 is_crisis(bool),
   severity(등급), reason(근거)을 강제했습니다. 자유 텍스트를 파싱하는 방식과
   달리 파싱 실패 재시도 로직이 필요 없습니다.

② 시스템 프롬프트 분리 — 페르소나와 안전 규칙
   페르소나 프롬프트(FRIEND / COUNSELOR)와 안전 규칙(SAFETY_RULES)을 분리해
   두고, 어떤 페르소나를 고르든 안전 규칙이 항상 결합되게 했습니다.
   진단 금지, 자해 방법 제공 금지, 전문가 상담 권유가 여기 들어갑니다.

③ 병렬 호출 — 응답 생성과 위기 판정을 동시에
   asyncio.gather 로 두 호출을 함께 보냅니다. 순차로 하면 API 왕복이 두 번
   쌓여 응답 3초 요건을 지킬 수 없습니다.

④ 짧은 타임아웃과 재시도 0회
   timeout 8초, max_retries=0 으로 고정했습니다. 기본 재시도(2회)를 두면
   실패한 호출 하나가 응답 예산을 통째로 먹습니다(실측 13.95초). 빨리 포기하고
   키워드 단독 판정으로 넘기는 편이 안전합니다.
```

### 2. 적용방법

```
① 2단계 위기 탐지 — 키워드 규칙 + LLM 문맥 판정
   1차: 서버 내부 키워드 사전으로 선별 (외부 API 없이 동작)
   2차: OpenAI API 로 문맥 판정
   판정 결과는 서버가 확정하고 클라이언트는 따르기만 합니다. 규칙을 앱에
   복제하면 반드시 어긋나기 때문입니다.

   ⚠ 키워드 필터를 서버 내부에 둔 이유가 요건에 있습니다(NFR-DV-003).
     외부 API 가 장애여도 위기 탐지가 단독으로 동작해야 합니다.

② 위험도에 따른 자동 대응 전환
   NORMAL   → 대화 계속
   CAUTION  → 힐링 콘텐츠 추천
   CRITICAL → 콘텐츠 추천 중단 후 긴급 상담(109) 화면으로 전환

③ 개인정보 마스킹 후 전송
   전화번호 등 개인식별정보를 마스킹한 뒤 API 로 보내고, 저장도 마스킹된
   상태로 합니다.

④ 스트리밍을 쓰지 않음
   응답을 한 번에 받습니다. 위기 판정이 CRITICAL 로 나오면 생성된 응답을
   버려야 하는데, 스트리밍으로 이미 흘린 글자는 회수할 수 없습니다.

⑤ 작업별 호출 분리
   reply(대화) · crisis(위기 판정) · summary(세션 요약) · daily(일일 요약)
   네 작업을 나눠 호출합니다.
```

### 3. 서비스 구조(아키텍처)

```
[수집]  Flutter 앱 · Health Connect (Android)
        수면 · 걸음 · 심박 · HRV        최소 15분 간격
                  |  HTTPS / REST
                  v
[비즈니스 서버]  FastAPI  <->  PostgreSQL
        JWT 인증 · 라이프로그 적재 · 키워드 위기 필터(1차)
                  |                          |
                  |  내부 API                |  ← 여기가 유료 SW 적용 지점
                  v                          v
[AI 추론 서버]              [OpenAI API]
 라이프로그 편차 기반 판정    페르소나 응답 · 위기 문맥 판정(2차)
 감정 · risk_score           세션 요약 · 일일 요약
                  |
                  v
[시스템 액션]  NORMAL→대화  |  CAUTION→콘텐츠  |  CRITICAL→긴급상담

※ 대용량 시계열 수집과 LLM 추론 부하가 서로 간섭하지 않도록 비즈니스 서버와
  AI 추론 서버를 분리했습니다.
※ 위기 문맥 탐지는 비즈니스 서버에 둡니다. AI 추론 서버에 두면 그 서버가
  죽을 때 위기 탐지도 같이 죽습니다.
```

---

## 결과

### 1. 적용 및 테스트 과정

```
① 회귀 테스트
   백엔드 60건 · Flutter 앱 131건 전부 통과 (2026.08.03 기준)
   API 30개 · 앱 화면 14개 · 관리자 관제 웹 2개가 모두 실제 API 에 연동됨

② 위기 판정 평가셋 구축
   위기 표현 유형을 나눠 200건 규모의 평가셋을 만들었습니다.
   직접적 의도(S1)처럼 키워드로 잡히는 유형과, 작별 인사(S5)·신변 정리(S6)·
   완곡한 은유(S8)처럼 사전으로 잡을 수 없는 유형을 구분했습니다.
   채점 도구: tools/eval_crisis.py

③ 비기능 요건 실측
   측정 도구: tools/bench_nfr.py
   한 번 재고 통과로 적지 않고, 중앙값과 최댓값을 함께 확인했습니다.

④ 장애 상황 시험
   외부 API 가 실패하는 상황을 만들어, 위기 탐지가 키워드 단독으로 계속
   동작하는지 확인했습니다.
```

### 2. 결과에 대한 분석 내용

```
① 유료 SW(OpenAI)로 평가셋 200건 전량을 채점했습니다

   개발 중에는 무료 한도(모델당 하루 20건) 때문에 15건밖에 재지 못했습니다.
   결제 후 한도가 풀려 고정 평가셋 200건을 운영 모델로 전량 채점했습니다.

     판정 200건   TP 91  FN 12  FP 4  TN 93
     재현율 0.883   정밀도 0.958   F1 0.919

   목표는 재현율 0.900 · F1 0.850 이므로 F1 은 통과, 재현율은 미달입니다.

   측정 과정에서 판정 로직의 결함 세 가지를 찾아 고쳤고, 고칠 때마다 같은
   평가셋으로 다시 쟀습니다.

     회차 0  원본                          F1 0.614
     회차 1  자해 발생·진행 상태 규칙 추가   F1 0.641
     회차 2  평가 방식 정합 + 판정 기준 정정 F1 0.865
     회차 3  사회적 고립 범주 추가          F1 0.919

   가장 크게 바뀐 것은 회차 2 입니다. 무기력·무의미감을 판정에서 빼도록
   지시하고 있었는데, 평가 기준은 그것을 위기로 세고 있었습니다. 기준을
   맞추자 재현율이 0.476 에서 0.777 로 올랐고 정밀도는 거의 그대로였습니다.
   오탐을 감수한 것이 아니라 원래 잡아야 할 것을 안 잡고 있었습니다.

   ⚠ 남은 미탐 12건 중 3건은 문맥이 있어야 풀립니다.
     - "보험 서류 어디 있는지 엄마한테 알려드렸어요."
     - "집 열쇠를 주인아저씨한테 미리 맡겨뒀어요."
     운영은 최근 대화를 함께 넣지만 평가셋에는 문맥이 없어 한 문장만으로
     판정합니다. 사람도 이 한 줄로는 맞히기 어렵습니다.

   ⚠ 이 수치는 최종 성적이 아닙니다. 200건 중 180건이 AI 초안이라 사람
     검수 후 다시 재야 합니다.

② 외부 API 장애 시에도 위기 탐지가 동작했습니다

     "요즘 정말 죽고 싶다는 생각이 들어요"
       → 판정 EMERGENCY · 중앙값 158ms · 최댓값 187ms

   외부 API 가 죽어도 긴급 화면이 0.2초 안에 뜹니다. 키워드 필터를 서버
   내부에 둔 설계가 요건대로 동작함을 확인했습니다.

③ 아직 측정하지 못한 것을 함께 적습니다

   대화 응답 3초 요건과 정상 경로의 위기 전환 시간은 측정하지 못했습니다.
   측정 도중 API 호출이 실패해, 실제로는 실패 왕복 시간을 잰 것이었기
   때문입니다. 응답 시간만 보면 정상처럼 보여 알아채기 어려웠습니다.
   ( 재측정 후 수치 보완 )
```

---

# 2쪽 — 증빙사진(결과화면)

권장 캡처 2장입니다. 유료 SW 가 실제로 동작한 화면이어야 합니다.

| | 무엇을 | 왜 |
|---|---|---|
| **사진 1** | AI 챗봇 대화 화면 — 페르소나 응답이 보이는 상태 | 대화 생성이 적용된 결과가 한눈에 보입니다 |
| **사진 2** | 위기 판정 후 긴급 상담 화면으로 전환된 모습 | 위기 문맥 판정 → 자동 대응 전환까지 이어진 결과입니다 |

- 대안: 관리자 관제 웹의 **위기 사건 이력**, 또는 **대화 기록의 요약 목록**
  (세션 요약이 적용된 결과)
- ⚠ 캡처 전에 `LLM_PROVIDER=openai` 로 띄우세요. 개발 기본값으로 찍으면
  유료 SW 결과 화면이 아닙니다
- ⚠ 폴백 문구(「지금 답변을 준비하는 데 시간이 걸리고 있어요」)가 뜬 화면은
  쓰지 마세요. API 호출이 실패한 화면입니다

---

# 비워둔 칸 — 나중에 채울 것

| 칸 | 넣을 것 |
|---|---|
| 과정명 | 정식 과정명 표기 |
| 사용 기간 | API 실제 사용 기간(결제일 ~ 마지막 호출일). 프로젝트 기간과 다릅니다 |
| 결과 ③ 재측정 | 대화 응답 3초·정상 경로 위기 전환 시간 |
| 증빙사진 2장 | `LLM_PROVIDER=openai` 로 띄운 뒤 캡처 |

## 참고 — 지금 상태에서 알아둘 것

**결제 금액이 문서마다 다릅니다.** 증빙(카드 매출전표) 금액이 기준입니다.

| 문서 | 금액 |
|---|---|
| `1. 구매완료리스트` 1행 | $40.00 / ₩57,920 |
| `1. 구매완료리스트` 합계행 | 30 / 42,240 |
| `3. 결제 증빙 자료` | US$5.50 |

**✅ 2026.08.03 — OpenAI 가 활성화되어 위기 판정 성능은 유료 SW 로 재측정을
마쳤습니다.** 위 「결과 ①」이 그 수치입니다(`gpt-5.6`, 평가셋 200건 전량).
근거는 [`USAGE_LOG.md`](../llm/USAGE_LOG.md) `LLM-007`.

**아직 Gemini 기준인 것이 남아 있습니다.**

| 항목 | 상태 |
|---|---|
| 위기 판정 재현율·F1 (`NFR-AI-001`) | ✅ OpenAI 재측정 완료 |
| 대화 응답 3초 (`NFR-DV-001`) | ⏳ **미측정** — `tools/bench_nfr.py` 재실행 필요 |
| 정상 경로 위기 전환 (`NFR-TS-001`) | ⏳ **미측정** — 같음 |
| 키워드 단독 폴백 (`NFR-DV-003`) | ✅ 로컬 함수라 공급자 무관 |

⚠ `bench_nfr.py` 는 백엔드(8000)·AI 서버(8001)가 떠 있어야 합니다.
**`$env:LLM_PROVIDER='openai'` 로 띄운 뒤** 재실행하세요.
