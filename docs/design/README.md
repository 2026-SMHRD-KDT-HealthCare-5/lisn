# 화면 시안 (docs/design)

화면설계서에 넣을 신규 화면 시안입니다. **코드로 만들고 PNG 로 굽습니다.**

> **최종 점검** 2026.07.31 · 시안 6장은 모두 완료됐습니다. 이 표의 “완료”는
> 디자인 산출물 기준이며 실제 앱·웹 구현 완료를 뜻하지 않습니다.

```
docs/design/
├── MAIN_*.png                앱 시안 4장 (780×1688 = 390×844 @2x)
├── ADMIN_*.png               관리자 웹 시안 2장 (1280×800)
└── src/
    ├── common.css              공통 팔레트·카드·폼 스타일
    └── *.html                  화면별 원본 HTML
```

## 왜 이 방식인가

화면설계서의 기존 시안 9장은 **고충실도 디자인**(마스코트 일러스트 포함)이고 Figma 등에서 뽑은 PNG 입니다. 신규 화면 6장도 같은 결로 맞춰야 하는데,

- **Figma REST API 는 읽기 전용**입니다. 노드 생성은 Plugin API 로만 되고 그건 Figma 안에서 실행돼야 해서 외부에서 만들 수 없습니다.
- 신규 6장은 전부 **폼·리스트·차트**라 마스코트가 필요 없습니다. 팔레트와 카드 스타일만 맞추면 기존 시안 옆에 놓아도 위화감이 없습니다.

## 만드는 방법

### 1. 팔레트는 기존 시안에서 직접 추출한다

눈대중으로 맞추면 미세하게 어긋납니다. PPTX 안의 PNG 를 꺼내 픽셀을 찍습니다.

```powershell
Add-Type -AssemblyName System.Drawing
$bmp = New-Object System.Drawing.Bitmap("image9.png")
$c = $bmp.GetPixel(10, 10)
"#{0:X2}{1:X2}{2:X2}" -f $c.R, $c.G, $c.B
```

추출 결과 (`image9.png` = 메인 홈 대시보드 기준):

| 용도 | 값 |
|---|---|
| 배경 | `#EDF2FF` |
| 카드 | `#FFFFFF` |
| 제목 텍스트 | `#24325F` |
| 보조 텍스트 | `#A8ACBA` |
| 포인트 (활성 아이콘) | `#8A9CF0` |
| 주행동 버튼 | `#5A6BE0` |
| 파스텔 민트 / 블루 / 피치 | `#EAF8F4` / `#EAF5FF` / `#FFF0E9` |

### 2. 캔버스는 390 × 844

기존 시안과 동일합니다. 관리자 웹 화면은 1280 × 800 으로 잡습니다 (앱과 달라 보이는 게 정상).

### 3. 폰트는 Pretendard 를 CDN 으로 부른다

시안의 둥근 한글 서체가 Pretendard 계열입니다. 설치하지 않고 웹폰트로 불러도 **PNG 로 구워지므로 어느 PC 에서 열어도 동일**합니다.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css">
```

### 4. 아이콘은 이모지 대신 인라인 SVG

**이모지는 OS 가 자기 색으로 렌더해서 팔레트 밖으로 튑니다.** SVG 로 그리고 `fill`·`stroke` 를 팔레트 값으로 고정하세요.

### 5. Edge 헤드리스로 렌더

이 PC 에는 Node·Python 이 없지만 Edge 는 있습니다.

```powershell
$edge = "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"
& $edge --headless=new --disable-gpu --hide-scrollbars `
        --force-device-scale-factor=2 --window-size=390,844 `
        --virtual-time-budget=4000 `
        --screenshot="out.png" "file:///.../src/화면.html"
```

- `--force-device-scale-factor=2` — 2배 해상도. 발표 화면에서 안 뭉갬
- `--virtual-time-budget=4000` — 웹폰트 로딩을 기다림. 없으면 폰트가 안 붙은 채 찍힘

## 디자인 규칙

- **경고색(빨강·주황)을 쓰지 않습니다.** 정신건강 위기 UI 에서 경고색은 불안을 키워 회피를 유발합니다. 주목도는 색이 아니라 구조로 만듭니다 — 하단 네비게이션 제거, 요소 최소화, 주행동 버튼에만 그림자.
- 문구는 진단·단정이 아닌 권유조로 씁니다. `MLCM_510` 및 `FR-AI-002`(진단 금지)의 요건입니다.

## 진행 현황

### 신규 화면 6장 (Part B)

| 화면 | 캔버스 | 상태 |
|---|---|---|
| `MAIN_EMERGENCY_01` 긴급 상담 연결 | 390×844 | ✅ 시안·Flutter 구현 완료 |
| `MAIN_LOGIN_02` 비밀번호 재설정 | 390×844 | ✅ 완료 |
| `MAIN_SETTING_02` 계정 관리·회원 탈퇴 | 390×844 | ✅ 완료 |
| `MAIN_REPORT_01` 정서 리포트 | 390×844 | ✅ 완료 |
| `ADMIN_LOGIN_01` 관리자 로그인 | 1280×800 | ✅ 시안·React 구현 완료 |
| `ADMIN_DASH_01` 관리자 관제 | 1280×800 | ✅ 시안·React 셸 완료, 데이터 API 대기 |

### 기존 화면 9장 (slide7~15) — 2026.07.31 재제작

**기존 Figma 시안이 확정 문안·HWP 데이터 모델과 어긋나 다시 만들었습니다.** PNG 를
슬라이드 이미지와 교체하면 됩니다.

| 화면 | slide | 기존 시안과 달라진 점 |
|---|---|---|
| `MAIN_LOGIN_01` | 7 | 「아이디」 → **이메일**, **비밀번호 찾기** 진입 추가 |
| `MAIN_JOIN_01` | 8 | **민감정보(생체·건강) 동의를 별도 항목으로 분리** (`SD-A③`) |
| `MAIN_JOIN_02` | 9 | **키(선택)** 추가 · 성별 **3종**(`MALE`/`FEMALE`/`OTHER`) · 「비상 연락처」→**연락처** |
| `MAIN_JOIN_03` | 10 | 수집 항목을 `consent_scopes` 기본값과 일치시킴 (활동량·수면 on, 체성분 off) |
| `MAIN_CHAT_01` | 11 | 페르소나 2종 카드 · 성격 변경 경로 안내 |
| `MAIN_CHAT_02` | 12 | **대화 기록 목록·요약·삭제** 추가 |
| `MAIN_HOME_01` | 13 | **AI 한줄 요약** 카드 추가 · 추천을 **영상·음악·운동**으로 정정 |
| `MAIN_LIFELOG_01` | 14 | **활동량·수면·생체·체성분 4종 차트**로 전면 재구성, **골격근량** 포함 |
| `MAIN_SETTING_01` | 15 | **웨어러블 항목별 동의 토글 · 페르소나 변경**으로 재구성 |

### 마스코트 에셋 (`assets/`)

마스코트는 래스터 일러스트라 HTML 로 다시 그릴 수 없습니다. **기존 PNG 에서 잘라내
재사용**합니다. 새로 그리면 나머지 시안과 결이 어긋납니다.

| 파일 | 출처 | 용도 |
|---|---|---|
| `header-login.png` | slide7 `image1.png` 상단 228px | 로그인 헤더 통째 |
| `mascot.png` | slide13 `image9.png` (210,22,180,158) | 홈 헤더 |
| `avatar-chat.png` | slide12 `image8.png` (44,16,40,40) | 챗봇 아바타·홈 감정 카드·페르소나 카드 |

배경이 `#EDF2FF` 로 균일해 **사각 크롭이 그대로 붙습니다.** 별도 누끼가 필요 없습니다.

명세는 [`../결정/화면설계서_개정안.md`](../결정/화면설계서_개정안.md) Part B,
확정 화면설명 문안은 [`../진행/작업이력.md`](../진행/작업이력.md) 21차 참조.

## ⚠ HWP 대조에서 나온 불일치 (미해결)

**시안은 HWP(기획서·요구사항정의서·데이터베이스요구사항분석서·테이블명세서)와 `db/schema.sql` 기준으로 맞췄습니다.** 그 과정에서 화면설계서
PPTX 와 HWP 가 어긋나는 곳이 나왔습니다.

| 항목 | 화면설계서 PPTX | 기획서·요구사항정의서·데이터베이스요구사항분석서·테이블명세서 · `schema.sql` | 시안 처리 |
|---|---|---|---|
| `USERS.phone` | 「**비상** 연락처」 | 「연락처」 (데이터베이스요구사항분석서 2절, 테이블명세서 컬럼 정의) | **연락처**로 표기 |
| `USERS.gender` | 남자 / 여자 2종 | `CHECK IN ('MALE','FEMALE','OTHER')` **3종** | **3종**으로 표기 |

- **「비상 연락처」는 의미가 다릅니다.** 비상연락처는 보호자 등 제3자 번호이고
  `phone` 은 본인 연락처입니다. 별도 컬럼이 없으므로 **둘 중 하나로 정해야** 합니다.
  본인 연락처로 두려면 화면설계서 문구를, 보호자 연락처로 두려면 데이터베이스요구사항분석서·테이블명세서에 컬럼을 고쳐야 합니다.
- 성별은 `OTHER` 가 스키마에 이미 있어 **화면에 선택지가 없으면 입력 불가능한 값**이 됩니다.

## 시안을 다시 렌더하는 절차

각 화면의 HTML을 수정한 뒤 같은 이름의 PNG로 다시 렌더합니다. 새 화면을 추가할 때는 `src/MAIN_EMERGENCY_01.html`을 복사하고 `common.css`의 공통 스타일을 재사용하세요.

```powershell
# 1. 복사
Copy-Item docs\design\src\MAIN_EMERGENCY_01.html docs\design\src\MAIN_REPORT_01.html

# 2. 내용 수정 (아래 화면별 구성 참고)

# 3. 렌더
$edge = "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe"
& $edge --headless=new --disable-gpu --hide-scrollbars `
        --force-device-scale-factor=2 --window-size=390,844 `
        --virtual-time-budget=4000 `
        --screenshot="docs\design\MAIN_REPORT_01.png" `
        "file:///$((Get-Location).Path -replace '\\','/')/docs/design/src/MAIN_REPORT_01.html"
```

관리자 웹 2장은 `--window-size=1280,800` 으로 바꾸고, HTML 의 `html, body { width/height }` 도 함께 고치세요.

### 화면별 구성

각 화면의 항목(❶❷❸)은 [`../결정/화면설계서_개정안.md`](../결정/화면설계서_개정안.md) Part B 에 문장으로 정의돼 있습니다. 그대로 UI 요소로 옮기면 됩니다.

| 화면 | 주요 구성 | 참고 |
|---|---|---|
| `MAIN_LOGIN_02` | 이메일 입력 → 발송 안내 → 새 비밀번호 입력 | 폼. 카드 2개면 충분 |
| `MAIN_SETTING_02` | 계정 정보 · 비밀번호 변경 · 탈퇴 버튼 · 삭제 범위 안내 | 리스트 + 경고성 안내 카드 |
| `MAIN_REPORT_01` | 기간 선택 · 감정 추이 곡선 · 위험 단계 분포 · PDF 내보내기 | **차트는 인라인 SVG 로 직접 그리세요.** 라이브러리를 CDN 으로 불러오면 렌더 타이밍이 어긋납니다 |
| `ADMIN_LOGIN_01` | 웹 중앙 정렬 로그인 폼 | 앱과 달라 보이는 게 정상 |
| `ADMIN_DASH_01` | 위험도 분포 요약 3칸 · 대상자 테이블 · 상세 패널 | 웹이라 여백과 밀도가 앱과 다름 |

### 주의

- **차트를 이미지로 넣지 마세요.** SVG 로 그리면 팔레트가 정확히 맞고 수정도 쉽습니다
- 리포트·대시보드의 숫자는 **그럴듯한 더미**를 넣되, `schema.sql` 의 값 범위를 지키세요 (`emotion_score` 0~100, `risk_level` 3종 등). 범위를 벗어난 값이 시안에 있으면 심사에서 지적됩니다
- 관리자 화면에 **실명을 넣지 마세요.** 위험도 목록은 `user_id` 앞 8자리 정도로 표기하는 편이 개인정보 설계와 일관됩니다

## 남은 작업 (2026.07.31 기준)

### 1. 슬라이드 이미지 교체 — 함은선

`docs/design/*.png` 15장을 PPTX 에 넣습니다.

- **slide7~15 (9장)** — 기존 이미지를 **교체**. 슬라이드 추가가 아니라 이미지 바꿔끼우기라 안전합니다
- **신규 6장** — 슬라이드를 새로 만들어야 합니다. XML 4곳 등록이 필요해 코드로는 미검증이니
  **기존 화면 슬라이드를 복제해 이미지·텍스트만 교체**하세요
- 교체 후 **콜아웃 번호(❶❷❸)를 시안 위에 다시 얹어야** 합니다. 시안에는 번호가 없습니다.
  특히 `MAIN_JOIN_02` 는 키 입력이 들어가 **❾까지 아홉 개**로 늘어납니다

### 2. 결정 필요 2건

- **`phone` 을 「연락처」로 볼지 「비상 연락처」로 볼지** (위 불일치 표)
- 메뉴 구성도 루트 노드 이름 · 「감정 변화 기록」 노드 처리
  → [`../결정/화면설계서_개정안.md`](../결정/화면설계서_개정안.md) 「결정 필요」

### 3. 시안 다듬기 (선택)

- `MAIN_CHAT_01` 페르소나 카드는 **아바타가 공용 이미지 하나**입니다. 성격별로 표정을
  다르게 하려면 일러스트가 추가로 필요합니다
- 홈 감정 카드의 얼굴도 챗봇 아바타를 재사용했습니다. 원본 슬라이드의 큰 구름 얼굴을
  쓰려면 **누끼 딴 PNG 가 따로 필요**합니다 (카드 흰 배경 위라 사각 크롭이 안 붙습니다)

## 남은 디자인 작업

- 상단 브랜드 표시는 현재 앱이 `마음이` + 포인트 색 하트를 사용합니다. 이 상태를 최종
  규칙으로 확정할지는 팀 확인이 필요하며, 운영 자산으로 옮길 때는 이모지 대신 SVG를
  권장합니다.
- **PPTX 에 슬라이드를 새로 추가하는 것은 미검증**입니다. slide XML · rels · presentation.xml · [Content_Types].xml 네 곳을 등록해야 합니다. 기존 슬라이드를 복제해 이미지만 교체하는 방식이 안전하나 확인이 필요합니다. 현재는 **PNG 만 제공하고 배치는 함은선 님이** 하는 것을 전제로 합니다.
