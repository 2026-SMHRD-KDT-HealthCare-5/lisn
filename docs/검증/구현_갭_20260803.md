# 요구사항 ↔ 구현 갭 — 2026.08.03

> **대조 기준** 02 요구사항정의서 `FR-*` 16건 · `NFR-*` 8건 · 유스케이스 14건
> vs `backend/app` · `ai/server` · `frontend/app` · `frontend/admin`
>
> 화면 단위 대조는 [`문서-구현_대조_20260802.md`](문서-구현_대조_20260802.md) 에
> 있습니다. **여기는 요구사항 단위**라 그쪽이 「화면이 있다」로 닫은 것도
> 데이터 경로까지 봅니다 — 실제로 그 차이에서 1건이 나왔습니다(갭 5).

---

## 요약

| 구분 | 건수 |
|---|---|
| 빠진 것 — 문서가 요구하는데 구현 없음 | **7** |
| 겹친 것 — 같은 규칙이 두 곳에 | **3** |
| 문서끼리 어긋난 것 | **1** (→ 개정안 `PL-25`) |

**빠진 것 7건 중 3건이 FCM 하나에 묶여 있습니다.** 갭 1·2·3 은 각각의 기능이
아니라 같은 인프라의 세 얼굴입니다.

---

# A. 빠진 것

## 갭 1 ⭐ FCM 발송 경로 전체

| 요구 | 내용 |
|---|---|
| `NFR-DV-002` | "**FCM 무음 푸시**로 동기화를 유도하고, 이후에도 수신되지 않으면 연동 상태를 '재시도 실패'로 표시하고 관리자 알림을 발송한다" |
| `MLCM_200` 7단계 | "**FCM 무음 푸시를 발송**하여 앱의 동기화 실행을 유도한다" |
| `MLCM_400` 5단계 | "사용자가 알림 수신 동의 상태인 경우, 시스템은 사용자가 **앱을 켜지 않더라도** 백그라운드 푸시 알림 API를 통해 맞춤형 힐링 콘텐츠 추천 메시지를 발송한다" |

**구현 상태**

| | |
|---|---|
| `USERS.fcm_token` | 컬럼 존재 — `schema.sql:47` · 04 · 05 **모두 반영됨** |
| 모델·스키마 | `models/user.py:39` · `schemas/user.py:36` **있음** |
| 토큰 저장 API | **없음** |
| 발송 코드 | **없음** — `firebase`·`fcm` 문자열이 코드 어디에도 없습니다 |

**컬럼만 파놓고 아무도 안 씁니다.**

## 갭 2 알림 설정 저장 API

화면설계서 `MAIN_SETTING_01` ❷ "알림 설정: 맞춤 케어 알림 수신 토글"

`settings_screen.dart:294` 가 스스로 밝혀둔 상태입니다.

> ⚠ 알림은 **아직 서버에 저장되지 않습니다.** … FCM 토큰 필드는 있지만
> 알림 설정 저장 API 가 없습니다.

화면에는 「알림 기능은 준비 중이에요」가 뜹니다.

**⚠ 토글 개수가 문서와 다릅니다.** 화면설계서는 **1개**("맞춤 케어 알림 수신
토글")인데 앱은 **3개**(감정 알림 / 추천 콘텐츠 알림 / 리포트 알림)를
그립니다. 저장 API 를 만들기 전에 **문서를 먼저 정해야** 컬럼 개수가
정해집니다 — 1개면 `BOOLEAN` 하나, 3개면 종류별로 필요합니다.

## 갭 3 미수신 감지 + 관리자 알림

`NFR-DV-002` — "`last_synced_at` 이 **3시간 이상 갱신되지 않은** 사용자를
미수신으로 감지"

**주기 실행 주체가 없습니다.** 백엔드에 스케줄러(APScheduler·cron)가 없고,
`last_synced_at` 을 훑는 코드도 없습니다.

> **갭 1·2·3 은 한 덩어리입니다.** FCM 을 붙이면 셋이 같이 닫힙니다.
> 그리고 「알림 수신 동의」는 `MLCM_400` 5단계가 이미 전제하므로,
> 04·05 에 컬럼이 없는 것은 신규 요구가 아니라 **기존 요구사항의 미반영**입니다.

## 갭 4 SMTP 미연동 — 재설정 토큰 전달 경로 없음

`FR-AD-004` — "비밀번호 분실 시 **이메일 인증을 통해 재설정 링크(코드)를
발송**하고 새 password로 갱신"

`backend/app/api/v1/auth.py:147` 에 그대로 적혀 있습니다.

> `TODO(메일): SMTP 미연동. 붙기 전까지 토큰을 사용자에게 전달할 경로가 없다.`

엔드포인트(`/auth/password-reset/request` · `/confirm`)와 앱 화면
(`password_reset_screen.dart`)은 **양쪽 다 있습니다.** 가운데 전달 수단만
비어 있어 **끝에서 끝까지 동작하지 않습니다.**

## 갭 5 ⭐ 체성분 — 화면은 있는데 데이터가 들어갈 길이 없습니다

[`문서-구현_대조_20260802.md`](문서-구현_대조_20260802.md) 가 `MAIN_LIFELOG_01` ❺
를 「조회 화면 추가」로 닫았습니다. **읽는 쪽은 맞지만 쓰는 쪽이 통째로
비어 있습니다.**

| 경로 | 상태 |
|---|---|
| `GET /body-composition` | 있음 · 앱이 호출함 ✅ |
| 라이프로그 화면 「체성분 기록」 절 | 있음 ✅ |
| `POST /body-composition` | 있음 · **앱이 호출하지 않음** ❌ |
| `health_reader.dart:57` `_types` | **체중·체지방이 목록에 없음** ❌ (걸음·거리·칼로리·심박·HRV·수면만) |
| 수동 입력 화면 | **없음** ❌ |

대조 문서는 "동의하지 않았거나 체성분계가 없으면 평생 비어 있는 게
정상"이라고 적었는데, **지금은 동의해도, 체성분계가 있어도 비어 있습니다.**

> `MAIN_JOIN_03`·`MAIN_SETTING_01` 이 체성분 수집 **동의를 받고 있습니다.**
> 받아놓고 수집하지 않는 동의는 그 자체로 지적 사항이 될 수 있습니다.

### ⚠ 그런데 Health Connect 로는 절반밖에 못 채웁니다

`health` 13.3.1 의 Android 지원 타입을 확인한 결과입니다.

| `BODY_COMPOSITION_METRICS` 컬럼 | Health Connect | 판정 |
|---|---|---|
| `weight_kg` | `WEIGHT` | ✅ 직접 |
| `body_water_kg` | `BODY_WATER_MASS` | ✅ 직접 |
| `bmr_kcal` | `BASAL_ENERGY_BURNED` | ✅ 직접 |
| `body_fat_kg` | `BODY_FAT_PERCENTAGE` | ⚠ **단위가 %** — `weight_kg × pct/100` 로 환산해야 하고, 체중이 없으면 못 구합니다 |
| `muscle_mass_kg` | **없음** | ❌ |
| `muscle_mass_min_kg` · `max_kg` | **없음** | ❌ 체성분계 표준범위라 플랫폼에 개념 자체가 없습니다 |
| `skeletal_muscle_kg` | **없음** | ❌ |

**⚠ `LEAN_BODY_MASS` 를 `muscle_mass_kg` 에 매핑하면 안 됩니다.** 제지방량은
근육 + 뼈 + 수분 + 장기라서 근육량과 다른 값입니다. 이름이 비슷하다고 넣으면
**틀린 숫자가 화면에 뜹니다.**

화면설계서 `MAIN_LIFELOG_01` ❺ 는 "체중·체지방·**근육량**·기초대사량"을
명시하는데, **근육량이 Health Connect 로는 안 옵니다.**

**세 갈래가 있고 팀 결정이 필요합니다.**

| 안 | 내용 | 대가 |
|---|---|---|
| A | Health Connect 로 4개만 채우고 근육량은 비움 | 화면설계서 ❺ 를 완전히 만족하지 못함. 문서 수정 필요 |
| B | 수동 입력 화면 추가 | 화면설계서에 화면 1장 추가 — 8/11 마감에 부담 |
| C | 체성분을 범위에서 제외 | `MAIN_JOIN_03` 동의 항목·04·05 테이블까지 정리해야 함 |

> **기업(라라랩스) 제공 데이터에는 8개 항목이 전부 있습니다.** 즉 스키마가
> 틀린 게 아니라, **실서비스 수집 경로(Health Connect)와 학습 데이터
> 출처(기업 정제본)의 항목이 다른** 것입니다. 이 차이가 지금까지 문서에
> 드러난 적이 없습니다.

## 갭 6 ⭐ 음성 입력 — 문서끼리 어긋남 → 개정안 `PL-25`

| 문서 | 상태 |
|---|---|
| 화면설계서 `MAIN_CHAT_02` ❸ | 음성 표기 **제거 완료** (`SD-A⑦`, 2026.08.02) |
| 02 `FR-AI-003` | "**멀티모달(텍스트/음성)** 공감 대화 생성" |
| 02 `MLCM_310` 1·2·6단계 | WAV 업로드 · **STT(Whisper 등)** · 음성 응답 |
| 구현 | 앱·서버 양쪽 **없음** |

**02 만 안 고쳐졌습니다.** 조치는 [`기획서_개정안.md`](../결정/기획서_개정안.md)
`PL-25` 로 옮겼습니다.

## 갭 7 죽은 엔드포인트 — `GET /contents/recommendations`

`home.py:204`. 주석은 "홈에 포함되지만 사용자가 다시 뽑아보고 싶을 때를
위해 분리한다"인데, **재추천 UI 가 없어 아무도 호출하지 않습니다.**

앱은 홈 응답에 실려 오는 `recommendations` 만 씁니다.

> 지우자는 게 아닙니다. **재추천 버튼을 붙이거나, 안 붙일 거면 주석의
> 「사용자가 다시 뽑아보고 싶을 때」를 지워야** 합니다. 지금은 있지도 않은
> 기능을 설명하는 주석입니다.

---

# B. 겹친 것

## 중복 1 ⭐ `risk_level → action` 매핑이 두 곳에

```
backend/app/api/v1/home.py:30
    _ACTION = {"NORMAL": "CHAT", "CAUTION": "CONTENT", "CRITICAL": "EMERGENCY"}

backend/app/api/v1/chat.py:_decide()
    RiskInfo(level="CRITICAL", action="EMERGENCY", ...)   ← 같은 매핑을
    RiskInfo(level="CAUTION",  action="CONTENT",   ...)      6줄에 걸쳐
    RiskInfo(level="NORMAL",   action="CHAT",      ...)      인라인 반복
```

**둘 다 주석에 "클라이언트에 규칙을 복제하면 반드시 어긋난다(API설계_사전결정
3절)"고 써놓고, 정작 서버 안에서 자기들끼리 복제하고 있습니다.**

지금은 값이 같습니다. 하지만 `MLCM_220`(선제 접촉)이 액션 축을 건드리므로
**손대기 전에 합치는 게 맞습니다.**

> ⚠ `risk_level_of()` 는 중복이 **아닙니다.** AI 서버에만 있고
> `analysis.py:_persist` 는 값 검증만 합니다(04 문서 6항 준수). 잘 지켜져
> 있습니다.

## 중복 2 감정 카테고리 매핑 — 복제는 의도적, 그런데 테스트가 없습니다

| 위치 | 역할 |
|---|---|
| `db/schema.sql:336` EMOTIONS 마스터 9종 | **정본** |
| `ai/server/main.py:216` `EMOTION_CATEGORY` | 요청마다 조회하지 않기 위한 복제본 |

`main.py:215` 에 "schema.sql 을 고치면 여기도 같이 고쳐야 한다"고 적혀
있습니다. **의도된 복제이므로 없애지 않습니다.**

**문제는 지키는 장치가 없다는 것입니다.** 챗봇 성격 이름은
`test/persona_label_test.dart` 가 화면설계서 추출본을 직접 읽어 대조하는데,
감정 9종에는 그런 테스트가 없습니다. 같은 방식으로 막을 수 있습니다.

## 중복 3 admin 위험도 라벨 — 정본을 만들어놓고 안 씁니다

```
frontend/admin/src/labels.js:9
    export const RISK_LABEL = { NORMAL: '안정', CAUTION: '주의', CRITICAL: '심각' }

frontend/admin/src/App.jsx:198-200
    ['안정 대상자', ...]   ← labels.js 를 쓰지 않고 하드코딩
    ['주의 대상자', ...]
    ['심각 대상자', ...]
```

> **앱(`report_screen.dart:340`)과 웹의 중복은 문제 삼지 않습니다.** 스택이
> 달라 컴포넌트를 공유할 수 없다는 것이 `MLCM_501` 4단계 검토에서 이미
> 정리된 사항입니다. **`admin` 안에서 겹치는 것만** 실수입니다.

---

## C. 갭이 아닌 것 — 확인해서 통과한 항목

오해를 막기 위해 남깁니다. 아래는 **의심했으나 정상**이었습니다.

| 항목 | 확인 결과 |
|---|---|
| `risk_level_of()` 위치 | AI 서버 단독. 비즈니스 서버는 검증만 — 04 문서 6항 준수 ✅ |
| 위기 판정 위치 | backend 단독(`NFR-DV-003` 때문). AI 서버에 없음 ✅ |
| 페르소나 이름 리터럴 | `ChatPersona` enum 한 곳 + 문서 대조 테스트 ✅ |
| `GET /admin/users/{id}/report` | `api.js:133` 이 템플릿 문자열로 호출 ✅ |
| 위기 키워드 사전 | `backend/app/data/crisis_keywords.json` 한 곳 ✅ |
| 화면설계서 ↔ 앱 | 2026.08.02 대조에서 6건 전부 해결 ✅ |

---

## 우선순위

| 순위 | 항목 | 이유 |
|---|---|---|
| 1 | **중복 1** 액션 매핑 통합 | `MLCM_220` 이 이 축을 건드립니다. **손대기 전에** |
| 2 | **갭 5** 체성분 수집 경로 | 동의를 받아놓고 수집하지 않는 상태 |
| 3 | **갭 1·2·3** FCM 묶음 | 요구사항 3건이 하나에 걸려 있음 |
| 4 | **중복 2** 감정 카테고리 테스트 | 30분이면 막힙니다 |
| 5 | **중복 3** admin 라벨 | 5분 |
| 6 | **갭 4** SMTP | 외부 계정이 필요합니다 |
| 7 | **갭 7** 죽은 엔드포인트 | 주석만 고쳐도 됨 |
