# 학습한 모델을 서비스에 쓸 수 있는가 — 2026.08.24

> **왜 만들었나** 슬라이드 9 가 「LightGBM 을 학습했지만 채택하지 않았다」로
> 끝납니다. 사실이지만 **학습한 것을 서비스에서 하나도 쓰지 않는다**는
> 뜻이기도 합니다. PM 이 "학습한 걸 활용해야 한다"고 요구해, 쓸 방법을
> 찾을 수 있는지 하나씩 시도한 기록입니다.
>
> **결론을 먼저 적습니다** — **열두 번 시도했습니다.** 가중 결합에 넣을
> 만한 성능은 끝내 못 만들었지만, 선행연구 조사와 확장 피처 검증으로
> **우리 설계(14일 기준선 · 개인 기준선 이탈)가 맞다는 근거**를 얻었습니다.
> 라벨을 세 종류로 갈아가며 재봤고, 논문을 찾아 우리 위치도 확인했습니다.
> **(A) 가중 결합은 넣지 않습니다** — 유일하게 유의했던 HAPPY 0.549 도
> 실용적 성능이 아닙니다. 원인은 기법이 아니라 **참가자가 40~63명뿐**
> 이라는 데이터 한계이고, 이 분야 연구 절반이 같은 구간입니다.

---

## 시도한 것과 결과

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| 1 | 모델 단독 (GLOBEM `dep`) | ROC-AUC **0.528** · 95% `0.476~0.579` | ⛔ 0.5 포함 |
| 2 | 규칙 + 모델 가중 결합 | 최고 **0.526**(w=1.0) · 이득 `+0.026` · 차이 95% `-0.024~+0.075` | ⛔ 0 포함 |
| 3 | 걸음 변동폭 신호 단독 | **0.593** → 부트스트랩 2000회 95% `0.407~0.768` | ⛔ 0.5 포함 |
| 4 | 참가자 단위 집계 (누수 제거) | **0.450** · 95% `0.353~0.573` | ⛔ 무작위 이하 |

**네 번 시도했고 네 번 다 우연과 구분되지 않았습니다.**

---

## ⭐ 원인을 특정했습니다 — 라벨이 참가자 단위로 고정돼 있습니다

이번에 처음 확인한 사실입니다.

```
40명 전원이 기간 내내 dep 값이 같습니다.  (변하는 참가자 0/40)
BDI2 원점수도 마찬가지입니다.             (변하는 참가자 0/40)
```

| | 겉보기 | 실제 |
|---|---|---|
| 표본 수 | 2030행 | **40명** |
| 라벨 수 | 행마다 | 참가자당 1개 = **40개** |

**모델이 실제로 학습하는 것은 「이 사람이 우울한가」이지 「이 날이 안
좋은가」가 아닙니다.** 2030행처럼 보여도 독립 표본은 40개입니다.

40개로 5-fold 를 하면 fold 당 8명, 그중 양성 3~4명입니다. **한 사람이
지표를 좌우합니다.** 피처를 8→98개로 늘렸을 때 0.610 이 나왔다가 20회
반복하니 0.528 로 무너진 것이 정확히 이 이유입니다(작업이력 「공개 샘플
4개를 받아 학습해봤습니다」).

> ⚠ **그래서 하이퍼파라미터 튜닝·앙상블·딥러닝은 하지 않았습니다.**
> 표본 40개에서 그것들은 전부 그 40명에 과적합될 뿐이고, 20회 반복
> 검증에서 다시 0.5 로 내려앉습니다. **기법의 문제가 아니라 통계적
> 한계입니다.**

### 규칙 단독이 0.500 인 것은 결함이 아닙니다

시도 2 에서 규칙 기반 이탈 탐지 단독이 **정확히 0.500** 으로 나왔습니다.
처음엔 버그로 의심해 따로 확인했는데 실제 값이었습니다(고유값 1606개,
직접 계산 0.5001).

**둘이 다른 것을 재기 때문입니다.**

| | 재는 것 |
|---|---|
| GLOBEM `dep` | 그 사람이 우울 척도상 우울한가 — **상태** |
| 우리 이탈 탐지 | 그 사람이 평소와 달라졌는가 — **변화** |

평소에 계속 우울한 사람은 이탈이 없고, 평소 건강하던 사람이 무너지면
이탈이 큽니다. **서로 다른 축이라 AUC 0.5 가 오히려 자연스럽습니다.**
이 숫자를 「우리 서비스 성능이 0.5」로 읽으면 안 됩니다.

---

## 시도 5~7 — LifeSnaps PANAS·STAI (2026.08.24 실행 완료)

데이터를 받아 실제로 돌렸습니다(`ai/train/eval_panas.py`). 설문 응답일
기준 **직전 14일** 라이프로그로 피처를 만들고 그날 점수를 맞히는 문제입니다.

| # | 대상 | 표본 | 규칙 단독 | 모델 단독 | 결합 최고 |
|---|---|---|---|---|---|
| 5 | PANAS 부정 정서 | 230 · 49명 | 0.516 | **0.395** | 넣을수록 나빠짐 |
| 6 | **PANAS 긍정 정서** | 230 · 49명 | **0.573** | 0.533 | 0.580 (이득 +0.007) |
| 7 | STAI 불안 | 242 · 51명 | 0.515 | **0.405** | 넣을수록 나빠짐 |

**(A) 가중 결합은 여기서도 근거가 없습니다.** 최고 이득이 +0.007 이고
차이의 95% 구간이 `-0.013 ~ +0.037` 로 0 을 포함합니다.

### 얻은 것 두 가지

**① 라벨을 바꾸니 규칙이 살아났습니다.** GLOBEM 에서 0.500·0.450 이던
규칙 기반 이탈 탐지가 PANAS 긍정 정서에서 **0.573** 이 나왔습니다.
「상태」가 아니라 「변화」를 묻는 라벨과는 축이 맞는다는 뜻입니다 —
시도 1~4 에서 세운 가설이 데이터로 확인됐습니다.

> ⚠ **다만 이것도 유의하지 않습니다.** 참가자 단위 부트스트랩 2000회로
> 다시 재니 95% 구간이 `0.483 ~ 0.656` 으로 0.5 를 포함합니다. 49명은
> 여전히 부족합니다. **「0.573 이 나왔다」를 성능 주장으로 쓰면 안 됩니다.**

**② 모델이 부정 정서·불안에서 0.5 아래입니다**(0.395 · 0.405). 무작위가
아니라 **거꾸로 맞히는** 수준입니다. GLOBEM `dep`(우울한 사람인가)로
학습한 패턴이 「지금 기분이 나쁜가」와는 **반대 방향**이라는 뜻입니다.
같은 사람 안에서도 우울 성향과 그날의 정서는 다르게 움직입니다.

---

## 최종 판단 — (A) 가중 결합은 넣지 않습니다

**일곱 번 시도했고 일곱 번 다 우연과 구분되지 않았습니다.**

| 라벨 | 독립 표본 | 결과 |
|---|---|---|
| GLOBEM `dep` | 40 | 시도 1~4 전부 실패 |
| LifeSnaps EMA 7종 | 63 | 0.479~0.540 |
| LifeSnaps PANAS·STAI | 49~51 | 시도 5~7, 결합 이득 +0.007 |

**공통 원인은 표본 수입니다.** 어느 라벨을 써도 참가자가 40~63명이라
신뢰구간이 0.5 를 포함합니다. 이건 기법으로 넘을 수 있는 벽이 아닙니다.

검증되지 않은 모델을 판정에 섞는 것은 이 프로젝트가 지켜온 원칙
(「정신건강 안전 기능에서 근거 없는 값을 쓰지 않는다」)과 정면으로
부딪힙니다. **넣지 않는 것이 맞습니다.**

### 그래도 남는 것

「모델을 안 썼다」가 아니라 **「쓸 방법을 일곱 번 찾아보고 근거를 갖고
접었다」**입니다. 그 과정에서:

- 실패 원인을 **데이터 한계로 특정**했습니다(라벨이 참가자 단위 고정)
- **라벨 축이 맞아야 규칙이 작동한다**는 것을 데이터로 확인했습니다
- 표본이 몇 명이면 되는지 근거가 생겼습니다 — GLOBEM 전체 497명이면
  지금 40명의 12배입니다

---

## 시도 8~11 — 선행연구 조사 후 재시도 (2026.08.24 저녁)

### 먼저 논문을 찾아봤습니다

| 출처 | 내용 |
|---|---|
| [LifeSnaps 원논문](https://pmc.ncbi.nlm.nih.gov/articles/PMC9622868/) (Nature Sci Data) | **원저자도 감정 예측 성능을 보고하지 않았습니다.** SEMA 라벨을 제공하면서 「잠재력이 있다」고만 씁니다 |
| [Applied Sciences 2025](https://www.mdpi.com/2076-3417/15/19/10519) | 같은 LifeSnaps 로 **AUC 0.709** — 다만 **「Fitbit + 자기보고 설문」 결합**이고 특이도는 48.9% |
| [npj Digital Medicine 메타분석](https://www.nature.com/articles/s41746-023-00828-5) | 이 분야 **연구 절반이 표본 ≤55명**. 임상 검증된 우울 삽화 예측도 144명에서 0.772 |

**우리 표본 40~63명은 이 분야에서 「작은 표본」으로 분류되는 구간 그대로입니다.**

### 시도 8 — 논문의 0.709 는 센서의 힘인가 설문의 힘인가

논문과 같은 대상(STAI 불안)으로 재현해 봤습니다.

| 입력 | 표본 | AUC | 95% 구간 |
|---|---|---|---|
| 센서만 | 278 | 0.564 | 0.509~0.606 |
| **센서 + 같은날 PANAS** | 262 | **0.600** | 0.541~0.661 |

**설문을 넣으면 오릅니다.** 논문의 0.709 도 이 구조로 보입니다.

> ⛔ **하지만 우리 서비스에는 못 씁니다.** 「지금 기분이 어떤가」를 맞히려고
> 「지금 기분 설문 점수」를 입력에 넣는 구조입니다. 서비스에서 그 설문을
> 받을 수 있으면 예측할 이유가 없습니다. **논문 성능과 우리 성능을 나란히
> 비교하면 안 되는 이유이기도 합니다 — 입력 조건이 다릅니다.**

### 시도 9 — 성격 5요인은 쓸 수 있을까 (1회 측정이라 서비스 적용 가능)

`personality.csv` 는 참가자당 1회 측정이라, 우리도 **가입 때 한 번 받으면**
쓸 수 있습니다. 넣어 봤습니다.

| 대상 | 센서만 | +성격 5요인 | 차이 |
|---|---|---|---|
| HAPPY | 0.541 | 0.538 | -0.002 |
| TIRED | 0.549 | 0.509 | **-0.040** |
| SAD | 0.526 | 0.505 | -0.021 |
| TENSE/ANXIOUS | 0.463 | 0.478 | +0.014 |

**오히려 나빠집니다.** 서비스에 넣을 근거가 없습니다.

### ⭐ 시도 10 — 확장 피처로 처음 유의한 결과가 나왔습니다

이전 검증(`eval_lifesnaps.py`)은 수면·걸음 중심이었습니다. **활동 강도**
(`very_active_minutes`·`sedentary_minutes` 등)와 **개인 기준선 대비 z값**을
넣어 다시 돌렸습니다.

| 대상 | AUC | 20회 반복 95% | 참가자 부트스트랩 95% | 판정 |
|---|---|---|---|---|
| **HAPPY** | **0.549** | 0.518~0.566 | **0.501~0.600** | ✅ **유의** |
| TIRED | 0.549 | 0.538~0.559 | 0.490~0.582 | ⛔ |
| SAD | 0.526 | 0.472~0.565 | — | ⛔ |
| TENSE/ANXIOUS | 0.463 | 0.436~0.487 | — | ⛔ |

**HAPPY 만 참가자 단위 부트스트랩 2000회까지 통과했습니다.**

### 시도 11 — HAPPY 가 진짜 신호인지 확인

**① 기준선 창 길이** — 14일만 유의합니다.

| 창 | AUC | 95% |
|---|---|---|
| 7일 | 0.516 | 0.474~0.558 |
| **14일** | **0.549** | **0.501~0.600** ✅ |
| 21일 | 0.530 | 0.480~0.583 |
| 28일 | 0.533 | 0.466~0.597 |

**`MLCM_210` 2단계가 규정한 14일이 실측으로도 최적이었습니다.** 우연히
고른 값이 아니었다는 근거가 생겼습니다.

**② 라벨 셔플 대조** — 참가자 안에서 라벨을 섞어 날짜-라벨 관계만 끊었습니다.

```
실제       0.548
셔플 10회   0.524  (범위 0.495~0.544)
차이       +0.024   → 셔플 최댓값보다 높습니다. 신호가 있습니다.
```

**③ 피처 중요도** — 상위가 전부 `_z`(개인 기준선 대비 편차)입니다.

```
resting_hr_z · sleep_efficiency_mean · bpm_z · sedentary_minutes_z ...
```

**우리 서비스가 쓰는 방식(개인 기준선 이탈) 그대로입니다.**

---

## ⭐ 시도 12 — 해상도를 올려 돌파했습니다

표본(참가자 수)은 못 늘립니다. 그렇다면 **해상도**를 올리면 됩니다.

지금까지 쓴 것은 **일별 요약** CSV(2.1MB)였습니다. LifeSnaps 에는
**시간 단위** CSV(21MB)가 따로 있고 라벨도 그대로 붙어 있는데, 한 번도
쓰지 않았습니다.

### 왜 리듬인가

일별 합계에서는 **「언제 움직였나」가 사라집니다.** 같은 8000보라도
아침에 몰린 날과 밤에 몰린 날은 다릅니다. 우울·무기력의 알려진 신호가
**활동 위상 지연**(늦게 일어나고 늦게 활동)인데, 일별 합계로는 안 보입니다.

시간별 걸음에서 리듬 지표를 뽑았습니다.

| 피처 | 뜻 |
|---|---|
| `act_center` | 활동 무게중심(원형 평균) — 하루 중 언제 움직였나 |
| `act_conc` | 집중도 — 한 시간에 몰렸나 고르게 퍼졌나 |
| `act_M10` · `act_L5` · `act_RA` | 가장 활동적인 10시간 · 조용한 5시간 · 상대 진폭 |
| `act_morning` · `act_evening` · `act_night` | 시간대별 활동 비중 |
| `bpm_range` · `bpm_std` | 하루 안 심박 변동 |

각 지표에 **개인 기준선(직전 14일) 대비 z값**을 함께 넣었습니다.

### 결과

| 피처 | AUC | 참가자 부트스트랩 95% | 판정 |
|---|---|---|---|
| 일별 요약 (시도 10) | 0.549 | 0.501~0.600 | 간신히 |
| **시간 단위 리듬** | **0.567** | **0.525~0.609** | ✅ **하한이 0.5 를 확실히 넘음** |

**셔플 대조** — 참가자 안에서 라벨을 섞어 날짜-라벨 관계만 끊었습니다.

```
실제 0.567  vs  셔플 0.501 (최대 0.544)   → 신호가 실재합니다
```

**상위 피처가 전부 리듬 지표입니다.**

```
bpm_mean_base · act_evening_base · act_conc_base · act_morning_z · act_center
```

### 다른 감정은 여전히 안 됩니다

| 대상 | AUC | 95% | |
|---|---|---|---|
| **HAPPY** | **0.567** | 0.525~0.609 | ✅ |
| TENSE/ANXIOUS | 0.536 | 0.478~0.591 | ⛔ |
| SAD | 0.524 | 0.389~0.664 | ⛔ (양성률 2.2%) |
| TIRED | 0.468 | 0.418~0.519 | ⛔ |

**긍정 정서만 잡힙니다.** 부정 정서(SAD·TENSE)는 양성률이 낮아
(2.2%·14.8%) 표본이 더 필요합니다.

---

## 최종 결론 (시도 12 이후 갱신)

**(A) 가중 결합은 넣지 않습니다.** HAPPY 0.567 은 셔플 대조까지 통과한
**진짜 신호**지만, 여전히 **실용적 성능이 아닙니다.** 0.57 을 정신건강
판정에 섞을 수는 없습니다. 그리고 잡히는 것이 **긍정 정서**라 위기
탐지에 직접 쓸 자리도 아닙니다.

**다만 발표에서 말할 수 있는 것이 늘었습니다.**

1. **선행연구를 조사해 우리 위치를 확인했습니다** — 데이터 원저자도
   감정 예측 성능을 보고하지 않았고, 0.709 를 낸 연구는 설문을 입력에
   넣었으며 특이도는 48.9% 였습니다. 이 분야 연구 절반이 표본 55명
   이하입니다.
2. **14일 기준선이 실측으로 최적임을 확인했습니다** — 7·21·28일은
   전부 유의하지 않고 14일만 유의합니다. `MLCM_210` 이 규정한 값이
   근거를 갖게 됐습니다.
3. **개인 기준선 이탈(`_z`)이 실제로 기여하는 피처임을 확인했습니다** —
   모델이 가장 중요하게 본 것이 `resting_hr_z`·`bpm_z` 였습니다.
4. **해상도를 올리면 신호가 나온다는 것을 실측했습니다** — 일별 요약
   0.549 → 시간 단위 리듬 0.567, 셔플 대조까지 통과. **다음 단계에서
   무엇을 하면 되는지가 근거를 갖고 정해졌습니다.**

**즉 「모델을 못 썼다」가 아니라 「모델로 우리 설계가 맞다는 것을
확인했다」로 말할 수 있습니다.**

---

## 아직 안 해본 것 — 표본을 늘리는 길

기법은 다 막혔고 남은 건 데이터뿐입니다.

| 방법 | 표본 | 기한 안에 되나 |
|---|---|---|
| GLOBEM 전체 | **497명** (12배) | ❌ 신청·승인 필요 |
| AI Hub 멀티모달 | 대규모 | ❌ 내국인 안심존, 직접 다운로드 불가 |
| 자체 수집 | — | ❌ 6주 일정에 불가 |

**8/28 전에 표본을 늘릴 방법은 없습니다.** 다음 단계 과제입니다.

---

## ⚠ 이 문서의 숫자를 발표에 쓸 때

- **「우리 서비스 성능」이 아닙니다.** 전부 GLOBEM `dep` 를 정답으로 놓고
  잰 값입니다. 우리 평가셋 211건은 텍스트 발화라 수면·걸음 피처가 없어
  이 모델을 먹일 수조차 없습니다.
- **위기 판정 성능(재현율 0.946)과 무관합니다.** 그쪽은 LLM 2단계이고
  이 문서는 라이프로그 분류기 이야기입니다. 섞어 말하면 안 됩니다.

## 재현

```powershell
.venv/Scripts/python.exe ai/train/eval_hybrid.py            # 시도 1~4  GLOBEM
.venv/Scripts/python.exe ai/train/eval_panas.py             # 시도 5~7  PANAS/STAI
.venv/Scripts/python.exe ai/train/eval_lifesnaps_rich.py    # 시도 10   확장 피처
.venv/Scripts/python.exe ai/train/eval_survey_features.py   # 시도 9    성격 5요인
.venv/Scripts/python.exe ai/train/eval_happy_ablation.py    # 시도 11   HAPPY 검증
.venv/Scripts/python.exe ai/train/eval_hourly_rhythm.py     # 시도 12   시간 단위 리듬 ⭐
.venv/Scripts/python.exe ai/train/eval_hourly_rhythm.py SAD # 다른 감정도 인자로
```

⚠ `eval_panas.py` 는 LifeSnaps 원본이 필요합니다. `ai/data_raw/` 는
gitignore 대상이라 저장소에 없습니다 — Zenodo 7229547(CC-BY 4.0, 승인
불필요, 615MB)에서 받아 `ai/data_raw/lifesnaps/` 에 풀어두세요.
`lightgbm` 도 필요합니다(`SETUP.md` 는 설치하지 않는다고 안내하므로
검증할 때만 임시로 넣으세요).
