# 학습한 모델을 서비스에 쓸 수 있는가 — 2026.08.24

> **왜 만들었나** 슬라이드 9 가 「LightGBM 을 학습했지만 채택하지 않았다」로
> 끝납니다. 사실이지만 **학습한 것을 서비스에서 하나도 쓰지 않는다**는
> 뜻이기도 합니다. PM 이 "학습한 걸 활용해야 한다"고 요구해, 쓸 방법을
> 찾을 수 있는지 하나씩 시도한 기록입니다.
>
> **결론을 먼저 적습니다** — **스물두 번 시도했습니다.** 가중 결합에 넣을
> 만한 성능은 끝내 못 만들었지만, 선행연구 조사와 확장 피처 검증으로
> **우리 설계(14일 기준선 · 개인 기준선 이탈)가 맞다는 근거**를 얻었습니다.
> 라벨을 세 종류로 갈아가며 재봤고, 논문을 찾아 우리 위치도 확인했습니다.
> **(A) 가중 결합은 넣지 않습니다** — 유일하게 유의했던 HAPPY 0.549 도
> 실용적 성능이 아닙니다. 원인은 기법이 아니라 **참가자가 40~63명뿐**
> 이라는 데이터 한계이고, 이 분야 연구 절반이 같은 구간입니다.

---

## 시도한 것과 결과

| # | 시도 | 결과 | 판정 |
|---|---|---|---|
| 1 | 모델 단독 (GLOBEM `dep`) | ROC-AUC **0.528** · 95% `0.476~0.579` | ⛔ 0.5 포함 |
| 2 | 규칙 + 모델 가중 결합 | 최고 **0.526**(w=1.0) · 이득 `+0.026` · 차이 95% `-0.024~+0.075` | ⛔ 0 포함 |
| 3 | 걸음 변동폭 신호 단독 | **0.593** → 부트스트랩 2000회 95% `0.407~0.768` | ⛔ 0.5 포함 |
| 4 | 참가자 단위 집계 (누수 제거) | **0.450** · 95% `0.353~0.573` | ⛔ 무작위 이하 |

**네 번 시도했고 네 번 다 우연과 구분되지 않았습니다.**

---

## ⭐ 원인을 특정했습니다 — 라벨이 참가자 단위로 고정돼 있습니다

이번에 처음 확인한 사실입니다.

```
40명 전원이 기간 내내 dep 값이 같습니다.  (변하는 참가자 0/40)
BDI2 원점수도 마찬가지입니다.             (변하는 참가자 0/40)
```

| | 겉보기 | 실제 |
|---|---|---|
| 표본 수 | 2030행 | **40명** |
| 라벨 수 | 행마다 | 참가자당 1개 = **40개** |

**모델이 실제로 학습하는 것은 「이 사람이 우울한가」이지 「이 날이 안
좋은가」가 아닙니다.** 2030행처럼 보여도 독립 표본은 40개입니다.

40개로 5-fold 를 하면 fold 당 8명, 그중 양성 3~4명입니다. **한 사람이
지표를 좌우합니다.** 피처를 8→98개로 늘렸을 때 0.610 이 나왔다가 20회
반복하니 0.528 로 무너진 것이 정확히 이 이유입니다(작업이력 「공개 샘플
4개를 받아 학습해봤습니다」).

> ⚠ **그래서 하이퍼파라미터 튜닝·앙상블·딥러닝은 하지 않았습니다.**
> 표본 40개에서 그것들은 전부 그 40명에 과적합될 뿐이고, 20회 반복
> 검증에서 다시 0.5 로 내려앉습니다. **기법의 문제가 아니라 통계적
> 한계입니다.**

### 규칙 단독이 0.500 인 것은 결함이 아닙니다

시도 2 에서 규칙 기반 이탈 탐지 단독이 **정확히 0.500** 으로 나왔습니다.
처음엔 버그로 의심해 따로 확인했는데 실제 값이었습니다(고유값 1606개,
직접 계산 0.5001).

**둘이 다른 것을 재기 때문입니다.**

| | 재는 것 |
|---|---|
| GLOBEM `dep` | 그 사람이 우울 척도상 우울한가 — **상태** |
| 우리 이탈 탐지 | 그 사람이 평소와 달라졌는가 — **변화** |

평소에 계속 우울한 사람은 이탈이 없고, 평소 건강하던 사람이 무너지면
이탈이 큽니다. **서로 다른 축이라 AUC 0.5 가 오히려 자연스럽습니다.**
이 숫자를 「우리 서비스 성능이 0.5」로 읽으면 안 됩니다.

---

## 시도 5~7 — LifeSnaps PANAS·STAI (2026.08.24 실행 완료)

데이터를 받아 실제로 돌렸습니다(`ai/train/eval_panas.py`). 설문 응답일
기준 **직전 14일** 라이프로그로 피처를 만들고 그날 점수를 맞히는 문제입니다.

| # | 대상 | 표본 | 규칙 단독 | 모델 단독 | 결합 최고 |
|---|---|---|---|---|---|
| 5 | PANAS 부정 정서 | 230 · 49명 | 0.516 | **0.395** | 넣을수록 나빠짐 |
| 6 | **PANAS 긍정 정서** | 230 · 49명 | **0.573** | 0.533 | 0.580 (이득 +0.007) |
| 7 | STAI 불안 | 242 · 51명 | 0.515 | **0.405** | 넣을수록 나빠짐 |

**(A) 가중 결합은 여기서도 근거가 없습니다.** 최고 이득이 +0.007 이고
차이의 95% 구간이 `-0.013 ~ +0.037` 로 0 을 포함합니다.

### 얻은 것 두 가지

**① 라벨을 바꾸니 규칙이 살아났습니다.** GLOBEM 에서 0.500·0.450 이던
규칙 기반 이탈 탐지가 PANAS 긍정 정서에서 **0.573** 이 나왔습니다.
「상태」가 아니라 「변화」를 묻는 라벨과는 축이 맞는다는 뜻입니다 —
시도 1~4 에서 세운 가설이 데이터로 확인됐습니다.

> ⚠ **다만 이것도 유의하지 않습니다.** 참가자 단위 부트스트랩 2000회로
> 다시 재니 95% 구간이 `0.483 ~ 0.656` 으로 0.5 를 포함합니다. 49명은
> 여전히 부족합니다. **「0.573 이 나왔다」를 성능 주장으로 쓰면 안 됩니다.**

**② 모델이 부정 정서·불안에서 0.5 아래입니다**(0.395 · 0.405). 무작위가
아니라 **거꾸로 맞히는** 수준입니다. GLOBEM `dep`(우울한 사람인가)로
학습한 패턴이 「지금 기분이 나쁜가」와는 **반대 방향**이라는 뜻입니다.
같은 사람 안에서도 우울 성향과 그날의 정서는 다르게 움직입니다.

---

## 최종 판단 — (A) 가중 결합은 넣지 않습니다

**일곱 번 시도했고 일곱 번 다 우연과 구분되지 않았습니다.**

| 라벨 | 독립 표본 | 결과 |
|---|---|---|
| GLOBEM `dep` | 40 | 시도 1~4 전부 실패 |
| LifeSnaps EMA 7종 | 63 | 0.479~0.540 |
| LifeSnaps PANAS·STAI | 49~51 | 시도 5~7, 결합 이득 +0.007 |

**공통 원인은 표본 수입니다.** 어느 라벨을 써도 참가자가 40~63명이라
신뢰구간이 0.5 를 포함합니다. 이건 기법으로 넘을 수 있는 벽이 아닙니다.

검증되지 않은 모델을 판정에 섞는 것은 이 프로젝트가 지켜온 원칙
(「정신건강 안전 기능에서 근거 없는 값을 쓰지 않는다」)과 정면으로
부딪힙니다. **넣지 않는 것이 맞습니다.**

### 그래도 남는 것

「모델을 안 썼다」가 아니라 **「쓸 방법을 일곱 번 찾아보고 근거를 갖고
접었다」**입니다. 그 과정에서:

- 실패 원인을 **데이터 한계로 특정**했습니다(라벨이 참가자 단위 고정)
- **라벨 축이 맞아야 규칙이 작동한다**는 것을 데이터로 확인했습니다
- 표본이 몇 명이면 되는지 근거가 생겼습니다 — GLOBEM 전체 497명이면
  지금 40명의 12배입니다

---

## 시도 8~11 — 선행연구 조사 후 재시도 (2026.08.24 저녁)

### 먼저 논문을 찾아봤습니다

| 출처 | 내용 |
|---|---|
| [LifeSnaps 원논문](https://pmc.ncbi.nlm.nih.gov/articles/PMC9622868/) (Nature Sci Data) | **원저자도 감정 예측 성능을 보고하지 않았습니다.** SEMA 라벨을 제공하면서 「잠재력이 있다」고만 씁니다 |
| [Applied Sciences 2025](https://www.mdpi.com/2076-3417/15/19/10519) | 같은 LifeSnaps 로 **AUC 0.709** — 다만 **「Fitbit + 자기보고 설문」 결합**이고 특이도는 48.9% |
| [npj Digital Medicine 메타분석](https://www.nature.com/articles/s41746-023-00828-5) | 이 분야 **연구 절반이 표본 ≤55명**. 임상 검증된 우울 삽화 예측도 144명에서 0.772 |

**우리 표본 40~63명은 이 분야에서 「작은 표본」으로 분류되는 구간 그대로입니다.**

### 시도 8 — 논문의 0.709 는 센서의 힘인가 설문의 힘인가

논문과 같은 대상(STAI 불안)으로 재현해 봤습니다.

| 입력 | 표본 | AUC | 95% 구간 |
|---|---|---|---|
| 센서만 | 278 | 0.564 | 0.509~0.606 |
| **센서 + 같은날 PANAS** | 262 | **0.600** | 0.541~0.661 |

**설문을 넣으면 오릅니다.** 논문의 0.709 도 이 구조로 보입니다.

> ⛔ **하지만 우리 서비스에는 못 씁니다.** 「지금 기분이 어떤가」를 맞히려고
> 「지금 기분 설문 점수」를 입력에 넣는 구조입니다. 서비스에서 그 설문을
> 받을 수 있으면 예측할 이유가 없습니다. **논문 성능과 우리 성능을 나란히
> 비교하면 안 되는 이유이기도 합니다 — 입력 조건이 다릅니다.**

### 시도 9 — 성격 5요인은 쓸 수 있을까 (1회 측정이라 서비스 적용 가능)

`personality.csv` 는 참가자당 1회 측정이라, 우리도 **가입 때 한 번 받으면**
쓸 수 있습니다. 넣어 봤습니다.

| 대상 | 센서만 | +성격 5요인 | 차이 |
|---|---|---|---|
| HAPPY | 0.541 | 0.538 | -0.002 |
| TIRED | 0.549 | 0.509 | **-0.040** |
| SAD | 0.526 | 0.505 | -0.021 |
| TENSE/ANXIOUS | 0.463 | 0.478 | +0.014 |

**오히려 나빠집니다.** 서비스에 넣을 근거가 없습니다.

### ⭐ 시도 10 — 확장 피처로 처음 유의한 결과가 나왔습니다

이전 검증(`eval_lifesnaps.py`)은 수면·걸음 중심이었습니다. **활동 강도**
(`very_active_minutes`·`sedentary_minutes` 등)와 **개인 기준선 대비 z값**을
넣어 다시 돌렸습니다.

| 대상 | AUC | 20회 반복 95% | 참가자 부트스트랩 95% | 판정 |
|---|---|---|---|---|
| **HAPPY** | **0.549** | 0.518~0.566 | **0.501~0.600** | ✅ **유의** |
| TIRED | 0.549 | 0.538~0.559 | 0.490~0.582 | ⛔ |
| SAD | 0.526 | 0.472~0.565 | — | ⛔ |
| TENSE/ANXIOUS | 0.463 | 0.436~0.487 | — | ⛔ |

**HAPPY 만 참가자 단위 부트스트랩 2000회까지 통과했습니다.**

### 시도 11 — HAPPY 가 진짜 신호인지 확인

**① 기준선 창 길이** — 14일만 유의합니다.

| 창 | AUC | 95% |
|---|---|---|
| 7일 | 0.516 | 0.474~0.558 |
| **14일** | **0.549** | **0.501~0.600** ✅ |
| 21일 | 0.530 | 0.480~0.583 |
| 28일 | 0.533 | 0.466~0.597 |

**`MLCM_210` 2단계가 규정한 14일이 실측으로도 최적이었습니다.** 우연히
고른 값이 아니었다는 근거가 생겼습니다.

**② 라벨 셔플 대조** — 참가자 안에서 라벨을 섞어 날짜-라벨 관계만 끊었습니다.

```
실제       0.548
셔플 10회   0.524  (범위 0.495~0.544)
차이       +0.024   → 셔플 최댓값보다 높습니다. 신호가 있습니다.
```

**③ 피처 중요도** — 상위가 전부 `_z`(개인 기준선 대비 편차)입니다.

```
resting_hr_z · sleep_efficiency_mean · bpm_z · sedentary_minutes_z ...
```

**우리 서비스가 쓰는 방식(개인 기준선 이탈) 그대로입니다.**

---

## ⭐ 시도 12 — 해상도를 올려 돌파했습니다

표본(참가자 수)은 못 늘립니다. 그렇다면 **해상도**를 올리면 됩니다.

지금까지 쓴 것은 **일별 요약** CSV(2.1MB)였습니다. LifeSnaps 에는
**시간 단위** CSV(21MB)가 따로 있고 라벨도 그대로 붙어 있는데, 한 번도
쓰지 않았습니다.

### 왜 리듬인가

일별 합계에서는 **「언제 움직였나」가 사라집니다.** 같은 8000보라도
아침에 몰린 날과 밤에 몰린 날은 다릅니다. 우울·무기력의 알려진 신호가
**활동 위상 지연**(늦게 일어나고 늦게 활동)인데, 일별 합계로는 안 보입니다.

시간별 걸음에서 리듬 지표를 뽑았습니다.

| 피처 | 뜻 |
|---|---|
| `act_center` | 활동 무게중심(원형 평균) — 하루 중 언제 움직였나 |
| `act_conc` | 집중도 — 한 시간에 몰렸나 고르게 퍼졌나 |
| `act_M10` · `act_L5` · `act_RA` | 가장 활동적인 10시간 · 조용한 5시간 · 상대 진폭 |
| `act_morning` · `act_evening` · `act_night` | 시간대별 활동 비중 |
| `bpm_range` · `bpm_std` | 하루 안 심박 변동 |

각 지표에 **개인 기준선(직전 14일) 대비 z값**을 함께 넣었습니다.

### 결과

| 피처 | AUC | 참가자 부트스트랩 95% | 판정 |
|---|---|---|---|
| 일별 요약 (시도 10) | 0.549 | 0.501~0.600 | 간신히 |
| **시간 단위 리듬** | **0.567** | **0.525~0.609** | ✅ **하한이 0.5 를 확실히 넘음** |

**셔플 대조** — 참가자 안에서 라벨을 섞어 날짜-라벨 관계만 끊었습니다.

```
실제 0.567  vs  셔플 0.501 (최대 0.544)   → 신호가 실재합니다
```

**상위 피처가 전부 리듬 지표입니다.**

```
bpm_mean_base · act_evening_base · act_conc_base · act_morning_z · act_center
```

### 다른 감정은 여전히 안 됩니다

| 대상 | AUC | 95% | |
|---|---|---|---|
| **HAPPY** | **0.567** | 0.525~0.609 | ✅ |
| TENSE/ANXIOUS | 0.536 | 0.478~0.591 | ⛔ |
| SAD | 0.524 | 0.389~0.664 | ⛔ (양성률 2.2%) |
| TIRED | 0.468 | 0.418~0.519 | ⛔ |

**긍정 정서만 잡힙니다.** 부정 정서(SAD·TENSE)는 양성률이 낮아
(2.2%·14.8%) 표본이 더 필요합니다.

---

## ⭐⭐ 시도 13 — 표본을 늘렸습니다. 세 감정이 유의해졌습니다

시도 12 에서 해상도를 올려 신호를 찾았지만 표본은 오히려 줄었습니다
(2108 → 1915). **표본을 늘릴 방법이 남아 있었습니다.**

### 하루 1표본이 아니라 「라벨이 붙은 시각」마다 1표본

SEMA 는 **하루에 1~4회, 서로 다른 시각에** 물었습니다. 지금까지는 그걸
하루로 뭉개 하나로 썼습니다.

```
하루 단위   2290 표본   (하루에 3번 물었어도 1개)
시각 단위   5029 표본   ← 2.2배
```

**오전 10시의 기분과 저녁 8시의 기분은 다른 관측입니다.** 라벨 시각
분포도 오전 10시(626) · 오후 3시(620) · 저녁 8시(572)로 하루에 흩어져
있습니다.

### 피처를 그 시각 기준으로 자릅니다

라벨이 10시에 붙었으면 **10시까지의** 데이터만 씁니다 — 그 뒤를 쓰면
미래를 보고 과거를 맞히는 것입니다.

| 피처군 | 내용 |
|---|---|
| 직전 6시간 | 그 시각 직전의 움직임 |
| 당일 누적 | 자정부터 그 시각까지 |
| **같은 시간대 기준선** | 직전 14일의 **같은 시각**과 견줌 |

> ⚠ **같은 시간대끼리 비교해야 합니다.** 저녁 8시 걸음을 하루 평균과
> 비교하면 「저녁엔 원래 많이 걷는다」가 이탈로 잡힙니다. 실제로 상위
> 피처가 전부 `_samehour` 계열입니다.

### 결과 — 세 감정이 유의합니다

| 감정 | AUC | 참가자 부트스트랩 95% | 셔플 대조 | |
|---|---|---|---|---|
| **TIRED** | **0.590** | 0.566~0.617 | 0.590 vs 0.513 | ✅ |
| **TENSE/ANXIOUS** | **0.582** | 0.548~0.611 | 0.582 vs 0.506 | ✅ |
| **HAPPY** | **0.581** | 0.548~0.618 | 0.581 vs 0.511 | ✅ |
| SAD | 0.419 | 0.347~0.494 | 구분 안 됨 | ⛔ |

**`TENSE/ANXIOUS`(불안)가 유의해진 것이 특히 큽니다** — 우리 서비스가
실제로 잡아야 하는 감정이고, 이전 시도에서는 0.463~0.536 으로 계속
실패했습니다.

`SAD` 만 여전히 안 됩니다. **양성률 3.0%** — 4500표본 중 135개뿐이라
표본이 더 필요합니다.

### 세 단계로 올라온 궤적

| 단계 | 표본 | HAPPY AUC | 95% 하한 |
|---|---|---|---|
| 일별 요약 (시도 10) | 2108 | 0.549 | 0.501 |
| 시간 리듬 (시도 12) | 1915 | 0.567 | 0.525 |
| **시각 단위 (시도 13)** | **4500** | **0.581** | **0.548** |

**해상도를 올리고 표본을 늘리자 성능과 신뢰도가 함께 올랐습니다.**

---

## 시도 14 — 모델을 바꾸면 나아지는가

논문(Applied Sciences 2025)이 CatBoost 로 0.709 를 냈는데 우리는
LightGBM 만 썼습니다. **모델 탓인지 데이터 탓인지 가려야** 다음 판단이
섭니다. 같은 피처·같은 분할로 6종을 비교했습니다.

> ⚠ **하이퍼파라미터 튜닝은 하지 않았습니다.** 표본이 61명이라 튜닝하면
> 그 61명에 맞춰질 뿐입니다. 각 모델 기본값 근처로만 비교했습니다 —
> 「모델 종류를 바꾸는 것만으로 달라지는가」가 질문이기 때문입니다.
>
> **선형 모델과 더미를 함께 넣었습니다.** 트리 계열끼리만 비교하면
> 「트리가 이 문제에 안 맞는 것 아닌가」를 못 가려냅니다.

| 모델 | HAPPY | TENSE/ANXIOUS | TIRED |
|---|---|---|---|
| Dummy(기준선) | 0.503 | 0.498 | 0.493 |
| **LogisticRegression** | **0.602** | **0.602** | **0.610** |
| RandomForest | 0.577 | 0.601 | 0.602 |
| LightGBM | 0.581 | 0.582 | 0.590 |
| XGBoost | 0.578 | 0.580 | 0.603 |
| CatBoost | 0.578 | 0.597 | 0.607 |

### ⭐ 가장 단순한 모델이 가장 낫습니다

**세 감정 모두 로지스틱 회귀가 1위**이고, LightGBM 대비 **+0.019 ~ +0.021**
로 폭이 일정합니다. 우연이 아닙니다.

부스팅 3종(LightGBM·XGBoost·CatBoost)은 0.578~0.607 로 **사실상 구분되지
않습니다.** 논문이 CatBoost 를 썼다는 것이 0.709 의 이유가 아니었다는
뜻이기도 합니다(시도 8 에서 확인했듯 그 차이는 설문 피처였습니다).

**왜 선형이 이기나** — 표본 61명에서 트리 계열은 참가자별 특이 패턴에
과적합됩니다. 신호가 약하고(AUC 0.6 언저리) 표본이 작을 때는 **단순한
모델이 덜 흔들립니다.** 교과서적인 결과입니다.

### 학습곡선 — 표본을 늘리면 트리가 역전할까

참가자 수를 줄여가며 재봤습니다.

| 참가자 | 표본 | LogReg | LightGBM |
|---|---|---|---|
| 18 | 1426 | 0.566 | 0.553 |
| 30 | 2342 | 0.602 | 0.601 |
| 42 | 3006 | 0.569 | 0.559 |
| 61 | 4500 | 0.610 | 0.589 |

**단조증가하지 않습니다.** 30명에서 0.602, 42명에서 0.569 로 떨어집니다 —
**어떤 참가자가 뽑히느냐에 따라 크게 흔들린다**는 뜻입니다. 표본을
늘리는 것만으로 안정적으로 좋아진다고 말할 수 없습니다.

**결론** — 모델 종류로 얻을 수 있는 것은 +0.02 정도이고, 그마저도
「더 단순한 쪽」입니다. **병목은 모델이 아니라 데이터입니다.**

---

## ⭐⭐⭐ 시도 15 — 전부 합쳐 최대치를 냈습니다

지금까지 얻은 것을 하나로 합쳤습니다.

| 출처 | 합친 것 |
|---|---|
| 시도 12 | 시간 단위 **리듬 피처**(활동 무게중심·집중도·M10/L5·상대진폭) |
| 시도 13 | **시각 단위 표본** · **같은 시간대 기준선** |
| 시도 14 | **로지스틱 회귀**가 트리보다 낫다 |
| 신규 | **순위 평균 앙상블**(선형 + 트리) |

> 순위 평균을 쓴 이유 — 두 모델의 확률 척도가 달라 그대로 더하면 한쪽이
> 먹습니다. 순위로 바꿔 더하면 척도 차이가 사라집니다.

### 결과 — 불안이 0.612 까지 올랐습니다

| 감정 | 시도 13 | **시도 15** | 개선 | 최고 구성 |
|---|---|---|---|---|
| **TENSE/ANXIOUS** | 0.582 | **0.612** | **+0.030** | 앙상블 |
| **TIRED** | 0.590 | **0.605** | +0.015 | 앙상블 |
| HAPPY | 0.581 | 0.585 | +0.004 | LightGBM |

**셔플 대조**(불안) — 실제 0.612 vs 셔플 0.490(최대 0.521). **신호가
실재합니다.**

| 구성 | 불안 AUC | 95% |
|---|---|---|
| LogisticRegression | 0.602 | 0.563~0.636 |
| LightGBM | 0.599 | 0.565~0.631 |
| **앙상블(순위 평균)** | **0.612** | **0.579~0.644** |

**두 모델이 서로 다른 실수를 하기 때문에** 합치면 올라갑니다 — 선형은
전반적 경향을, 트리는 상호작용을 잡습니다.

### ⚠ 다만 피처를 늘리는 것이 항상 좋지는 않았습니다

리듬 피처를 더하자 **HAPPY 에서 로지스틱 회귀가 0.602 → 0.557 로
떨어졌습니다.** 피처가 22 → 49개로 늘면서 선형 모델이 흔들린 것입니다.

**「좋은 재료를 다 넣으면 다 좋아진다」가 아닙니다.** 감정마다 맞는
구성이 다르고, 그래서 감정별 최고 구성도 갈립니다.

### 전체 궤적

| 단계 | 표본 | 불안(TENSE) | 95% 하한 |
|---|---|---|---|
| 일별 요약 (시도 10) | 2108 | 0.463 | ⛔ |
| 시간 리듬 (시도 12) | 1915 | 0.536 | ⛔ |
| 시각 단위 (시도 13) | 4500 | 0.582 | 0.548 ✅ |
| **전부 합침 (시도 15)** | **4507** | **0.612** | **0.579** ✅ |

**우리가 실제로 잡아야 하는 불안이 0.463 → 0.612 로 올라왔습니다.**

---

## 시도 16 — 원본 라벨을 열어 문제 정의를 다시 세웠습니다 (2026.08.25)

지금까지 CSV 의 `HAPPY`·`SAD`·`TIRED`·`TENSE/ANXIOUS` **네 컬럼을 각각
이진 문제로** 풀고 있었습니다. 원본(`sema.bson`)을 열어 보니 그게 아니었습니다.

```
MOOD 는 단일 필드입니다.
  RESTED/RELAXED 1179 · TIRED 1126 · NEUTRAL 822 · HAPPY 789
  TENSE/ANXIOUS 620 · ALERT 345 · SAD 147 · (그 외 8)
```

**다중분류 한 문제**를 네 개로 쪼개 풀고 있었던 것이고, 그래서 `SAD`
양성률이 **3.0%** 까지 떨어졌습니다(4500 중 135). 어떤 모델도 임계점을
찾을 수 없는 조건이었습니다.

### 정서가(valence)로 묶어 다시 쟀습니다

| 정의 | 표본 | 양성률 | AUC | 95% | |
|---|---|---|---|---|---|
| **wide** — 부정(TIRED 포함) vs 긍정 | 3759 | 45.1% | 0.523 | 0.486~0.567 | ⛔ |
| **narrow** — TIRED 를 뺀 부정 vs 긍정 | 2752 | 25.0% | **0.569** | **0.521~0.617** | ✅ |
| happy (비교용) | 3759 | 18.3% | **0.580** | 0.531~0.636 | ✅ |

**⭐ `TIRED` 를 섞으면 신호가 죽습니다.** 양성률이 45% 로 가장 균형 잡힌
`wide` 가 오히려 유일하게 유의하지 않습니다.

> **피로는 정서가 아니라 상태입니다.** 센서에 찍히는 모습이 불안·슬픔과
> 다르고, 한 라벨로 묶으면 서로를 지웁니다. 표본이 3759 → 2752 로 **줄어도**
> 성능은 0.523 → 0.569 로 오릅니다 — **양을 늘리는 것보다 라벨을 정확히
> 가르는 것이 중요하다**는 실측 사례입니다.

`narrow` 는 **우리 서비스가 실제로 잡아야 하는 것**과 같습니다 —
불안·슬픔·분노·공포. 셔플 대조도 통과했습니다(실제 0.569 vs 셔플 0.482,
최대 0.506).

### 원본에서 함께 확인한 것

- **라벨 시각이 분 단위**입니다. CSV 는 시간으로 뭉갰습니다
- `PLACE`(HOME·WORK/SCHOOL·OUTDOORS…)가 같이 있지만 **쓰지 않았습니다** —
  설문으로 받은 값이라 서비스에서는 얻을 수 없습니다(시도 8 과 같은 이유)

---

## 시도 17 — 9.7GB 원본을 열었습니다 (2026.08.25)

`fitbit.bson` 9.7GB 는 **이미 받아둔 zip 안에 있었습니다.** 압축을 안
풀었을 뿐입니다. 열어 보니 시간별 CSV 에 없던 것이 있었습니다.

```
heart_rate                     48,720,073   초 단위 심박
Heart Rate Variability Details    220,512   ⭐ 실제 RMSSD
Wrist Temperature               4,372,241
mindfulness_eda_data_sessions      16,070   피부 전기활동(EDA)
Respiratory Rate Summary            3,000
Stress Score                        1,911   Fitbit 자체 산출
```

`extract_bson.py` 가 이것을 **152MB parquet** 로 접습니다(9.7GB → 152MB,
6분). RAM 32GB 에서 터지지 않게 길이 접두사로 문서를 하나씩 걸어가며
**바이트로 먼저 거르고** 필요한 것만 디코딩합니다.

> ⚠ **여기서 pandas 3 함정에 걸렸습니다.** `to_datetime` 기본 해상도가
> **마이크로초**로 바뀌었는데 `Timestamp.value` 는 여전히 나노초를 줍니다.
> 그대로 비교해 **1000배 어긋나 아무것도 안 걸렸습니다.** 에러가 안 나고
> 결과만 0 이라 알아채기 어렵습니다.

### ⛔ 그런데 새 피처만으로는 오히려 나빴습니다

| 구성 | 피처 | AUC | |
|---|---|---|---|
| 시간 CSV 리듬만 | 27 | **0.578** | ✅ |
| 시간 CSV 전부 | 50 | 0.556 | ✅ |
| + 분 단위 심박 | 66 | 0.552 | ✅ |
| + 일 단위 생체 | 95 | 0.551 | ✅ |
| **분 단위만**(리듬 제외) | 59 | **0.519** | ⛔ 셔플과 구분 안 됨 |

**피처를 넣을수록 단조 하락합니다.** 참가자가 60명뿐이라 **얻는 신호보다
늘어나는 분산이 큽니다.**

> **가장 잘 듣던 것을 새 재료로 갈아치우면 안 됩니다.** 분 단위 원본이
> 더 정밀하니 리듬 피처는 빼도 된다고 본 것이 실수였습니다.

---

## ⭐⭐⭐ 시도 18 — 폴드 안 피처 선택으로 풀었습니다

피처가 문제라면 **줄이면 됩니다.** 다만 어떤 것을 줄일지는 데이터가
정하게 하되, **학습 폴드만 보고** 정해야 합니다.

```python
make_pipeline(StandardScaler(), SelectKBest(f_classif, k=30), LogisticRegression(...))
```

> ⚠ **선택을 파이프라인 밖에서 하면 안 됩니다.** 전체 데이터로 고른 피처는
> 평가 폴드의 정답을 이미 본 것이라 AUC 가 크게 부풀려집니다. 흔한 실수라
> 여기서는 `Pipeline` 안에 넣어 구조적으로 막았습니다.

| K | AUC | 95% |
|---|---|---|
| 5 | 0.518 | 0.470~0.567 |
| 10 | 0.576 | 0.535~0.611 ✅ |
| 20 | 0.580 | 0.536~0.621 ✅ |
| **30** | **0.592** | **0.552~0.630** ✅ |
| 50 | 0.551 | 0.513~0.587 ✅ |
| 95 (전부) | 0.551 | 0.513~0.588 ✅ |

> ⚠ **K=30 은 같은 데이터를 보고 골랐습니다.** 다만 10~30 이 전부
> 0.576~0.592 라 칼날 위의 값은 아닙니다. **엄밀히는 K 도 중첩
> 교차검증으로 골라야 합니다** — 이 한계는 그대로 남습니다.

### 여기에 앙상블까지 얹은 최종 결과

| 대상 | 표본 | 양성률 | **앙상블 AUC** | 95% | 셔플(최대) |
|---|---|---|---|---|---|
| **TENSE/ANXIOUS** | 3801 | 14.7% | **0.608** | 0.565~0.649 | 0.466 (0.486) |
| 부정 정서(narrow) | 2786 | 25.0% | **0.598** | 0.558~0.638 | 0.497 (0.515) |
| HAPPY | 3801 | 18.1% | **0.596** | 0.542~0.652 | 0.504 (0.520) |
| 부정 정서(wide) | 3801 | 45.0% | 0.568 | 0.525~0.612 | 0.520 (0.562) |

**네 대상 모두 유의**하고, **`wide` 만 셔플과의 거리가 좁습니다** —
`TIRED` 를 섞은 정의라 시도 16 의 결론과 일치합니다.

### 우리가 잡아야 하는 불안의 궤적

| 단계 | 표본 | AUC | |
|---|---|---|---|
| 일별 요약 (시도 10) | 2108 | 0.463 | ⛔ |
| 시간 리듬 (시도 12) | 1915 | 0.536 | ⛔ |
| 시각 단위 (시도 13) | 4500 | 0.582 | ✅ |
| 전부 합침 (시도 15) | 4507 | 0.612 | ✅ |
| **원본 + 피처 선택 (시도 18)** | 3801 | **0.608** | ✅ |

> **시도 15 의 0.612 와 나란히 두면 안 됩니다.** 라벨 출처(CSV 다중 컬럼
> vs 원본 `MOOD`)와 표본이 다릅니다. 시도 18 쪽이 **검증이 더 엄격합니다** —
> 라벨이 한 곳에서 나오고, 피처 선택이 폴드 안에 있습니다.

---

## 시도 19~22 — 재는 방식이 틀렸었습니다 (2026.08.25)

### ⭐ 시도 19 — 우리가 재야 할 것은 「사람 사이」가 아니라 「사람 안」입니다

과거 정서 이력(직전 응답·부정 비율)을 넣었더니 전체 AUC 가
**0.598 → 0.728** 로 뛰었습니다. 그런데 **셔플 대조도 0.666 으로 같이
뛰었습니다.**

원인이 분명합니다 — 참가자 안에서 라벨을 섞어도 **그 사람의 부정 비율은
보존**됩니다. 즉 0.728 의 상당 부분은 「이 사람이 원래 부정적인가」입니다.

> **전체 AUC 는 사람 사이 차이로 부풀려집니다.** 늘 우울한 사람과 늘 밝은
> 사람을 가르기만 해도 점수가 오릅니다. **우리 서비스가 하는 일은 그게
> 아닙니다.**
>
> **「이 사람의 나쁜 순간을 이 사람의 좋은 순간보다 위로 올리는가」**

그래서 **참가자 내부 AUC** 를 도입했습니다 — 참가자별로 따로 재서 평균.
개인 기준선 이탈 탐지의 목표와 정확히 같은 질문입니다.

| 입력 | 전체 AUC | 셔플 | **참가자 내부** | 셔플(최대) |
|---|---|---|---|---|
| 센서만 | 0.598 | 0.505 ✅ | 0.589 | 0.503 (0.552) ✅ |
| 센서+정서이력 | 0.728 | **0.666** ⚠ | **0.612** | 0.455 (0.501) ✅ |

> ⚠ **셔플 대조에서 이력 피처도 다시 계산해야 합니다.** 참 라벨로 만든
> 이력을 두고 목표만 섞으면 대조군이 참 라벨의 구조를 들고 있어 비교가
> 성립하지 않습니다. 처음에 이렇게 짰다가 고쳤습니다.

### 시도 20 — 참가자별 표준화: 이득 없음(그러나 설계 근거)

입력에서도 사람 사이 차이를 빼봤습니다(`x' = (x - mean_user)/std_user`).

| 입력 | 전체 | 참가자 내부 |
|---|---|---|
| 원본 | 0.598 | **0.589** |
| 참가자 표준화 | 0.551 | 0.587 |
| 둘 다 | 0.568 | 0.585 |

**차이가 없습니다.** 이미 `_z` 피처(개인 기준선 대비 편차)가 그 일을 하고
있었다는 뜻입니다 — **이득은 아니지만 우리 설계가 맞았다는 근거**입니다.

### ⛔ 시도 21 — 시간 분할: 좋아 보였지만 기각했습니다

참가자 분할은 「처음 보는 사람」을 재는데, 우리 서비스는 그 사용자의
과거를 갖고 씁니다. 그래서 사람마다 **앞 70% 로 배우고 뒤 30% 를**
맞혀 봤습니다.

```
LightGBM 전체 AUC 0.653  ← 지금까지 중 가장 높음
셔플 대조         0.611 (최대 0.666)   ⛔ 통과 못 함
```

**같은 사람이 학습·평가에 다 있어 「누가 원래 부정적인가」를 외운
것**입니다. 숫자가 높다고 채택하지 않습니다.

---

## ⭐⭐⭐ 시도 22 — 중첩 교차검증으로 마지막 구멍을 막았습니다

시도 18 의 0.608 에는 단서가 붙어 있었습니다 — **`K=30` 을 전체 결과를
보고 골랐습니다.** 그래서 **바깥 폴드의 학습 부분만으로 K 를 고르게**
바꿨습니다.

```
바깥 5겹(참가자 분할)
  └ 학습 부분에서 안쪽 4겹을 돌려 K 를 고른다
  └ 고른 K 로 학습 부분 전체를 다시 학습
  └ 바깥 평가 부분을 예측     ← K 선택 과정을 한 번도 본 적이 없다
```

폴드마다 다른 K 가 뽑혔습니다(불안: 15·10·30·20·15) — 하나에 고정한 것이
아닙니다.

### 최종 결과 — 단서 없는 숫자

| 대상 | 최고 구성 | 전체 AUC | 95% | **참가자 내부** | 셔플 내부(최대) |
|---|---|---|---|---|---|
| **TENSE/ANXIOUS** | LogReg | 0.606 | 0.564~0.647 | **0.648** | 0.504 (0.517) ✅ |
| **HAPPY** | 앙상블 | 0.595 | 0.539~0.651 | **0.640** | 0.495 (0.523) ✅ |
| 부정 정서(narrow) | LogReg | 0.567 | 0.521~0.609 | 0.556 | 0.518 (0.538) ✅ |

**⭐ 참가자 내부 AUC 가 전체보다 높습니다**(불안 0.648 vs 0.606).
**신호가 실제로 「사람 안」에 있다**는 뜻이고, 이것이 우리 서비스가
하는 일과 정확히 같습니다.

### 운영 지점 — 이 성능으로 무엇을 할 수 있나

`eval_context.py` · `eval_person.py` 가 함께 냅니다. 부정 정서 기준
(기본 양성률 25.0%), 센서만:

| 상위 | 정밀도 | 재현율 | 향상 |
|---|---|---|---|
| 5% | 36.0% | 7.2% | 1.44배 |
| 10% | 34.5% | 13.8% | 1.38배 |
| 20% | 33.9% | 27.2% | 1.36배 |

**확정 판정에는 못 씁니다.** 다만 「지금 어떠세요?」를 **언제 물을지**
고르는 데는 무작위보다 1.4배 낫습니다.

---

## 최종 결론 (시도 22 이후 갱신)

**(A) 가중 결합은 이번 발표 범위에서는 넣지 않습니다.** 불안의 참가자
내부 AUC 가 **0.648**(중첩 교차검증 · 셔플 0.504)까지 올랐고 신호가
「사람 안」에 있다는 것도 확인했지만, 0.65 는 **정신건강 판정에 섞을
실용적 성능이 아닙니다.**

> **다만 「무엇에는 쓸 수 있는지」가 정해졌습니다.** 확정 판정에는 못 쓰고,
> **「지금 어떠세요?」를 언제 물을지 고르는 데**는 무작위보다 1.4배
> 낫습니다. 이건 우리 서비스의 선제 접촉(`MLCM_220`)이 하는 일과 같습니다.
게다가 이 검증은 LifeSnaps 라벨 기준이라, 우리 서비스의 실제 판정이
좋아지는지는 별도로 확인해야 합니다.

> **다만 「안 된다」에서 「아직 이르다」로 바뀌었습니다.** 시도 1~9 는
> 우연과 구분되지 않아 근거가 없었지만, 지금은 신호가 실재하고
> 올리는 방향(해상도·표본)도 실측으로 확인됐습니다.
>
> **그리고 모델을 바꿔서 넘을 벽이 아니라는 것도 확인했습니다**(시도 14) —
> 6종을 비교했더니 가장 단순한 로지스틱 회귀가 1위였고, 부스팅 3종은
> 서로 구분되지 않았습니다. 병목은 모델이 아니라 데이터입니다.

**다만 발표에서 말할 수 있는 것이 늘었습니다.**

1. **선행연구를 조사해 우리 위치를 확인했습니다** — 데이터 원저자도
   감정 예측 성능을 보고하지 않았고, 0.709 를 낸 연구는 설문을 입력에
   넣었으며 특이도는 48.9% 였습니다. 이 분야 연구 절반이 표본 55명
   이하입니다.
2. **14일 기준선이 실측으로 최적임을 확인했습니다** — 7·21·28일은
   전부 유의하지 않고 14일만 유의합니다. `MLCM_210` 이 규정한 값이
   근거를 갖게 됐습니다.
3. **개인 기준선 이탈(`_z`)이 실제로 기여하는 피처임을 확인했습니다** —
   모델이 가장 중요하게 본 것이 `resting_hr_z`·`bpm_z` 였습니다.
4. **해상도와 표본을 올리면 신호가 나온다는 것을 실측했습니다** —
   일별 요약 0.549 → 시간 리듬 0.567 → 시각 단위 0.581, 셔플 대조까지
   전부 통과. **세 감정(TIRED·TENSE·HAPPY)이 유의**해졌고, 특히
   불안이 잡히기 시작했습니다. **다음 단계에서 무엇을 하면 되는지가
   근거를 갖고 정해졌습니다.**

**즉 「모델을 못 썼다」가 아니라 「모델로 우리 설계가 맞다는 것을
확인했다」로 말할 수 있습니다.**

---

## 아직 안 해본 것 — 표본을 늘리는 길

기법은 다 막혔고 남은 건 데이터뿐입니다.

| 방법 | 표본 | 기한 안에 되나 |
|---|---|---|
| GLOBEM 전체 | **497명** (12배) | ❌ 신청·승인 필요 |
| AI Hub 멀티모달 | 대규모 | ❌ 내국인 안심존, 직접 다운로드 불가 |
| 자체 수집 | — | ❌ 6주 일정에 불가 |

**8/28 전에 표본을 늘릴 방법은 없습니다.** 다음 단계 과제입니다.

---

## ⚠ 이 문서의 숫자를 발표에 쓸 때

- **「우리 서비스 성능」이 아닙니다.** 전부 GLOBEM `dep` 를 정답으로 놓고
  잰 값입니다. 우리 평가셋 211건은 텍스트 발화라 수면·걸음 피처가 없어
  이 모델을 먹일 수조차 없습니다.
- **위기 판정 성능(재현율 0.946)과 무관합니다.** 그쪽은 LLM 2단계이고
  이 문서는 라이프로그 분류기 이야기입니다. 섞어 말하면 안 됩니다.

## 재현

```powershell
.venv/Scripts/python.exe ai/train/eval_hybrid.py            # 시도 1~4  GLOBEM
.venv/Scripts/python.exe ai/train/eval_panas.py             # 시도 5~7  PANAS/STAI
.venv/Scripts/python.exe ai/train/eval_lifesnaps_rich.py    # 시도 10   확장 피처
.venv/Scripts/python.exe ai/train/eval_survey_features.py   # 시도 9    성격 5요인
.venv/Scripts/python.exe ai/train/eval_happy_ablation.py    # 시도 11   HAPPY 검증
.venv/Scripts/python.exe ai/train/eval_hourly_rhythm.py     # 시도 12   시간 단위 리듬 ⭐
.venv/Scripts/python.exe ai/train/eval_hourly_rhythm.py SAD # 다른 감정도 인자로
.venv/Scripts/python.exe ai/train/eval_momentary.py         # 시도 13   시각 단위 ⭐⭐
.venv/Scripts/python.exe ai/train/eval_models.py            # 시도 14   모델 6종 비교
.venv/Scripts/python.exe ai/train/eval_best.py              # 시도 15   전부 합침 ⭐⭐⭐
.venv/Scripts/python.exe ai/train/eval_best_shuffle.py      # 시도 15   앙상블 셔플 대조
.venv/Scripts/python.exe ai/train/eval_valence.py narrow    # 시도 16   정서가 이진
.venv/Scripts/python.exe ai/train/extract_bson.py           # 원본 9.7GB → parquet
.venv/Scripts/python.exe ai/train/eval_minute.py            # 시도 17   분 단위만 (⛔ 대조군)
.venv/Scripts/python.exe ai/train/eval_final.py             # 시도 17   전부 합침
.venv/Scripts/python.exe ai/train/eval_ablation.py          # 시도 18   절제 + K 스윕
.venv/Scripts/python.exe ai/train/eval_selected.py tense    # 시도 18   피처 선택
.venv/Scripts/python.exe ai/train/eval_context.py           # 시도 19   정서이력 + 내부 AUC ⭐
.venv/Scripts/python.exe ai/train/eval_person.py            # 시도 20   참가자별 표준화
.venv/Scripts/python.exe ai/train/eval_temporal.py          # 시도 21   시간 분할 (⛔ 기각)
.venv/Scripts/python.exe ai/train/eval_nested.py tense      # 시도 22   중첩 CV ⭐⭐⭐
```

⚠ `eval_panas.py` 는 LifeSnaps 원본이 필요합니다. `ai/data_raw/` 는
gitignore 대상이라 저장소에 없습니다 — Zenodo 7229547(CC-BY 4.0, 승인
불필요, 615MB)에서 받아 `ai/data_raw/lifesnaps/` 에 풀어두세요.
`lightgbm` 도 필요합니다(`SETUP.md` 는 설치하지 않는다고 안내하므로
검증할 때만 임시로 넣으세요).
