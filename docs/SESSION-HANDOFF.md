# 세션 인수인계

> 다른 컴퓨터나 새 세션에서 작업을 이어갈 때 이 문서부터 읽으세요.
> Claude Code 의 대화 기록과 메모리는 **PC 로컬에 프로젝트 경로 기준으로** 저장되므로 기기 간에 따라가지 않습니다. 대신 맥락을 저장소에 남겨두는 방식으로 이어갑니다.
>
> **최종 갱신** 2026.07.30

## 0. 문서 역할 분담

| 문서 | 담는 것 |
|---|---|
| [`docs/review/작업이력.md`](review/작업이력.md) | **완료된 결정과 그 근거.** 날짜순 누적. 새 결정이 나오면 맨 위에 추가 |
| [`docs/review/문서개정_체크리스트.md`](review/문서개정_체크리스트.md) | **아직 문서에 반영 안 된 개정 항목.** 반영되면 지움 |
| [`docs/review/회의안건_20260729.md`](review/회의안건_20260729.md) | **아직 안 정해진 것.** 정해지면 지우고 작업이력으로 |
| 이 문서 | 새 환경에서 시작하는 방법 · 현재 상태 요약 |

**완료된 항목을 체크리스트나 안건 문서에 남겨두지 마세요.** 무엇을 해야 하는지가 안 보이게 됩니다.

---

## 1. 새 환경에서 시작하기

```bash
git clone https://github.com/2026-SMHRD-KDT-HealthCare-5/lisn.git
```

```powershell
.\tools\check-env.ps1
```

그다음 아래 프롬프트를 Claude Code 에 **그대로 붙여넣으면** 이전 세션과 같은 상태에서 시작합니다.

---

## 2. 킥오프 프롬프트 (복사해서 붙여넣기)

```
귀기울임(LISN) 프로젝트를 이어서 진행할게. 시작 전에 아래를 읽고 현재 상태를 파악해줘.

1. CLAUDE.md — 프로젝트 맥락, 작업 규칙, 이 저장소 특유의 함정
2. docs/SESSION-HANDOFF.md — 시작 방법과 현재 상태
3. docs/review/작업이력.md — 지금까지 확정된 결정과 근거
4. docs/review/문서개정_체크리스트.md — 남은 문서 개정 항목
5. docs/review/회의안건_20260729.md — 아직 안 정해진 것

읽은 뒤에 (a) 지금 저장소 상태, (b) 막혀 있는 항목과 그 이유, (c) 바로 착수 가능한 작업을
정리해서 알려줘. 산출물 HWP 본문이 필요하면 원본 대신 docs/extracted/ 의 추출본을 읽어.

나는 이응균, 이 팀의 PM이고 산출물 문서 총괄을 맡고 있어.
```

---

## 3. 현재 상태 (2026.07.30)

| 영역 | 상태 |
|---|---|
| 저장소 골격 · 산출물 버전 관리 · 본문 추출 파이프라인 | 완료 |
| 산출물 전수 검수 → 개정 항목 도출 | 완료 |
| 선행 안건 확정 (1-1 · 2 · 3 · 4 및 후속 결정) | 완료 |
| `db/schema.sql` ↔ 04 · 05 문서 정합 | **일치** ✅ |
| 01~04 HWP 개정 | **완료** ✅ |
| 05 테이블명세서 | **2건** 남음 — `05-N③`(작성일자) · `SD-E1`(`role`) |
| 발표자료 PPTX 개정 | **완료** ✅ (2026.07.30 원문 대조 확인) |
| 화면설계서 PPTX | 텍스트 반영 완료 · **디자인 작업 남음** → [`화면설계서_개정안.md`](review/화면설계서_개정안.md) |
| 라이프로그 전처리 (`ai/`) | 파이프라인 검증 완료 (PMData `p01` · GLOBEM 샘플 1개) |
| 프론트엔드 | 화면 7개 목업 완성 · **API 연동 0줄** (`함은선` 브랜치) |

**8/11 문서 개정 마감 · 8/28 최종 발표. 7/31부터 3주차 — 개발 착수.**

### 지금 해야 할 일

1. **백엔드 라우터 구현** — `users`·`devices` → `lifelog` → `chat` 순서
2. **문서 개정 3건** — `05-N③` · `SD-E1`(04·05 HWP)
3. **미결 안건 2건** — [`회의안건_20260729.md`](review/회의안건_20260729.md). 이미지 섹션 확인 · 런타임 설치 현황
4. **화면설계서 디자인 작업** — 완성된 `docs/design/` 시안 6장 배치와 기존 와이어프레임 마커 수정. 함은선 님 담당

### 알아둘 것

- **`db/schema.sql` 이 정본입니다.** 04·05 문서와 완전히 일치하는 상태이니, 스키마 관련 서술은 이 파일을 기준으로 확인하세요.
- **문서 5종 + 발표자료 2종이 서로 물려 있습니다.** 한 곳을 고치면 다른 곳도 같이 고쳐야 하는 경우가 많으니, 수정 전에 체크리스트의 「연쇄 영향」·「수정 대상」을 확인하세요.
- **`[확인]` 표시 항목은 추정으로 채우지 마세요.** 법령 조문, 데이터셋 접근성 등이 포함됩니다.

### ⚠ 붙여넣기 사고가 세 번 났습니다

체크리스트 문안을 HWP 에 붙여넣을 때마다 확인하세요. 상세는 체크리스트 「표기 규칙」.

1. **마크다운 기호** — `**굵게**` 별표, `` `코드` `` 백틱이 딸려 들어감
2. **스마트 따옴표** — 한글이 `'FRIEND'` → `'FRIEND’` 로 바꿔 **SQL 이 실행되지 않음**
3. **고치다 한 글자 더 지움** — 고친 줄은 앞뒤를 함께 다시 읽으세요

### 문서 대조 방법

문서를 고친 뒤 **PDF 로 내보내고** 아래를 돌리면 `docs/extracted/` 가 갱신되어 diff 로 변경분을 볼 수 있습니다.

```powershell
python tools/doc2txt.py
```

> **이 PC 는 Python 3.12.7 이 PATH 에 없습니다.** 전체 경로로 실행하세요.
> `& 'C:\Users\smhrd1\AppData\Local\Programs\Python\Python312\python.exe' tools/doc2txt.py`
>
> 새로 설치한 Python 은 **애플리케이션 제어 정책이 numpy DLL 을 차단**합니다. pandas 가 필요한 스크립트는 아나콘다 쪽(`C:\Users\smhrd1\anaconda3\python.exe`)으로 돌리세요.

---

## 3-3. 2026.07.31 (3차 세션) — 백엔드 착수, 브랜치 통합

### ⚠ 브랜치를 쓰지 않습니다

`backend` · `함은선` 을 **전부 main 에 병합**했습니다. 혼자 개발하는 상황이라 브랜치가 마찰만 만들었습니다. **모든 작업을 main 에서 하세요.**

### 지금 돌아가는 것

```
PostgreSQL 17 ─ lisn DB (테이블 8 · 인덱스 6 · EMOTIONS 9종 시드)
        ↑
FastAPI ─ /api/v1/auth/* 6개 · JWT 인증
```

회원가입 → 로그인 → 로그아웃 전 구간을 실제 서버로 검증했습니다.

```powershell
cd backend
uvicorn app.main:app --reload
```

`backend/.env` 는 gitignore 대상이라 **새 PC 에서는 `.env.example` 을 복사해 다시 만들어야 합니다.**

### 완료

| 항목 | 내용 |
|---|---|
| [`API설계_사전결정.md`](review/API설계_사전결정.md) | 7건 확정. 명세를 바꾸기 전에 반드시 읽을 것 |
| [`API명세_초안.md`](API명세_초안.md) | 외부 32 · 내부 2. 유스케이스 14건 전수 커버 |
| SQLAlchemy 모델 8종 | `schema.sql` 매핑. `create_all()` 안 씀 |
| 인증 계층 + auth 라우터 6개 | bcrypt · JWT · `require_admin` |
| [`HANDOFF-CODEX.md`](design/HANDOFF-CODEX.md) | 프론트 인수인계. Codex 가 단독으로 읽고 시작 가능 |
| 신규 화면 시안 6장 | `docs/design/` HTML 원본 + PNG 완료. 모바일 4장·관리자 웹 2장 |

### 남은 TODO — 코드에 표시돼 있음

- **`phone` 이 평문 저장**입니다. `02-F`(3) 은 AES-256-GCM 을 요구합니다
- **SMTP 미설정**이라 비밀번호 재설정 토큰을 로그로 출력합니다. 운영 전 제거 필수

### 이어서 할 것

1. `users` · `devices` 라우터 — 인증이 되니 바로 이어짐. `phone` 암호화도 여기서
2. `lifelog` 라우터 — `POST /lifelog/batch` 가 앱 push 의 핵심
3. `chat` 라우터 — OpenAI 연동
4. `HEALING_CONTENTS` 시드 — **비어 있어서 콘텐츠 추천이 동작할 수 없습니다.** 04 문서 7항이 "사전 안전 검수를 거친 콘텐츠만"을 원칙으로 못 박아 사람이 골라야 합니다
5. 문서 3건 — `FR-DP-001`(pull→push) · `SD-E1`(04·05 `role`) · `05-N③`(작성일자)

---

## 3-2. 2026.07.30 (2차 세션) — 프론트 방향 전환

**프론트엔드를 문서 기준으로 재구축하기로 했습니다.** 기존 Flutter 앱이 화면설계서를 참조하지 않고 작성되어 화면 구성·기능 범위가 문서와 다릅니다. **앱이 아니라 문서가 정본입니다.**

### 완료

| 항목 | 내용 |
|---|---|
| `함은선` 브랜치 정리 | `flutter_app` → `app` 개명, `lib/config·models·services` + `frontend/admin` + `frontend/design` 신설, 패키지명 `com.example` → `com.lisn.maeume`, `minSdk` **26** (Health Connect 요구값) |
| 화면설계서 감사 | 복붙 슬라이드 3장, 누락 화면 6개, 사라진 기능 2건, `MLCM_500` 4중 부여 발견 |
| [`화면설계서_개정안.md`](review/화면설계서_개정안.md) | Part A~E. 화면 **9개 → 15개** |
| **화면설계서 PPTX 실제 반영** | slide1·2·10·13·14·15 텍스트 교체, 단독 `-` 36개 제거(slide7 서식으로 통일). `tools/patch-pptx-text.ps1` |
| `db/schema.sql` | `USERS.role` 추가 (`SD-E1`) |
| `.claude/settings.json` | 읽기 전용 명령 허용 패턴 25개. **팀 공유됨** |
| [`docs/design/`](design/README.md) | 신규 화면 시안을 코드로 만드는 방법 + `MAIN_EMERGENCY_01` 완성 |

### `USERS.role` 을 왜 넣었나

관리자 기능이 `MLCM_501` · `FR-MN-003` · `MLCM_510` · 04 접근통제 · 발표자료 slide5 에 모두 있는데 **데이터 모델에 관리자 개념이 없었습니다.** 판별 수단이 없어 `MLCM_501` 을 구현할 수 없는 상태였습니다. 이 컬럼이 `02-F`(6) 의 *"접근통제(본인 및 권한 있는 관리자로 제한)"* 방어 논리도 완성합니다.

### PPTX 를 코드로 고치는 법

`tools/patch-pptx-text.ps1` 이 예시입니다. **한글·PowerPoint 로 다시 저장하면 임베드 폰트가 풀리는데, XML 을 직접 고치면 그 위험이 없습니다.**

- `<a:t>` 안의 글자만 바꾸고 `<a:rPr>`(서식)은 건드리지 않으므로 폰트·크기가 그대로 유지됩니다
- **교체는 정확히 1건 매칭될 때만 적용**하도록 짰습니다. 0건이면 건너뛰고 보고하므로 재실행해도 안전합니다
- 적용 후 반드시 검증하세요 — 슬라이드 수, 전체 파트 수, 각 슬라이드 XML 파싱

### 남은 화면설계서 작업 — 함은선 님

전부 **와이어프레임 이미지에 ❶❷❸ 마커를 얹는 디자인 작업**이라 코드로 못 합니다.

- slide7 로그인(4→7), slide12 챗봇 대화(4→8), slide9 키 입력란 — 항목이 늘어나 마커 추가 필요
- 신규 화면 6장 배치
- slide2 변경이력 v0.7 행 추가·검수자 기입, slide3 메뉴 구성도 노드 추가

문안은 개정안 Part A·B 에 전부 있습니다.

### 이어서 할 것

1. `SD-E1` 을 04·05 HWP 에 반영 (제가 못 함)

---

## 4. 저장소 밖 리소스

- [Notion 프로젝트 페이지](https://app.notion.com/p/3ab02025254781d18e1ac402a0f59d77) — 자료실, 팀원 4명 게스트(보기 전용) 공유
- [Google Drive 공유 폴더](https://drive.google.com/drive/folders/1myjO0y6uNJW75gCbehvgbTvioo-Te2EW)

> Notion 은 무료 플랜 1,000블록 제한을 피하려고 팀원을 **게스트**로 초대한 구조입니다. 멤버로 승격하면 제한이 걸립니다.

---

## 5. 이 문서들을 유지하는 방법

대화에서 결정된 내용은 **반드시 저장소에 남기세요.** 채팅 기록은 기기와 세션을 넘어가지 않지만 저장소는 따라갑니다.

- 결정이 나면 → [`작업이력.md`](review/작업이력.md) 맨 위에 기록하고, **해당 항목을 안건·체크리스트에서 삭제**
- 문서 개정을 반영하면 → 체크리스트에서 해당 항목 삭제
- 프로젝트 상태가 바뀌면 → 이 문서 3절
- 작업 규칙이나 새로 발견한 함정 → `CLAUDE.md`
