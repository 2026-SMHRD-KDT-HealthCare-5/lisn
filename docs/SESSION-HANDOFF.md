# 세션 인수인계

> 다른 컴퓨터나 새 세션에서 작업을 이어갈 때 이 문서부터 읽으세요.
> Claude Code 의 대화 기록과 메모리는 **PC 로컬에 프로젝트 경로 기준으로** 저장되므로 기기 간에 따라가지 않습니다. 대신 맥락을 저장소에 남겨두는 방식으로 이어갑니다.
>
> **최종 갱신 2026.08.25** — ⭐ **이어서 작업한다면 [§3-0](#3-0-지금-여기부터-20260825)
> 부터 읽으세요.** 그 아래 §3(8/06)·§3-2(8/24)는 지나간 기록이고, **지금
> 해야 할 일은 §3-0 에 있습니다.**

## 0. 문서 역할 분담

| 문서 | 담는 것 |
|---|---|
| [`docs/진행/작업이력.md`](진행/작업이력.md) | **완료된 결정과 그 근거.** 날짜순 누적. 새 결정이 나오면 맨 위에 추가 |
| [`docs/진행/문서개정_체크리스트.md`](진행/문서개정_체크리스트.md) | **아직 문서에 반영 안 된 개정 항목.** 반영되면 지움 |
| [`docs/llm/PROMPT_REFERENCE.md`](llm/PROMPT_REFERENCE.md) | LLM·프롬프트·API 자료 검증 결과와 출처 우선순위 |
| [`docs/llm/USAGE_LOG.md`](llm/USAGE_LOG.md) | LLM 관련 구현에 실제 사용한 자료·방법·평가 결과 |
| [`docs/학습자료.md`](학습자료.md) | **팀 내부 학습용.** 왜 이렇게 설계했나 · 되돌리면 안 되는 것 · 실패 사례집 |
| [`docs/진행/구현_갭.md`](진행/구현_갭.md) | 문서 ↔ 구현 대조. **어긋나면 앱을 고칩니다** |
| 이 문서 | 새 환경에서 시작하는 방법 · 현재 상태 요약 |

**완료된 항목을 체크리스트나 안건 문서에 남겨두지 마세요.** 무엇을 해야 하는지가 안 보이게 됩니다.

---

## 1. 새 환경에서 시작하기

```bash
git clone https://github.com/2026-SMHRD-KDT-HealthCare-5/lisn.git
```

```powershell
.\tools\check-env.ps1
```

의존성과 `backend/.env`를 구성한 뒤에는 루트에서 아래 명령 하나로 FastAPI·AI 추론 서버·
관리자 웹·Flutter를 실행할 수 있습니다.

```powershell
.\tools\start-dev.ps1
```

VS Code 사용자는 `lisn.code-workspace`를 열고 `Ctrl+Shift+B`를 누르면 됩니다. 실제
프로세스를 띄우지 않고 준비 상태만 확인할 때는 `.\tools\start-dev.ps1 -Check`를 사용합니다.

> ### ⚠ `start-dev.ps1` 이 해주지 않는 두 가지 (2026.08.03)
>
> 새 PC 에서 여기서 막힙니다. 절차와 함정은 루트
> [`README.md`](../README.md) 「빠른 시작」과 [`SETUP.md`](SETUP.md) 에 있습니다.
>
> **① DB 를 만들지 않습니다.** `CREATE DATABASE` → `schema.sql` → `seed_*.sql` 을
> 직접 넣어야 합니다. `psql` 은 PATH 에 없어 전체 경로로 불러야 하고, 한글
> Windows 콘솔에서는 `$env:PGCLIENTENCODING='UTF8'` 없이는 UTF-8 인 `db/*.sql` 이
> 중간에 깨집니다.
>
> **② 로그인할 계정을 만들지 않습니다.** 시드가 만드는 계정은
> `demo.crisis@lisn-test.example`(비밀번호는 `seed_demo_persona.sql` 주석) 하나뿐입니다.
> 작업이력에 적힌 `admin@lisn.dev` 는 **저장소에 없습니다** — 앱에서 회원가입한 뒤
> `role` 을 `ADMIN` 으로 올리세요(`SETUP.md` 14번).
>
> 잘 떴는지는 `/docs` 가 아니라 **`/health/db` 가 `connected`** 인지로 봅니다.
> `/docs` 는 DB 가 없어도 열립니다.

그다음 아래 프롬프트를 Claude Code 에 **그대로 붙여넣으면** 이전 세션과 같은 상태에서 시작합니다.

---

## 2. 킥오프 프롬프트 (복사해서 붙여넣기)

```
귀기울임(LISN) 프로젝트를 이어서 진행할게. 시작 전에 아래를 읽고 현재 상태를 파악해줘.

1. CLAUDE.md — 프로젝트 맥락, 작업 규칙, 이 저장소 특유의 함정
2. docs/SESSION-HANDOFF.md — **§3-0 「지금 여기부터」를 먼저** 읽어
3. docs/가이드/최종발표자료_작성기준.md — 8/28 발표자료 작성 기준
4. docs/검증/기업과제_대조_방어_20260825.md — 기업 브리프 대조·예상 질문

읽은 뒤에 (a) 지금 저장소 상태, (b) 막혀 있는 항목과 그 이유, (c) 바로 착수 가능한 작업을
정리해서 알려줘. 산출물 HWP 본문이 필요하면 원본 대신 docs/extracted/ 의 추출본을 읽어.

나는 이응균, 이 팀의 PM이고 산출물 문서 총괄을 맡고 있어.
발표가 8/28 이야. 목표는 좋은 평가를 받는 것이고, 부족한 부분은 우리 팀 능력이 아니라
구조적 제약이라는 걸 검증 가능한 근거로 보여주고 싶어.
```

---

## 3-0. 지금 여기부터 (2026.08.25)

> **이 절만 읽어도 이어서 일할 수 있습니다.** 아래 §3(8/06)·§3-2(8/24)는
> 지나간 기록입니다.

### ⛔ 가장 먼저 — 푸시가 안 돼 있습니다

```bash
git log --oneline origin/main..HEAD   # 9건
git push
```

**이 PC 에 git 자격 증명이 없어 에이전트가 푸시하지 못했습니다.**
대화형 터미널에서 한 번 `git push` 해서 브라우저 로그인을 등록하세요
(`cmdkey /list` 에 `git:https://github.com` 이 있으면 등록된 상태).

### 8/25 에 한 일 — 세 덩어리

#### ① 학습된 모델을 판정에 넣었습니다 ⭐

**브리프 필수 항목인 「기저선 이탈 위험 스코어」를 학습으로 개선**했습니다.

```
참가자 내부 AUC   기존 규칙 0.491  →  학습된 집계 0.609
이득 +0.115 (참가자 단위 부트스트랩 95% +0.056 ~ +0.176)
```

- **입력을 하나도 안 늘렸습니다** — 「상위 3개 평균 ÷ 4.0」이라는 **임의
  집계만** 로지스틱 회귀로 교체
- **경보 총량이 같습니다** — 모델 확률을 규칙과 같은 분포로 옮김
  (histogram matching)
- **규칙 바닥(0.75)·폴백** — 모델이 없거나 지표가 절반도 안 차면 규칙으로
  돌고 `model_version` 이 `rule-` 접두사를 유지합니다

| 파일 | |
|---|---|
| `ai/server/model_score.py` | 학습된 집계·보정·폴백 |
| `ai/server/model/anomaly_lr.joblib` | 아티팩트(8KB, 커밋됨) |
| `ai/server/main.py` | `_predict` 의 ② 단계만 교체 |
| `ai/server/test_model_score.py` | 안전 성질 8건 |
| `ai/train/train_service_model.py` | 재학습 |

⚠ **`model_version` 이 이제 `hybrid-lr-v1-20260825` 입니다.**
`rule-` 로 시작하면 모델이 관여하지 않은 것입니다.

#### ② 앱 사용 로그를 구현했습니다 — 브리프 유일 미충족이었습니다

기업 브리프 개발목표 **첫 줄**이 「**앱 사용 로그** + 웨어러블」인데
웨어러블만 있었습니다. **스키마부터 판정까지 관통**으로 넣었습니다.

```
AppUsagePlugin.kt (UsageStatsManager) → app_usage_reader.dart
→ lifelog_sync.dart _attachUsage() → schemas/lifelog.py
→ db/schema.sql [05-U] → ai/server/main.py _FEATURES
```

지표 셋 — `screen_time_min` · `night_screen_min`(22~06시) ·
`app_session_count`. **패키지명·앱 이름은 저장하지 않습니다.**
`AccessibilityService` 는 Play 정책 심사 대상이라 **의도적으로 피했습니다.**

⚠ **권한이 없으면 `0` 이 아니라 `null`** 입니다. 0 으로 적재하면 개인
기준선이 내려앉습니다. 테스트로 고정돼 있습니다.

#### ③ 기업 브리프 원문을 받아 대조했습니다 ⭐⭐

**우리가 브리프를 오해하고 있었습니다.**

| | |
|---|---|
| **필수** | 개인 기저선 → **이탈 기반 위험 스코어** ← 우리가 만든 그것 |
| **선택** | 우울·정서 상태 **분류** 모델 ← 26회 시도한 그것 |
| 데이터 | **[오픈 데이터 활용]** ← 기업이 지정 |
| 완성도 | **MVP(데모 수준)** · 목적은 **관련 기술 교육** |

원본은 `documents/기업과제/PROJECT_02_*.pdf`.
대조표·예상 질문은
[`기업과제_대조_방어_20260825.md`](검증/기업과제_대조_방어_20260825.md).

> ⛔ **「기업이 데이터를 안 줘서 못 했다」고 말하면 안 됩니다.** 브리프에
> 「오픈 데이터 활용」이 명시돼 있고 심사위원이 읽을 수 있습니다.
> **멘토가 그 자리에 있을 수 있습니다.**

### ⭐ 다음에 할 일 — 최종발표자료 개정

**기준 문서를 세워뒀습니다** —
[`docs/가이드/최종발표자료_작성기준.md`](가이드/최종발표자료_작성기준.md).
**슬라이드를 고치기 전에 반드시 읽으세요.**

**손댈 5장 / 안 댈 11장**입니다.

| 우선 | 슬라이드 | 무엇 |
|---|---|---|
| **P0** | **9** | `model_version` 이 이제 `hybrid-` 라 **현재 문구가 거짓**입니다 |
| **P0** | **12** | 회귀 테스트 332 → **348** (AI 22→30 · 앱 165→173) |
| **P1** | **4 또는 8** | 앱 사용 로그 한 줄 (브리프 필수인데 없음) |
| **P1** | **9** | 브리프 지정 방법 비교(Isolation Forest 0.473/0.494 vs 우리 0.609) |
| **P1** | **9 또는 15** | 선행연구 재현 — **가장 차별적인 재료** |
| **P1** | **15** | 항목 갱신(실기기 확인 · 표본 확대) |

> ⚠ **백엔드 131 은 이 PC 에서 재측정할 수 없습니다**(`backend/.env` 없음).
> 로컬 DB 가 있는 PC 에서 한 번 돌려 확인한 뒤 348 을 확정하세요.

**슬라이드 9 는 감점을 막는 수정이 아니라 가점을 만드는 수정입니다.**
지금 문구는 「AI 과제에서 AI 를 안 썼다」로 읽히는데, 실제로는 학습된
모델이 판정에 들어가 있고 규칙보다 낫다는 것을 통계적으로 보인 상태입니다.

### 발표 방어 재료 — 「우리 탓이 아니다」를 숫자로

| 근거 | 출처 |
|---|---|
| 표본 62명 = **이 분야 중앙값 60.5명**, 76%가 100명 미만 | JMIR 2025;27:e77066 (42편) |
| **외부 검증한 연구는 42편 중 1편(2%)** | 〃 |
| 같은 데이터 **70% 논문을 재현하니 기준선이 66.7%** | `ai/train/eval_replicate_paper.py` |
| 브리프 지정 Isolation Forest 는 규칙과 구분 안 됨 | `ai/train/eval_isoforest.py` |

⚠ **제도·플랫폼 제약(AI Hub 안심존·삼성헬스 HRV·HealthKit 장비)은 먼저
꺼내지 마세요.** 질문이 나왔을 때만 씁니다.

### AI 검증 이력 — 26회

전부
[`학습모델_활용_시도_20260824.md`](검증/학습모델_활용_시도_20260824.md)
에 있습니다. **재현 스크립트가 `ai/train/` 에 전부 커밋돼 있습니다.**

⚠ **원본 데이터는 저장소에 없습니다**(`ai/data_raw/` gitignore).
재현하려면 [Zenodo LifeSnaps](https://zenodo.org/records/7229547) 에서
`rais_anonymized.zip`(587MB)을 받아 `ai/data_raw/lifesnaps/` 에 두고
`ai/train/extract_bson.py` 를 돌리세요(6분, 9.7GB → 152MB parquet).

### 아직 안 한 것

| | 왜 |
|---|---|
| **git push** | 이 PC 에 자격 증명 없음 — 대화형 터미널 필요 |
| 슬라이드 개정 | 기준만 세웠고 **PPTX 는 안 건드렸습니다** |
| 앱 사용 로그 실기기 확인 | 에뮬레이터는 사용 이력이 거의 없어 값이 0 에 가깝습니다 |
| Health Connect 실기기 검증 | 실기기 없이는 못 넘김 |
| FCM 실기기 e2e 수신 | 자격증명 초기화까지만 확인 |
| 폰트 재임베드 | 8/24 병합 때 풀렸습니다. PowerPoint 로 다시 켜야 합니다 |

### ⚠ 이 세션에서 겪은 함정 — 다시 밟지 마세요

- **pandas 3 은 `to_datetime` 기본 해상도가 마이크로초**인데
  `Timestamp.value` 는 나노초입니다. 그대로 비교하면 **1000배 어긋나
  결과만 0 이 되고 에러는 안 납니다**
- **셔플 대조에서 파생 피처도 다시 계산**해야 합니다. 참 라벨로 만든
  이력을 두고 목표만 섞으면 대조군이 참 라벨 구조를 들고 있습니다
- **피처 선택은 파이프라인 안에서** 합니다. 밖에서 고르면 평가 폴드의
  정답을 이미 본 것이라 AUC 가 부풀려집니다
- **한글이 든 파일을 bash heredoc 으로 패치하면 깨집니다.** 큰 수정은
  Write 로 통째로 다시 쓰세요

---

## 3. 현재 상태 (2026.08.06)

> ### 발표자료 PPTX 는 이번 범위에서 뺐습니다 (2026.08.04 · PM 결정)
>
> `프로젝트_기획서_발표자료_귀기울임.pptx` 는 **기획서 발표용이고 그 발표는
> 끝났습니다.** 지난 자료를 소급해 고치지 않습니다 — `PL-20`~`PL-24` 를
> **8/11 마감 범위에서 뺍니다.**
>
> **최종 발표는 8/28 이고 아직 착수하지 않았습니다.** 그 자료를 이 PPTX 에서
> 파생시킨다면 `85%`·`0.80` KPI 타일과 `LSTM Autoencoder + LightGBM` 표기가
> 그대로 따라옵니다. 교체 문안은 [`산출물_개정안.md`](결정/산출물_개정안.md)
> `PL-20` 에 확정본으로 있습니다.
>
> ⚠ 그래서 **`tools/check_docs.py` 가 발표자료를 검사하지 않습니다**(2026.08.06).
> 범위 밖인 것이 계속 실패로 잡히면 **검사기가 0 이 될 수 없어 완료 판정
> 기준으로 못 씁니다.** 지금은 전부 통과합니다.
>
> **8/28 준비를 시작하면 예외를 도로 빼세요** — `check_docs.py` 의 `EXEMPT`.
> 빼는 순간 모델명 7 · 성능목표 2 가 다시 잡히고, 그게 고칠 목록입니다.

| 영역 | 상태 |
|---|---|
| 저장소 골격 · 산출물 버전 관리 · 본문 추출 파이프라인 | 완료 |
| 산출물 전수 검수 → 개정 항목 도출 | 완료 |
| 선행 안건 확정 (1-1 · 2 · 3 · 4 및 후속 결정) | 완료 |
| `db/schema.sql` ↔ 데이터베이스요구사항분석서·테이블명세서 정합 | **일치** ✅ |
| 기획서~테이블명세서 HWP 개정 | **전부 완료** ✅ 8/05 에 18건 · 8/06 에 `PL-25`·`PL-38`·`PL-39` + **최종 점검 12건**. **8/11 마감 대상 0건** |
| 발표자료 PPTX 개정 | (기획서용) 7/29 범위는 완료 ✅ · `PL-20`~`PL-24` 6장은 **미해결·이월** |
| **최종발표자료**(8/28용) | **완료** ✅ pptxgenjs 로 새로 제작, 16장(2026.08.22 — 시연영상 슬라이드 추가). `PL-20`~`PL-24` 서술 없음(별개 파일) · 성과 지표 실측값 최신 |
| 화면설계서 PPTX | **21장 개정 완료** ✅ 신규 6장 · 문안 · 시안 · 콜아웃 전부 반영 |
| 라이프로그 전처리 (`ai/`) | 파이프라인 검증 완료 (PMData `p01` · GLOBEM 공개 샘플 4개) |
| 백엔드 API | **전 라우터 구현·검증 완료** (34개) — `/health` 2개 제외. 2026.08.24 `/admin/sync-failures` 추가 |
| AI 추론 서버 | 구동 완료 · **개인 기준선 이탈 탐지**(`rule-baseline-v1`) — 지표 7개 · 중앙값·MAD robust z · `streak_days` · `deviant_features`. **학습은 했고 채택하지 않았습니다** — LightGBM 학습 후 GLOBEM·LifeSnaps 두 데이터로 검증(0.528 · 0.479~0.540) → 근거를 갖고 미채택 |
| 프론트엔드 | **앱 13개 화면 · 관리자 웹 2개 전부 API 연동 완료** |
| 위기 판정 평가셋 | **완료** ✅ 211건(위기 111·비위기 100) · `team` 211/211 · 교차 라벨링·조정 완료 · **재현율 0.946 · 정밀도 0.921 · F1 0.933** (2026.08.12 · 목표 둘 다 충족) |
| 성능 요건 | **통과** ✅ `NFR-DV-001` 1418/2052ms · `NFR-TS-001` 1535/1986ms (예산 3000ms · 2026.08.12) |
| 문서 ↔ 구현 대조 | **전건 해소** ✅ 화면 6건 + 요구사항 단위 갭 1·2·3·4·5·8·9 전부. 마지막 둘(갭3 미수신 감지·갭4 SMTP)은 2026.08.24 → [`구현_갭.md`](진행/구현_갭.md) |
| 선제 접촉 (`MLCM_220`) | **끝에서 끝까지 완료** ✅ 조건 6개 · 세션 선생성 · `OUTREACH_LOGS` · 홈 카드 · 성격 선택 배너 · **FCM 발송**(2026.08.21 실측 확인) |
| Health Connect 수집 | **구현 완료** · 에뮬레이터 워커 동작 확인 · **실기기 검증만 남음** |
| 회귀 테스트 | **332건** (backend 131 · AI 22 · 앱 165 · 관리자 웹 14) — 2026.08.24 실측(SMTP 3건 + 미수신 감지 10건 추가로 118→131). **손으로 세지 말고 아래로 대조하세요** |
| 코드 전수 점검 | **결함 14건 해소** ✅ (2026.08.02) → [`작업이력.md`](진행/작업이력.md) 43·44차 |
| 실서버 관통 점검 | 13건 수정 뒤 **실패 0** ✅ (`tools/smoke_mvp.py`) |
| 릴리스 빌드 | APK·관리자 웹 **빌드 확인** ✅ · 실기기 전용 결함 3건 선제 수정 |
| 관리자 관제 4개 항목 | `MLCM_501` ❶분포 ❷목록·검색 ❸상세 ❹위기이력 **전부 완료** (2026.08.02) |
| 클라우드 배포 | **인프라 완료** ✅ NCP VM 1대 + Docker Compose. HTTPS(`101.79.24.15.nip.io`)·관리자 웹 로그인·FCM 실키·APK 재빌드 전부 확인(2026.08.24). **서버는 지금 정지 상태**(요금 절약) — 재개 방법은 [§3-2](#32-클라우드-배포-20260824) |

**8/11 문서 개정 마감 · 8/28 최종 발표. 7/31부터 3주차 — 개발 착수.**

**8/11 문서 마감 — 완료(0건).  8/28 최종 발표까지 D-6(2026.08.22 기준).**

> ⚠ **날짜를 상대값으로 적지 마세요.** 이 줄이 예전엔 "22일 · 5일"이라
> 적혀 있었는데, 8/06 에 쓴 값이 그대로 남아 8/22 에 읽으면 둘 다 틀립니다.
> 기준 날짜를 항상 괄호로 같이 적으세요 — `db/refresh_demo_dates.sql` 이
> 겪은 것과 같은 함정입니다.

> ### Gemini 가 재현율에서 앞섭니다 — 아직 성적은 아닙니다
>
> 같은 프롬프트로 **Gemini 3.6 Flash 가 재현율 0.922 · F1 0.931**. 정밀도도
> 0.941 로 거의 같아 **오탐을 늘려 얻은 것이 아닙니다.** `gpt-5.6` 이 놓친
> 10건 중 5건이 `S4`·`S5`·`S6`(준비·작별·신변 정리)로, **키워드로는 하나도
> 안 잡히는 유형**입니다.
>
> ⚠ **200건을 한 번에 준 배치 조건이라 그대로 못 씁니다.** 무료 한도에 막혀
> 같은 조건 재측정이 **53/211** 에서 멈춰 있습니다. 하루 20건씩 이어
> 돌립니다(아래 「잊기 쉬운 것」). **문서는 바꾸지 않았습니다** → `LLM-010`

### ✅ 산출물 최종 점검 — 완료 (2026.08.06)

산출물 5종을 **기계 → 사람** 순으로 훑었습니다. 문서 개정과 별개로,
**이미 반영된 내용이 서로 맞는지**를 본 작업입니다.

```powershell
python tools/check_docs.py --with-tests   # 모델명·성능목표·링크·인용·API·붙여넣기·테스트
python tools/check_numbers.py             # 문서 간 수치 · 미달 목표 단정
python tools/check_dup.py                 # 한 표 칸에 같은 문장이 두 번 있는가
python tools/check_screens.py             # 화면설계서 ↔ 앱
```

**넷 다 통과합니다.**

#### 잡은 것 — 12건 전부 반영

| 문서 | 무엇 |
|---|---|
| 요구사항정의서 | `PL-25` 음성·STT·TTS 잔존 |
| 요구사항정의서 | `NFR-AI-001` 중복 · 「200건 vs 211건」 모순 |
| 요구사항정의서 | 「문맥 확인 필요」 절 삭제 사고 → 복구 |
| 기획서 | 「텍스트와 음성(STT/TTS)」 2곳 |
| 기획서 | **「재현율 90% 이상을 확보한다」 단정** → 「목표로 한다」 |
| 빅데이터분석정의서 | 「설문」 — 기능이 어느 문서에도 없음 |
| 빅데이터분석정의서 | 「1차 모델」·「피처」 — 학습 안 하기로 함 |
| 빅데이터분석정의서 | 체성분을 Health Connect 기준으로 |
| 빅데이터분석정의서 | 「재현율 90% 이상 **달성**」 → 미달 명시 |
| 빅데이터분석정의서 | AI 서버 반환 계약 — `emotion_id`·`system_action` 틀림 |
| 빅데이터분석정의서 | 「`is_crisis=true` → `CRITICAL` 확정」 — 실제는 `MEDIUM` 이면 `CAUTION` |
| 빅데이터분석정의서 | 위기 단어 예시 「사망·힘듦」 — 사전에 없음 |

**데이터베이스요구사항분석서·테이블명세서는 이상 없었습니다** —
`schema.sql` 9개 테이블 89개 컬럼이 전부 있고 폐기 결정 잔재 0건.

#### 새로 만든 검사 셋

| | 무엇을 잡나 | 왜 |
|---|---|---|
| `check_numbers.py` | 문서 간 수치 · **미달 목표 단정** | 「200건 vs 211건」·「90% 확보한다」 |
| `check_dup.py` | 한 표 칸 안 중복 문장 | 개정 문안을 앞에 붙이고 옛 문안을 안 지움 |
| `check_docs.py` 「붙여넣기」 | 안내문·마크다운 기호 혼입 | 「교체」 두 글자가 본문에 들어감 |

**셋 다 「아는 사고」로 먼저 시험해 잡히는 것을 확인했습니다.** `check_dup`
은 처음 짠 것이 아는 사고를 못 잡아 다시 짰고, **그 과정에서 삭제 사고가
드러났습니다.**

#### ⚠ 남은 것 하나 — 구현 과제

**`severity` 의 `UNCLEAR` 가 코드에 없습니다.** `PL-29`·`PL-31` 이
요구사항정의서·빅데이터분석정의서에 반영됐는데 `CrisisVerdict.severity`
설명과 `CRISIS_SYSTEM` 프롬프트에 기준이 없어 **모델이 그 값을 낼 수
없습니다.** 문서가 정본이고 구현이 밀린 상태입니다.

> **8/12 · 한 번 만들어 보고 되돌렸습니다** → `docs/llm/USAGE_LOG.md` LLM-018.
>
> 판정값과 정책 매핑만 넣었더니 **재현율 0.946 → 0.919 · 정밀도 0.921 →
> 0.895 · F1 0.933 → 0.907** 로 **셋이 전부 내려갔습니다.**
>
> 이유는 **묶음의 절반만 만들었기 때문**입니다. `MLCM_320` 4-1·4-2 의
> UNCLEAR 는 ① 판별 불가 판정 ② **확인 질문 생성** ③ 답변으로 재판정
> ④ 한 세션 1회 제한 의 네 조각이고, **가치는 ②에서 나옵니다.** 되묻지
> 않으면 판별될 기회가 없어 그냥 CAUTION 으로 뭉개는 것이 되고, 대가로
> 원래 잡던 TP 3건을 잃습니다.
>
> **다시 한다면 ②③④(세션 상태 필요)까지 같이 만든 뒤 재채점하세요.**
> ①만 넣는 것은 분류기를 순수하게 나쁘게 만듭니다.

⚠ **프롬프트를 고치면 평가셋 캐시가 무효가 되어 211건 재채점**이
따라옵니다.

#### 다음에 점검할 때 — 알아야 할 것

- **삭제·교체 항목은 기계가 못 잡습니다.** 인용 검사는 「현재 문장이
  있는가」를 보는데 삭제 항목은 인용할 문장이 없습니다. **반영 뒤
  「추출본에서 0회인가」를 직접 세세요.** 오늘 이것으로 두 번 당했습니다
- **「전체 교체」 문안을 만들 때는 셀의 끝을 확인하세요.** 추출본 표 덤프는
  **쪽마다 잘립니다.** 조각만 보고 전체를 봤다고 착각하면 안 보이는 절을
  지웁니다(실제 발생)
- **띄어쓰기·조사·맞춤법은 점검 범위 밖입니다**(PM 결정 2026.08.06)

### 테스트 건수 대조

```powershell
$env:LISN_TEST_DATABASE_URL = "postgresql+asyncpg://postgres:비밀번호@localhost:5432/lisn"
$env:FLUTTER_BIN = "C:\flutter\bin\flutter.bat"   # PATH 에 있으면 생략
python tools/check_docs.py --with-tests
```

**환경변수 두 개가 필요한 이유**

- `LISN_TEST_DATABASE_URL` — `.env` 기본값이 **공용 DB** 라 그대로 돌리면
  `conftest` 가 막습니다. 테스트는 계정을 만들고 지우므로 팀 DB 에서
  돌리면 안 됩니다. **검사기가 대신 로컬을 골라주지 않습니다** — 어느 DB 로
  돌릴지는 사람이 정할 일입니다
- PowerShell 은 `VAR=x 명령` 을 못 씁니다. 그래서 `$env:` 로 받습니다

⚠ **건수만 맞으면 통과가 아닙니다.** 실패가 하나라도 있으면 건수를 세기
전에 멈춥니다. 전에는 `8 failed, 94 passed` 를 **94 로 읽어** 빨간 스위트가
통과했습니다(2026.08.06).

### ⭐ 지금 해야 할 것 — 2026.08.06

**문서·평가셋이 닫혔고 선제 접촉도 끝에서 끝까지 붙었습니다.**
남은 것은 **실기기에서만 확인되는 것**과 **외부 자격증명이 필요한 것**입니다.

| 순위 | 무엇 | 누가 | 메모 |
|---|---|---|---|
| **1** | **워치를 계속 차기 + Health Connect 실기기 검증** | 윤일준·함은선 | ⏳ **오늘부터 시작해야 합니다** — 워치 데이터가 2일치뿐이라 기준선이 안 잡힙니다(아래) |
| 2 | **FCM 발송 경로** | 윤일준 | Firebase 자격증명 선행. 앱 토큰 발급 + 서버 발송 **양끝 모두** 비어 있습니다 |
| 3 | SW 결과보고서 증빙 2장 · `MUSIC`/`FOOD` 콘텐츠 | 이응균 | **문서 개정은 전부 끝났습니다** — 남은 산출물 작업은 이것뿐 |
| ~~4~~ | ~~프롬프트 검토 (`OUTREACH_SYSTEM`)~~ | 김건영 | ✅ **2026.08.12 종료 — 프롬프트 수정 없음.** 목표 전부 통과(아래) → `USAGE_LOG` `LLM-019` |
| — | **8/28 발표 준비 시작 전에 읽을 것** | 전원 | [`발표_재료`](검증/발표_재료_20260806.md) 내세울 것 · [`발표_방어_숫자점검`](검증/발표_방어_숫자점검_20260806.md) 말하면 안 되는 것 |

> ### ✅ 4번 종료 — 고칠 것이 없었습니다 (2026.08.12)
>
> **먼저 요건부터** — `MLCM_220` 3단계는 「관찰된 이탈 지표를 근거로 첫 발화를
> 생성한다」까지만 규정합니다. **페르소나 반영은 안 적혀 있습니다.** 요건
> 미충족이 아니라 완성도 문제였습니다.
>
> 운영과 같은 모델(`gpt-5.4-mini`)로 12건을 재측정한 결과입니다.
>
> | 항목 | 이전(`gpt-5.6` 시절) | 목표 | 현재 |
> |---|---|---|---|
> | 유사도 평균 | 0.76 | < 0.60 | **0.50** ✔ |
> | 유사도 최대 | 0.97 | < 0.85 | **0.71** ✔ |
> | 닫는 문장 동일 | 10/12 | ≤ 3/12 | **0/12** ✔ |
> | 기계 검사 위반 | — | 0건 | **0/12** ✔ |
>
> **`LLM-017` 의 모델 교체가 이것도 같이 고쳤는데 아무도 다시 안 쟀습니다.**
> 프롬프트는 손대지 않았습니다 — 통과하는 것을 고치면 되레 무너집니다.
>
> ⚠ **재는 도구가 고장나 있었습니다.** `eval_outreach.py` 가 `--model` 을
> 헤더에만 찍고 실제 호출은 안 바꿨고, 캐시 키에 모델이 없었으며, 기본
> 모델이 **판정용(`gpt-5.4`)**이었습니다. 셋 다 `LLM-016` 이 응답 모델을
> 가른 뒤 따라오지 않은 것입니다. 고쳤습니다.
>
> **사람 판단 대기 2건** — ① COUNSELOR 3/12 건이 지속일수를 그대로 말합니다
> (「최근 5일 동안」·「2주째」). 감시 느낌 대 근거 제시로 볼 여지가 갈립니다
> ② FRIEND 1건에 세미콜론(`;`)이 들어갑니다

> ### ⏳ 1번은 시작이 늦으면 만회가 안 됩니다 (2026.08.06 발견)
>
> 실기기 내보내기를 받아 보니 **수면 2일 · 심박 2일 · HRV 1일**뿐입니다.
> 폰만으로 모이는 걸음·칼로리는 8~35일인데 **워치가 필요한 것만 짧습니다.**
>
> 기준선 이탈 탐지는 7지표 중 **5개가 수면 파생**이고, AI 서버는 실측
> **3일 미만이면 422** 로 끊습니다. 선제 접촉은 **연속 이탈 3일**이 조건이고
> 기준선은 **최근 14일**입니다.
>
> **기기를 손에 넣은 날 바로 되는 일이 아닙니다.** 3일이면 판정이 돌기
> 시작하고 14일이면 기준선이 제 모습을 갖춥니다. 8/28 까지 22일이라 지금
> 시작하면 넉넉하지만, **미루면 그만큼 그대로 밀립니다.**
>
> 필드 대조·단위 함정은
> [`삼성헬스_내보내기_대조_20260806.md`](검증/삼성헬스_내보내기_대조_20260806.md).

> **FCM 이 없어도 선제 접촉은 동작합니다.** 앱을 열면 홈 카드와 배너로
> 닿습니다. FCM 은 **앱을 안 여는 사람**에게 닿기 위한 것이라, 없다고
> 기능이 성립하지 않는 것은 아닙니다. 시연은 지금 상태로 가능합니다.


**세부 사항**

| | |
|---|---|
| 실기기 검증 | 아래 「1. Health Connect」 — **선제 수정한 3건**을 먼저 읽으세요 |
| FCM | 동의 저장은 닫혔습니다(갭 2). 남은 것은 **앱 토큰 발급 · 서버 발송** → [`구현_갭.md`](진행/구현_갭.md) |
| SW 결과보고서 | 「결과 ①③」은 채웠고 **증빙사진 2장·과정명·사용기간**이 남았습니다 |
| `MUSIC`·`FOOD` | 후보 3건 조사 완료 · **PM 승인·링크 재확인** |

> **끝난 일은 여기 두지 않습니다.** 평가셋(κ 포함) · HWP 개정 · 화면설계서
> `PL-26` · 발표자료 이월 · 선제 접촉 구현은 전부 닫혔고
> [`작업이력.md`](진행/작업이력.md) 에 근거와 함께 있습니다.

#### 잊기 쉬운 것 둘

**① Gemini 한도가 리셋되면 이 한 줄.** 순서대로 돌리면 하루치를 비위기
문장에 다 쓰므로 **갈리는 유형만** 지정합니다. 갈리는 유형 24건은
2026.08.05 에 채웠고(누적 53/211), 나머지는 하루 20건씩 이어 돌립니다.

```powershell
python tools/eval_crisis.py --stage llm --limit 20 --model gemini-3.6-flash --only S4,S5,S6,M8,M9
```

**② `.env` 는 이제 공용 DB 가 기본입니다**(2026.08.05). 방향이 예전과
반대이니 주의하세요.

| | 어디를 보나 |
|---|---|
| 평소 개발·통합 | **공용** — `.env` 기본값 그대로 |
| `pytest` | **로컬** — 공용을 향하면 `conftest` 가 막습니다 |
| 시연 녹화 | **로컬** — `.env` 주석에 주소가 있습니다(49차) |

테스트가 로컬을 봐야 하는 이유는 **계정을 만들고 지우기 때문**입니다. 공용
에서 돌리면 남의 개발 중에 행이 생겼다 사라지고, 실패하면 찌꺼기가 남습니다.

#### ⚠ 계좌번호가 든 파일을 커밋하지 마세요

`documents/` 에 SW 구매 관련 파일 4개가 들어와 있는데, 그중
**`1. [KDT][헬스케어5회차]프로젝트용 SW구매완료리스트.xlsx` 안에
`이응균`·`국민은행`·`계좌번호`가 들어 있습니다.**

**이 저장소는 공개입니다.** 올리면 지워도 커밋 기록에 남습니다.

**✅ 2026.08.06 — `.gitignore` 로 막았습니다**(`documents/*SW구매*`·`*구매완료*`).
전에는 「미추적으로 두기」로 막고 있었는데 그건 **사람이 매번 기억해야
하는 방식**입니다. `git add -A` 한 번이면 들어갑니다.

⚠ **파일은 PC 마다 다릅니다.** 이 PC 에는 없고 PM 의 다른 PC 에 있습니다.
규칙은 저장소에 있으니 어느 PC 에서든 걸립니다.

- 결제 증빙(매출전표·통장사본)도 같은 이유로 저장소에 두지 마세요
- 나머지 3개(결과보고서 양식 · 증빙 예시 · 팀별 양식)는 개인정보가 없어
  올려도 되지만, 짝이 되는 파일끼리 흩어지지 않게 함께 판단하세요

> **폴더명 소문자 통일은 2026.08.03 에 반영했습니다.** `Documents/` →
> `documents/` 이고 `01_`~`05_` 접두사가 빠졌습니다. 내용은 14개 전부
> 동일함을 해시로 대조하고 rename 으로 커밋했습니다. 경로를 참조하던
> 7곳(`tools/doc2txt.py` 등)도 같이 고쳤습니다.

> **5번은 HWP 편집이라 사람이 해야 합니다.** 교체 문안은
> [`문서개정_체크리스트.md`](진행/문서개정_체크리스트.md) `SD-B①` 에
> 그대로 있습니다. 문서는 「모델을 학습한다」고 되어 있는데 실제로는 하지
> 않기로 확정했고, 이제 **두 데이터셋 실측 근거**가 있습니다.

<details>
<summary>2026.08.02 에 닫힌 것 (펼치기)</summary>

| 항목 | 결과 |
|---|---|
| 화면설계서 슬라이드 6장 | 스크립트로 추가·검증. `tools/add-pptx-slide.ps1` |
| 문안 교체 `SD-A③`~`SD-A⑦` | 전부 반영. `SD-03`·`SD-07`·`SD-09`·`SD-12` 도 함께 |
| 시안 15장 갱신 | `tools/sync-pptx-mockups.py`. 캐릭터 잘림도 수정 |
| Health Connect 수집 | 권한·집계·전송·재시도 구현. 실기기 검증만 남음 |
| 문서 ↔ 구현 대조 6건 | 다섯은 앱을 고치고, 하나(음성 입력)는 문서를 고침 |

</details>

---

### 남은 일 셋 — 자세히

#### 1. Health Connect ⭐ — 구현은 끝났습니다. 실기기 확인만 남았습니다

2026.08.02 에 권한 요청·수집·전송·재시도를 전부 구현했습니다.

**에뮬레이터에서 확인된 것** (`flutter run -d emulator-5554`)

```
D/WM-WorkerWrapper: Starting work for ...workmanager.BackgroundWorker
I/flutter: [동기화] SyncResult(SyncOutcome.permissionDenied, sent=0, queued=0)
I/WM-WorkerWrapper: Worker result SUCCESS
```

15분 워커가 등록되고, 백그라운드 아이솔레이트가 실제로 실행되고, Health Connect
가 없으니 `permissionDenied` 로 정상 종료했습니다. **앱은 크래시하지 않습니다.**

### ⚠ 실기기 검증 전에 — 2026.08.02 에 선제 수정한 3건

**전부 에뮬레이터에서는 보이지 않던 것**입니다. 고쳐뒀지만 실기기에서
동작을 확인해야 합니다.

| 무엇 | 확인할 것 |
|---|---|
| 평문 HTTP 차단 | **`network_security_config.xml` 에 시연 PC IP 를 추가**했는지. 안 넣으면 릴리스 빌드가 전 API 실패합니다 |
| `tel` 패키지 가시성 | 109 버튼이 **전화 앱을 실제로 여는지** |
| 추천 카드 탭 | 카드를 누르면 **브라우저가 열리는지** |

> 앞의 둘은 증상이 「서버가 죽었다」·「전화 앱이 없다」로 보입니다.
> **그렇게 보이면 이 표를 먼저 의심하세요.**

**실기기에서 확인할 것** — 에뮬레이터에는 Health Connect 가 없어 여기까지입니다.

1. 가입 3단계에서 「연동하기」 → **권한 다이얼로그가 실제로 뜨는지**
2. 승인 후 걸음·수면이 홈에 뜨는지 (즉시 1회 당깁니다)
3. 앱을 닫고 15분 뒤 `last_synced_at` 이 갱신되는지
4. Health Connect 설정에서 권한을 껐을 때 수집이 멈추는지

- ⚠ 값이 없으면 **0 이 아니라 null** 입니다. 「0걸음」과 「측정 안 됨」이 갈립니다
- ⚠ `health_reader.dart` 의 `_types` 를 고치면 **매니페스트 권한도** 고쳐야 합니다.
  선언 안 된 타입은 예외 없이 빠진 채 승인돼, 그 지표만 영원히 null 이 됩니다
  (`health_permission_drift_test.dart` 가 대조합니다)
- 설계 이유는 `frontend/app/README.md` 「라이프로그 수집」 절에 있습니다


#### 2. `MUSIC`·`FOOD` 콘텐츠 — 팀이 직접

`EXERCISE`·`ARTICLE` 12건은 시드돼 있습니다. 기준은
[`힐링콘텐츠_큐레이션.md`](가이드/힐링콘텐츠_큐레이션.md).

> **판정 감정이 틀려도 해롭지 않은가**가 기준입니다. 이 기준으로 후보 2건을
> 탈락시킨 적이 있습니다(질병관리청 「운동」 — 질환별 처방·체중감량 /
> PTSD 카드뉴스 — 진단기준·약물명).

#### 3. 실기기 QA

지금까지 검증은 **전부 Android 에뮬레이터** 기준입니다. 1 이 끝난 뒤 진행합니다.

---

### 하기로 하지 않은 것 — 다시 묻지 않도록

| 항목 | 결정 |
|---|---|
| AI 모델 학습 | **안 합니다.** 공개 샘플 40명으로 ROC-AUC 0.528, 전체 데이터는 PhysioNet 심사 45일 |
| `GET /reports/export` | 서버에 안 만듭니다. **앱이 PDF 를 조판**합니다 |
| 음성 입력(STT) | 이번 범위 밖. 마이크 버튼 제거됨(테스트로 고정) |
| iOS | **장비·계정·일정** — macOS·Xcode 필수, Apple Developer Program 필요. ⚠ 「기술적으로 불가능」이 아닙니다(`health` 패키지는 HealthKit 도 감쌉니다) → CLAUDE.md 안건 2 |
| 자동 로그인·응답 스트리밍 | refresh token 없음 / 위기 판정 전 글자를 회수할 수 없음 |
| **초 단위 수집** | **분석 단위가 하루**라 같은 값이 나옵니다. Android 주기 작업 최소가 15분이고, 더 자주 하려면 상주 알림이 필요합니다 → [작업이력 55차](진행/작업이력.md) |
| **`documents/` 원본 산출물 개정** | **2026.08.25 — 요청 없으면 안 합니다.** `최종발표자료_귀기울임.pptx` 만 예외(§3-0 이 손댈 대상). 문서↔구현 어긋남을 발견해도 **개정 제안조차 하지 않습니다** — 검사기(`check_docs.py`)가 잡아내는 건 그대로 보고하되, 산출물 5종(기획서·요구사항정의서·빅데이터분석정의서·데이터베이스요구사항분석서·테이블명세서)·화면설계서는 손대지 않습니다 |

### 알아둘 것

- **`db/schema.sql` 이 정본입니다.** 데이터베이스요구사항분석서·테이블명세서와 완전히 일치하는 상태이니, 스키마 관련 서술은 이 파일을 기준으로 확인하세요.
- **문서 5종 + 발표자료 2종이 서로 물려 있습니다.** 한 곳을 고치면 다른 곳도 같이 고쳐야 하는 경우가 많으니, 수정 전에 체크리스트의 「연쇄 영향」·「수정 대상」을 확인하세요.
- **`[확인]` 표시 항목은 추정으로 채우지 마세요.** 법령 조문, 데이터셋 접근성 등이 포함됩니다.

### ⚠ 붙여넣기 사고가 세 번 났습니다

체크리스트 문안을 HWP 에 붙여넣을 때마다 확인하세요. 상세는 체크리스트 「표기 규칙」.

1. **마크다운 기호** — `**굵게**` 별표, `` `코드` `` 백틱이 딸려 들어감
2. **스마트 따옴표** — 한글이 `'FRIEND'` → `'FRIEND’` 로 바꿔 **SQL 이 실행되지 않음**
3. **고치다 한 글자 더 지움** — 고친 줄은 앞뒤를 함께 다시 읽으세요

### 문서 대조 방법

문서를 고친 뒤 **PDF 로 내보내고** 아래를 돌리면 `docs/extracted/` 가 갱신되어 diff 로 변경분을 볼 수 있습니다.

```powershell
python tools/doc2txt.py
```

> ⚠ **`python` 이 PATH 에 없거나 Microsoft Store 스텁일 수 있습니다.**
> 저장소 venv 를 쓰세요 — `.\.venv\Scripts\python.exe tools/doc2txt.py`.
> 없으면 [`SETUP.md`](SETUP.md) 로 만드세요.
>
> ⚠ **pandas 가 필요한 스크립트는 아나콘다 쪽으로** 돌리세요. 새로 설치한
> Python 은 애플리케이션 제어 정책이 numpy DLL 을 차단하는 PC 가 있습니다.

---

## 3-2. 클라우드 배포 (2026.08.24)

> "왜 이 구성인가"는 [`docs/결정/배포_아키텍처.md`](결정/배포_아키텍처.md),
> "어떻게 하는가"는 [`infra/README.md`](../infra/README.md) 에 있습니다.
> 여기는 **지금 어디까지 됐고 다음이 뭔지**만 담습니다.

### 지금까지 한 것

NCP(네이버클라우드플랫폼) VM 1대 + Docker Compose 로 첫 배포를 끝냈습니다.

- VPC(`lisn-vpc`) · Subnet(`lisn-public-subnet`) · ACG(`lisn-vpc-default-acg`,
  22 는 그때 작업하던 PC IP 만·80/443 전체) · Server(`lisn-server`,
  Ubuntu 24.04 · s2-g3 · 시간요금제) 생성 완료
- Docker 설치 → 저장소 clone → `docker compose up -d --build` → **4개
  컨테이너(postgres·backend·ai-server·nginx) 전부 정상 기동**
- `http://<공인IP>/health`·`/docs`·`/`(관리자 웹) **외부 접속 확인 완료**
- 배포 중 코드 버그 1건 발견·수정·push 끝남 — `ai/server/main.py` 가
  `Path(__file__).parents[2]` 로 `backend/.env` 를 찾는데, 컨테이너 안에서는
  파일이 `/app/main.py` 하나뿐이라 `IndexError` 로 죽었습니다
  ([f80f0db](https://github.com/2026-SMHRD-KDT-HealthCare-5/lisn/commit/f80f0db)).
  **다시 배포해도 이 문제는 안 나옵니다.**

### ✅ 서버는 다시 켜져 있습니다 (2026.08.24 저녁)

공인 IP `101.79.24.15` — **재개해서 실제로 확인했습니다.**
`restart: unless-stopped` 대로 4개 컨테이너(postgres·backend·ai-server·nginx)
가 dockerd 기동과 함께 전부 자동으로 따라 떴습니다(`docker compose ps` 로
확인, postgres `healthy`). §3-2 윗줄이 "확인한 적 없다"고 적어 둔 걱정은
해소됐습니다. IP 는 안 바뀌었습니다.

**요금이 계속 나가고 있습니다.** 더 안 쓸 거면 콘솔에서 다시 정지하세요.

### ✅ ACG 22번 포트 — 8/24 그 PC IP 하나로만 열려 있던 문제

이 문서 위쪽 "지금까지 한 것"이 "22 는 그때 작업하던 PC IP 만"이라고 적어
둔 그대로였습니다. **다른 네트워크에서 SSH 하면 비밀번호가 맞아도
`Connection timed out` 으로 막힙니다** — 증상이 "비밀번호가 안 맞나?"
처럼 보여서 헷갈리기 쉽습니다. ACG 인바운드 22번 규칙에 접근소스를
추가해 풀었습니다. **필요하면 접근소스에 IP 를 더 추가하세요** — NCP
콘솔 → Server → ACG → `lisn-vpc-default-acg`.

### SSH 키 — 새 키 하나가 이미 등록돼 있습니다

`claude-code@lisn-deploy-20260824` 라는 이름의 키가 `authorized_keys` 에
추가돼 있습니다(개인키는 이 키를 만든 세션의 스크래치패드에만 있고
저장소에는 없습니다). **다른 환경에서 이어가려면 이 키가 없으니** 아래
원래 절차를 따르세요:

1. NCP 콘솔 → Server → `lisn-server` 선택 → 서버 관리 및 설정 변경 →
   **관리자 비밀번호 확인** → 인증키 파일(`.pem`) 선택 → 비밀번호 확인
   - **원본 pem 이 그 PC 에도 없으면** 인증키 자체를 새로 만들어야
     합니다(서버 관리 및 설정 변경 → 인증키 변경). 기존 pem 은 못 씁니다.
2. 그 비밀번호로 `ssh root@<공인IP>` 접속 (비밀번호 인증)
   - ⚠ **NCP 콘솔의 "콘솔 접속"(웹 화면)이 아니라 본인 PC 터미널을
     쓰세요.** 콘솔 접속의 가상 키보드가 특수문자(`!` `-` `%` 등)를
     다르게 보내 `login incorrect` 가 나는 걸 8/24 에 겪었습니다.
3. 새 환경의 SSH 공개키를 `~/.ssh/authorized_keys` 에 추가해서 키 인증으로
   전환 — `infra/README.md` 「인증키 설정」절 참고
4. **비밀번호는 절대 대화창(Claude 등)에 붙여넣지 마세요.** 서버 관리자
   권한 그 자체입니다. (2026.08.24 세션에서 실제로 두 번 그렇게 다뤄질
   뻔했습니다 — 그때 콘솔에서 확인한 비밀번호는 유출된 것으로 간주하고
   있습니다. **아직 관리자 비밀번호 초기화를 안 했다면 지금 하세요** —
   키 인증이 이미 되니 비밀번호를 몰라도 접속엔 지장 없습니다)

### 다음에 할 일

1. ~~Server 시작 → `docker compose ps` 로 4개 컨테이너 확인~~ **8/24 저녁
   완료**
2. ~~HTTPS 발급~~ **8/24 저녁 완료** — `https://101.79.24.15.nip.io`,
   Let's Encrypt(만료 2026-11-22), 자동 갱신 크론(매주 월 03:00)까지 등록.
   `infra/nginx/lisn.conf` 를 SSL 버전으로 서버에서 직접 교체했습니다 —
   **저장소의 `lisn.conf` 는 그대로 HTTP 부팅용입니다**(`infra/README.md`
   가 원래 이렇게 설계돼 있습니다. 서버 재배포 시 다시 SSL 절차를 밟으세요)
3. ~~`backend/firebase-service-account.json` 을 더미로만 올려뒀던 것~~
   **8/24 밤 완료** — 실제 서비스 계정 키(`lisn-952c8`,
   `firebase-adminsdk-fbsvc@...`)를 받아 `scp` 로 올리고
   `chmod 600` · `docker compose restart backend` 까지 했습니다.
   컨테이너 안에서 `firebase_admin.initialize_app()` 이 실제로
   성공하는 것까지 확인했습니다(`project_id: lisn-952c8`).
   ⚠ **이건 자격증명 파싱 성공까지입니다 — 실기기로 알림을 실제로
   받아보는 e2e 발송 테스트는 아직 안 했습니다.** 8/21 로컬 실측 때는
   에뮬레이터로 수신까지 확인했지만, 이 클라우드 인스턴스에서 실제
   발송을 트리거해 본 적은 없습니다(대상자에게 선제 접촉 조건이
   맞아떨어져야 자연 발생하거나, `tools/demo_notify.py` 로 재발송).
4. ~~Flutter 앱 release APK 재빌드~~ **8/24 밤 완료** —
   `--dart-define=API_BASE_URL=https://101.79.24.15.nip.io` 로 빌드,
   컴파일된 `libapp.so` 안에 그 URL 문자열이 실제로 박힌 것까지 확인.
   `flutter pub get --enforce-lockfile` 로 lockfile 무결성도 확인했습니다.
   산출물: `frontend/app/build/app/outputs/flutter-apk/app-release.apk`
   (저장소에는 안 넣습니다 — 빌드 산출물)
5. ✅ 관리자 웹으로 실제 로그인·DB 연동까지 확인 — **클라우드 인스턴스에서도
   2026.08.24 저녁에 확인 끝났습니다.**
   - ⚠ **가는 길에 진짜 버그 하나를 발견·수정했습니다.** 관리자 웹 빌드가
     `VITE_API_BASE_URL=http://101.79.24.15`(HTTP)로 굳어 있었는데, 페이지는
     HTTPS 로 뜨니 브라우저가 **혼합 콘텐츠로 로그인 요청 자체를 조용히
     차단**했습니다("서버에 연결할 수 없습니다"만 뜨고 네트워크 요청은
     0건). `infra/README.md` 4절·트러블슈팅 표가 정확히 이 함정을
     경고하고 있었는데, HTTPS 전환 전에 HTTP 주소로 빌드해 올린 뒤 그대로
     남아 있었던 것으로 보입니다. `VITE_API_BASE_URL=https://101.79.24.15.nip.io/api/v1`
     로 로컬에서 다시 빌드해 `scp` 로 올리고 nginx 를 재시작해 해소했습니다.
   - ✅ **로컬 dev 환경에서도 2026.08.24 에 확인 끝났습니다** — 로그인 →
     대시보드·대상자 조회·위기 사건 이력·로그아웃, `ADMIN` 아닌 계정 차단까지
     전부 정상. 코드 경로 자체는 검증됐으니 클라우드에서 막히면 배포 설정
     쪽일 가능성이 큽니다. 자세한 내용은
     [`진행/작업이력.md` 60차](진행/작업이력.md#20260824-60차--pptx-병합-충돌-해소--관리자-웹-로그인-로컬-실측)

---

## 3-1. 지난 세션 기록은 작업이력에 있습니다

7/30~8/6 의 세션별 기록(무엇을 왜 그렇게 정했는지)은
[`진행/작업이력.md`](진행/작업이력.md) 에 날짜순으로 있습니다. **54차까지**
있고 맨 위가 최신입니다.

전에는 이 문서가 세션 기록까지 안고 있었습니다. 그러면 **「지금 무엇을
해야 하는가」가 지난 기록에 묻힙니다** — 이 문서는 §0 이 정한 대로
**시작 방법과 현재 상태**만 담습니다.

| 찾는 것 | 어디 |
|---|---|
| 왜 이렇게 정했나 | [`작업이력.md`](진행/작업이력.md) |
| 되돌리면 안 되는 것 · 실패 사례 | [`학습자료.md`](학습자료.md) |
| 문서가 요구하는데 구현이 없는 것 | [`구현_갭.md`](진행/구현_갭.md) |
| LLM 으로 무엇을 시도하고 버렸나 | [`llm/USAGE_LOG.md`](llm/USAGE_LOG.md) |

---

## 4. 저장소 밖 리소스

- [Notion 프로젝트 페이지](https://app.notion.com/p/3ab02025254781d18e1ac402a0f59d77) — 자료실, 팀원 4명 게스트(보기 전용) 공유
- [Google Drive 공유 폴더](https://drive.google.com/drive/folders/1myjO0y6uNJW75gCbehvgbTvioo-Te2EW)
- **NCP 서버 SSH 인증키** — `C:\Users\smhrd1\.ssh\lisn-server-key.pem` (2026.08.24
  생성한 PC 에만 있음, git 에 없음·재발급 불가). 다른 환경에서 이어가는 방법은
  [§3-2](#32-클라우드-배포-20260824) 참고

> Notion 은 무료 플랜 1,000블록 제한을 피하려고 팀원을 **게스트**로 초대한 구조입니다. 멤버로 승격하면 제한이 걸립니다.

---

## 5. 이 문서들을 유지하는 방법

대화에서 결정된 내용은 **반드시 저장소에 남기세요.** 채팅 기록은 기기와 세션을 넘어가지 않지만 저장소는 따라갑니다.

- 결정이 나면 → [`작업이력.md`](진행/작업이력.md) 맨 위에 기록하고, **해당 항목을 안건·체크리스트에서 삭제**
- 문서 개정을 반영하면 → 체크리스트에서 해당 항목 삭제
- 프로젝트 상태가 바뀌면 → 이 문서 3절
- 작업 규칙이나 새로 발견한 함정 → `CLAUDE.md`
