# 산출물 문서 개정 체크리스트

> **최종 갱신** 2026.07.30 · **작성자** 이응균(PM) · **목표 완료일** 2026.08.11
> **대상** 01~05 HWP 5종 + 발표자료·화면설계서 PPTX 2종
>
> **이 문서에는 아직 반영되지 않은 항목만 남깁니다.**
> 완료된 결정과 그 근거 → [`작업이력.md`](작업이력.md) · 미결 안건 → [`회의안건_20260729.md`](회의안건_20260729.md)

---

## 1. 진행 현황

| 문서 | 남은 항목 | 담당 |
|---|---|---|
| 01 프로젝트 기획서 | 1 — `01-P` (차별성 절 재작성 + 수치) | 이응균 |
| 02~04 HWP | **0 — 전부 완료** ✅ | — |
| 05 테이블명세서 | 2 — `05-Q`(테이블설명 3곳) → `05-N③`(작성일자) | 이응균 |
| 발표자료 PPTX | 4 — `PT-C` `PT-D` `PT-G` `PT-E` (**보류**) | 이응균 |
| 화면설계서 PPTX | 1 — `SD-A` (**보류** — 별도 작업 중) | 함은선 |

**총 8건이고, 보류 5건을 빼면 지금 할 것은 3건입니다.** 🔒 잠금 없음. 단 **`05-N③`(작성일자)은 항상 마지막**입니다.

> **`05-P` 복원 완료.** FK 참조 7곳·CHECK 값 목록 6곳·단위 4곳이 전부 일관되게 들어갔습니다.
> **05 의 컬럼·타입·제약·비고는 확정**이고 남은 건 테이블설명 3곳과 날짜뿐입니다.

> ⚠ **`01-P` 가 이번 감사에서 가장 큽니다.** 「차별성」의 첫 항목이 「유사 서비스 분석」과 **같은 주장을 다시 하고 있습니다.** 절은 2장을 닫는 종합이므로 살리되, **반복을 참조로 바꾸고 앞 절에 없는 내용으로 채우는** 재작성입니다.

> **2026.07.30 완료** — `01-G`~`01-O` `03-F` `04-E`~`04-I` `05-F`~`05-M` `05-N①②` `05-O` `05-P` · `결정-1`(연락처 선택 입력)
> 근거는 [`작업이력.md`](작업이력.md) 로 옮겼습니다.

> ### 반복되는 감사 패턴
> **같은 종류가 계속 나옵니다.** 새 항목을 볼 때 아래 세 가지를 먼저 의심하세요.
> 1. **같은 주장이 여러 절에 반복** — `01-N`(방법론↔3장) · `01-O④`(기대효과↔활용방안) · `01-P①`(유사서비스↔차별성)
>    - 다만 **절을 지우는 게 답이 아닌 경우가 있습니다.** 「차별성」처럼 장을 닫는 종합 절은 위치상 필요하므로, **반복된 문장을 앞 절 참조로 바꾸고 빈 자리를 새 내용으로 채웁니다.**
> 2. **다른 칸이 이미 말하는 것을 비고에 재진술** — 단위 17칸 · `PK UUID` 8곳 · `gen_random_uuid()` 8곳 · 테이블설명의 컬럼 나열
> 3. **출처와 함께 제시한 수치를 요약 표에서 다시 인용** — `01-P②`(STP Targeting 의 18.3%/19.59%)

> **2026.07.30 전수 검증** — 완료 표시 항목이 전부 실제로 반영돼 있음을 `docs/extracted/` 원문과 대조해 확인했습니다.
> `PK UUID` **0건** · 스마트 따옴표(U+2019) **0건** · `raw_payload` 04·05 **0건** · `access_token` **0건** · `자동 진단` **0건** · `스크린 타임` **0건** · `위험 가구` **0건**
> `03-F①` JSON 샘플은 **컬럼 순서까지 `db/schema.sql` 과 일치**하고, `05-N①` 은 지울 17칸만 지우고 **남길 4개(`BPM`·`ms`·`m`·`kcal`)를 정확히 남겼습니다.**
>
> **01 참고문헌 9건은 전부 본문에서 출처명으로 인용되고 있어 미사용 문헌이 없습니다.** 번호 인용이 아닌 서술 인용 방식이며, 기획서에서는 일반적입니다. 손대지 마세요.
>
> ⚠ **grep 0건을 미반영으로 판정하지 마세요.** `01-G`·`01-H②` 가 처음 0건으로 나왔는데, 실제로는 반영돼 있고 **줄바꿈이 문구를 쪼갠 것**이었습니다(`앱에서 최소 15분` / `간격 전송`). 반드시 절 전체를 읽어 확인하세요.
>
> ⚠ **반대로 검색이 오탐을 낼 수도 있습니다.** 01 의 `혈압` 1건은 활용방안이 아니라 **시장 현황의 통계 인용**(한국미디어패널조사)이라 문제없습니다.

## 2. 표기 규칙

- **[교체]** 해당 문장/항목을 통째로 바꿈 · **[추가]** 기존 내용 뒤에 덧붙임 · **[확인]** 사실관계 확인 후 판단
- ⭐ 심사 방어에 직결 · ⭐⭐ 그중에서도 우선

> ### ⚠ 붙여넣을 때마다 확인하세요
> 실제로 다섯 번 다 사고가 났습니다.
> 1. **마크다운 기호** — `**굵게**` 의 별표, `` `코드` `` 의 백틱이 딸려 들어갑니다
> 2. **스마트 따옴표** — 한글이 `'FRIEND'` 를 `'FRIEND’` 로 바꿉니다. **SQL 이 실행되지 않습니다**
> 3. **고칠 때 한 글자 더 지우기** — 따옴표를 고치다 옆의 정상 따옴표까지 지워 `IN ('FRIEND,` 가 된 적이 있습니다
> 4. **앞뒤 줄까지 붙여넣기** — 이 문서의 코드 블록은 위치를 잡으려고 **앞뒤 줄을 함께 보여줍니다.** 그 줄들은 이미 문서에 있으니 빼고 붙여넣으세요
> 5. **글자 조각이 남기** — 비고를 고치다 `heart_rate` 의 조각인 `eart_rat` 가 남은 적이 있습니다(`05-F`). **고친 칸은 끝까지 읽으세요**

> ### ⚠ 컬럼을 지우면 언급된 곳을 전부 찾으세요
> `raw_payload` 를 지웠더니 **테이블 목록**과 **테이블설명**에 각각 남았습니다. 컬럼 하나가 문서 안 3~4곳에 등장합니다.
>
> **`Ctrl+F` 로 컬럼명을 검색해 0건이 될 때까지** 확인하세요. 대상은 ① 2쪽 테이블 목록 ② 테이블설명 ③ 컬럼 정의표 ④ CREATE 스크립트 ⑤ 04 객체 정의서입니다.

> ### ⚠ PPTX 저장 시
> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.5MB → 0.3MB). `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.

> ### 비고·설명 칸을 채우려고 채우지 마세요
> **칸이 있으면 채워야 한다는 압박이 실제로 문서를 나쁘게 만들었습니다.** 단위 17칸, `PK UUID` 8곳, `gen_random_uuid()` 8곳이 모두 그 결과입니다.
>
> **적을 것** — 그 칸 말고는 알 수 없는 것. 값 범위, enum 값, 참조 대상, 구현 방법, 설계 근거.
> **적지 말 것** — 컬럼명·타입·제약 칸이 이미 말하는 것, 04 에 전체 원칙으로 선언된 것, 다른 칸에 있는 서술의 재진술.
>
> **비워두는 게 정상입니다.** 비고(remarks)는 필요할 때만 쓰는 칸이고, 빈 칸이 많을수록 채워진 칸이 눈에 띕니다.
>
> ⚠ **다만 지울 때 남길 것까지 함께 지우지 마세요.** 실제로 `05-O` 에서 FK 참조 대상 6곳과 CHECK 값 목록 3곳이 함께 지워졌습니다. **`FK`·`CK` 칸의 `Y` 는 "제약이 있다"만 말하고 "어디를 참조하는지 / 어떤 값이 허용되는지"는 말하지 못합니다.** → `05-P`

> ### ⚠ 이 문서의 축약 표기를 그대로 옮기지 마세요
> 표 안에서 자리를 아끼려고 `단위 + DEFAULT 0` 처럼 줄여 쓴 것이 문서에 그대로 들어간 적이 있습니다(`05-P③`). 실제로 들어갈 값은 `단위: m, DEFAULT 0` 입니다.
>
> **작성 측 규칙** — 표의 「교체」 칸에는 **문서에 들어갈 문자열을 그대로** 적는다. 설명이 필요하면 표 밖에 쓴다.

---

# 01. 프로젝트 기획서

## 01-P. 10쪽 「차별성」 절 재작성 + Targeting 수치 정리 **[교체]** ⭐⭐ *(2026.07.30 신규)*

### ① 「차별성」 절 — 종합 성격으로 다시 쓰기 **[교체]**

> **절은 살립니다.** 2장이 `시장 현황 → 유사 서비스 분석 → SWOT → STP → 차별성` 으로 흐르는데, 「차별성」을 빼면 장이 STP 표에서 끝나버립니다. **장을 닫는 종합이 필요합니다.**
> 문제는 존재가 아니라 **종합이 아니라 반복이라는 점**입니다.

**현재 첫 항목이 앞 절과 같은 말입니다.**

| | 내용 |
|---|---|
| 「유사 서비스 분석」 두 번째 항목 | 비교 대상은 능동적 입력 기반이지만 **본 서비스는 라이프로그로 정서 상태를 자동 분석** |
| 「차별성」 첫 항목 | 스마트워치 생체신호(수면·심박·HRV)로 **감정을 자동 분류**, 자기보고식 경쟁사와 근본적 차별화 |

**교체** — 앞에서 증명한 것은 **결론만 참조**하고, 앞 절에 없는 내용을 채웁니다. 3개 항목으로 바꾸세요.

| 차별점 | 세부 내용 |
|---|---|
| 수집·분석·대응 통합 | 앞의 비교에서 확인한 바와 같이, 라이프로그 자동 수집·AI 정서 분석·위험도 자동 분류를 하나의 서비스에서 연결한 사례는 비교 대상 중 없다. 본 서비스는 세 단계를 단일 파이프라인으로 연결한다. |
| 선택형 페르소나와 멀티모달 대화 | 사용자가 조언형(T)·공감형(F) 중 대화 상대의 성격을 직접 선택하고, 텍스트와 음성(STT/TTS) 양쪽으로 대화한다. 감정 기록을 부담스러워하는 사용자가 대화 형태로 접근할 수 있게 한다. |
| 위험 단계별 대응 분기 | 주의 단계에서는 음악·음식·운동·아티클을 추천하고, 심각 단계에서는 콘텐츠 추천을 중단하고 109 긴급 상담 연결로 전환한다. 위기 안내에 그치지 않고 단계별로 다른 액션을 수행한다. |

**바뀐 점**

- **첫 항목** — `근본적 차별화` 같은 재주장을 빼고 **"앞의 비교에서 확인한 바와 같이"** 로 앞 절을 참조합니다. 종합 절의 어법입니다
- **두 번째** — `MBTI형 T/F` · `음성 대화(STT/TTS)` 는 **「유사 서비스 분석」에 없는 정보**입니다. 살려서 독립 항목으로
- **세 번째** — 신규. 유사 서비스 분석은 Wysa 를 `△`(위기 발화 감지·안전 안내)로 표기했는데, **단계별 대응 분기는 그 표에서 드러나지 않습니다.** 여기서 채우면 표의 `△` 가 왜 `△` 인지 설명됩니다

> **`STP Positioning` 은 그대로 두세요.** 한 줄 선언과 종합 절은 층위가 다릅니다.

### ② `STP 전략` → Targeting 의 수치 삭제

**현재**
> 국내 정신건강 수요 확대 및 글로벌 정신건강 앱·웨어러블 시장 고성장(18.3%, 19.59%)+자사 라이프로그 역량 부합 → '스마트워치 사용 1인 가구'에 집중하는 니치 전략

**교체**
> 국내 정신건강 수요 확대 및 글로벌 정신건강 앱·웨어러블 시장 고성장 + 자사 라이프로그 역량 부합 → '스마트워치 사용 1인 가구'에 집중하는 니치 전략

`18.3%` · `19.59%` 는 「시장 현황」에서 **출처와 함께 이미 제시**했습니다. STP 표에서 괄호로 다시 쓰면 **근거 없이 숫자만 반복하는 꼴**입니다.

> `Segmentation` ↔ `Positioning` 의 `상담에 부담` / `상담 장벽 낮춤` 은 **문제·해법 쌍이라 STP 구조상 자연스럽습니다. 놔두세요.**

---

# 05. 테이블 명세서

> **삭제할 컬럼은 없습니다.** `raw_payload` 제거 후 남은 75개 컬럼 전부에 근거가 있으며, 더 줄이면 기업 제공 데이터를 받지 못하는 구조가 됩니다.

> **`05-N①②`(단위 17칸·`PK UUID` 8곳)와 `05-O`(비고 21곳)는 완료**되었습니다.

## 05-P. 과하게 지워진 비고 복원 — 10곳 **[추가]** ⭐ *(2026.07.30 신규)*

> **`05-O` 는 완료**되었습니다. `gen_random_uuid()` 8곳 · 컬럼명이 말하는 것 5곳 · 1쪽 목록 비고 8칸이 정리됐습니다.
> **다만 남겨야 할 것까지 함께 지워졌습니다.**

### ① FK 참조 대상 — 6곳 복원 🔥

**FK 칸의 `Y` 만으로는 어디를 가리키는지 알 수 없습니다.** 8곳 중 1곳만 남아 기준도 어긋난 상태입니다.

| 테이블 | 컬럼 | 복원할 비고 |
|---|---|---|
| `DEVICE_HEALTH_CONNECTIONS` | `user_id` | `USERS.user_id 참조 (CASCADE)` |
| `LIFELOG_METRICS` | `user_id` | `USERS.user_id 참조 (CASCADE), 복합 UNIQUE` |
| `CHAT_SESSIONS` | `user_id` | `USERS.user_id 참조 (CASCADE)` |
| `EMOTION_RISK_SCORES` | `user_id` | `USERS.user_id 참조 (CASCADE)` |
| `EMOTION_RISK_SCORES` | `emotion_id` | `EMOTIONS.emotion_id 참조` |
| `HEALING_CONTENTS` | `emotion_id` | `EMOTIONS.emotion_id 참조` |
| `BODY_COMPOSITION_METRICS` | `user_id` | ✅ **이미 있음** — 이것만 남았습니다 |

`LIFELOG_METRICS.user_id` 의 **`복합 UNIQUE`** 는 `05-J` 로 넣은 것입니다. UK 칸 표시와 짝이므로 함께 복원하세요.

### ② CHECK 제약의 값 목록 — 3곳 복원 🔥

**CK 칸이 `Y` 인데 어떤 값만 허용되는지 문서에서 알 수 없습니다.** CREATE 스크립트를 읽어야 알게 됩니다.

| 테이블 | 컬럼 | 복원할 비고 |
|---|---|---|
| `USERS` | `gender` | `MALE, FEMALE, OTHER` |
| `USERS` | `persona_type` | `FRIEND, COUNSELOR` |
| `EMOTION_RISK_SCORES` | `risk_level` | `NORMAL, CAUTION, CRITICAL` |

**같은 성격인데 어떤 건 남고 어떤 건 지워져 기준이 어긋났습니다.**

```
EMOTIONS.category           → NORMAL, CAUTION, CRITICAL   ✅ 남음
EMOTION_RISK_SCORES.risk_level → (빈칸)                   ❌ 같은 값인데 지워짐
CHAT_SESSIONS.persona_type  → FRIEND, COUNSELOR           ✅ 남음
USERS.persona_type          → (빈칸)                      ❌ 같은 값인데 지워짐
HEALING_CONTENTS.category   → MUSIC, FOOD, EXERCISE, ...  ✅ 남음
USERS.gender                → (빈칸)                      ❌ 지워짐
```

> **기준이 일관되지 않은 게 값 목록이 없는 것보다 눈에 띕니다.** enum 값은 CK 칸으로 대체할 수 없는 정보이므로 **전부 남기는 쪽**이 맞습니다.

### ③ `단위 +` 표기 — 4곳 정정

제가 체크리스트 표에 **축약해 쓴 표기가 그대로 옮겨졌습니다.** 실제 값으로 바꾸세요.

| 컬럼 | 현재 (잘못) | 교체 |
|---|---|---|
| `distance` | `단위 + DEFAULT 0` | `단위: m, DEFAULT 0` |
| `calories` | `단위 + DEFAULT 0` | `단위: kcal, DEFAULT 0` |
| `heart_rate` | `단위 + 미측정 구간은 NULL` | `단위: BPM, 미측정 구간은 NULL` |
| `hrv` | `단위 + 미측정 구간은 NULL` | `단위: ms, 미측정 구간은 NULL` |

> **제 잘못입니다.** 체크리스트 표에서 `단위 + ...` 로 줄여 쓴 것을 그대로 옮기신 것입니다. 앞으로 표에는 **문서에 들어갈 값을 그대로** 적겠습니다.

---

### 남은 비고 현황 — 참고

정리 후 79개 컬럼 중 **비고가 있는 것은 16개**입니다. (`05-P` 복원 후 26개)

| 성격 | 컬럼 |
|---|---|
| 값 범위 | `emotion_score` `risk_score` (0~100) |
| enum 값 | `emotion_code` `category`×2 `persona_type`(CHAT_SESSIONS) |
| 구현 방법 | `device_name` `platform_type` |
| 설계 근거 | `collected_at` `last_synced_at` `heart_rate` `hrv` `sensitive_agreed_at` |
| 결정 근거 | `phone` |
| 용도 구분 | `session_summary` `description` |
| 단위 | `distance` `calories` |

**63개가 빈칸입니다.** 이게 정상입니다.

## 05-Q. 테이블설명 — 컬럼 나열 제거 **[교체]** *(2026.07.30 신규)*

**바로 아래에 컬럼 정의표가 있는데 설명에서 컬럼을 다시 늘어놓고 있습니다.** 3곳입니다.

| 테이블 | 현재 | 교체 |
|---|---|---|
| `USERS` | 회원 계정 프로필, 비밀번호 해시, 선호 챗봇 성격 및 푸시 토큰 관리 | 회원 계정 및 프로필 관리 |
| `LIFELOG_METRICS` | 생체 시계열 데이터(활동·수면·심박수·HRV) 시계열 적재 | 활동·수면·심박 생체 시계열 적재 |
| `BODY_COMPOSITION_METRICS` | 체성분계/웨어러블 연동을 통한 사용자 신체 구성(체중, 체지방, 근육량 등) 측정 이력 관리 | 체성분계 연동 신체 구성 측정 이력 |

`USERS` 의 `비밀번호 해시` `선호 챗봇 성격` `푸시 토큰` 은 각각 `password_hash` `persona_type` `fcm_token` 이고, `BODY_COMPOSITION_METRICS` 의 `체중, 체지방, 근육량` 도 컬럼명 그대로입니다.

> ### ⚠ `LIFELOG_METRICS` 는 제 문안이 잘못됐습니다
> `05-L①` 로 드린 문안에 **"시계열"이 두 번** 들어가 있었습니다(`생체 시계열 데이터(…) 시계열 적재`). 제 실수입니다.

> 나머지 5개 테이블설명은 컬럼 나열 없이 **테이블의 역할만** 서술하고 있어 그대로 두세요.
> `EMOTIONS`(시스템 공통 감정 코드 및 분류 기준 정의) · `DEVICE_HEALTH_CONNECTIONS` · `CHAT_SESSIONS` · `EMOTION_RISK_SCORES` · `HEALING_CONTENTS`

## 05-N③. 작성일자 갱신 — 8개 테이블 **[교체]** 🔚 마지막에

전부 `2026.07.29` 로 되어 있는데 **7/30 에 개정했습니다.** 16곳(본문 8 + 표 8)입니다.

> **`05-P` 를 마친 뒤 마지막에 한 번에** 갱신하세요. 개정이 계속되면 또 어긋납니다.
> `05-D` → `05-N③` 로 **두 번 지적했다가 같은 이유로 다시 남은** 항목입니다. **이번이 마지막 개정이 될 때** 손대세요.

---

# 발표자료 PPTX

> **아래 4건은 슬라이드 순서대로 배열했습니다. 파일을 한 번만 열면 됩니다.**
> `PT-A` `PT-B` `PT-F①②` 와 slide8 스택 표기는 **반영 완료**되었습니다.

## PT-C. slide5 SWOT — ST 전략 **[교체]** ⭐

`01-B` 로 **기획서는 고쳤는데 발표자료가 안 따라왔습니다.** 발표에서 그대로 노출되는 문구입니다.

**현재**
> ST (강점+위협) **기기 내 마스킹과 암호화 설계로** 규제 강화 및 감시 우려 선제 대응

**교체**
> ST (강점+위협) 위험도 기반 계층적 데이터 보호 설계로 규제 강화 및 감시 우려 선제 대응

> 실제 구현은 앱이 `user_id` 를 포함해 전송하고 PII 마스킹도 **백엔드 저장 시점**에 수행합니다. "기기 내"는 사실과 다릅니다.
>
> ⚠ **기획서 SWOT 은 `01-H` 로 갱신을 마쳤습니다.** slide5 의 WO·WT 에도 기획서와 같은 문제(IRB 승인 지연·정부 R&D 지원사업)가 남아 있는지 **함께 확인**하세요. 있으면 아래 문안으로 맞추면 됩니다.
> - WO → `무료 공개 데이터셋과 교내 공용 PostgreSQL 로 데이터·인프라 비용 절감`
> - WT → `승인 절차 없는 공개 데이터셋으로 학습 범위 한정, 공개 데이터 기반 검증`

## PT-D. slide6 · slide8 — 배치 스케줄러 · Node.js 위치 **[교체]**

**slide8 의 스택 표기(`앱 클라이언트(Flutter) · 관리자 웹(React)`)는 이미 반영됐습니다.** 남은 것은 두 가지입니다.

### ① slide8 시스템 아키텍처

| 현재 | 문제 | 교체 |
|---|---|---|
| `15분 주기 배치 스케줄러 (UPSERT 적재)` | `02-H` push 전환 미반영. 서버가 당겨오는 구조로 읽힘 | `앱 push 수신 → UPSERT 적재 (최소 15분 간격)` |
| `메인 백엔드 (Business Server) FastAPI/Node.js` | **Node.js 가 백엔드로 표기됨.** Node 는 React 관리자 웹 빌드용 | `메인 백엔드 FastAPI` · 관리자 웹은 별도 블록으로 분리 |

> **01·02·05 는 모두 앱 push 구조로 개정을 마쳤습니다. 발표자료만 남았습니다.** 지금은 **"그래서 누가 데이터를 가져오나요"** 에 문서와 발표가 다르게 답합니다.

### ② slide6 「핵심 기능」

**현재**
> 위험도 단계별 대응 **(FastAPI, Node.js)**

**교체**
> 위험도 단계별 대응 (FastAPI)

## PT-G. slide12 「학습 데이터셋 확보 계획」 — 오타 **[교체]**

`PT-F②` 로 데이터셋 블록을 교체하는 과정에서 **글자가 하나 끼어들었습니다.**

**현재** — GLOBEM 블록
> Source: **손** 14일 집계 수면·걸음 피처 + BDI-II 우울 척도 점수·우울 라벨

**교체**
> Source: 14일 집계 수면·걸음 피처 + BDI-II 우울 척도 점수·우울 라벨

> `손` 한 글자만 지우면 됩니다. 나머지 문안은 정확합니다.

## PT-E. 팀원 소개 슬라이드 — Apple Health 연동 **[교체]** ⭐

**현재**
> BACKEND / DB 윤일준 — API 서버, PostgreSQL 설계, Health Connect · **Apple Health** 연동

**안건 2에서 iOS/Apple Health 구현을 제외하기로 확정**했는데 담당 업무로 남아 있습니다. 02 요구사항정의서에 **"iOS 는 구현 대상에서 제외한다"** 를 이미 명시해두었으므로 정면으로 부딪힙니다.

**교체**
> BACKEND / DB 윤일준 — API 서버, PostgreSQL 설계, Health Connect 연동

---

# 화면설계서 PPTX

## SD-A. 변경이력 v0.5 + 연동 2건 **[확인]** — ⏸ 보류

> **별도 작업이 진행 중이라 보류합니다.** 해당 작업이 끝난 뒤 아래를 확인하세요.

### ① 변경이력 v0.5

- 작성일자가 `2026.07.2` / `8` 로 셀이 분리되어 깨져 있음 → `2026.07.28`
- **v0.5 행의 검수자 칸이 공란** → 다른 행과 맞춰 기입
- 문서 하단 「작성방법) …」 안내 블록은 **제출 전 삭제** (문서 자체에 "제출할 경우 작성방법 내용은 모두 지우고 제출해야 한다"고 명시되어 있음)

### ② `MAIN_JOIN_02` 에 연락처 선택 입력란 추가 — `결정-1` 후속

**연락처를 선택 입력으로 확정**했고 04 문서에는 반영했는데, **받는 화면이 없습니다.** 현재 `MAIN_JOIN_02` 는 이메일·비밀번호·이름·생년월일·성별만 받습니다.

`❷ 사용자 인적사항` 설명에 연락처를 추가하고 **선택 항목임을 명시**하세요.
> ❷ 사용자 인적사항: 이름, 생년월일, 성별 입력 (연락처는 선택 입력)

> **이 화면이 없으면 04 문서의 "선택 입력 항목인 연락처"가 근거를 잃습니다.** `phone` 컬럼을 유지하는 논리 전체가 여기에 걸려 있습니다.

### ③ `MAIN_JOIN_01` 민감정보 동의 항목 분리 표기 — `05-K` 연동

`MAIN_JOIN_01` 이 이미 **"서비스 이용약관 및 민감정보(생체/건강 데이터) 수집 동의 처리"** 로 되어 있고 필수(❷~❹)·선택(❺) 체크 구조를 갖추고 있습니다.

**남은 일은 그 필수 항목 중 민감정보 동의를 별도 항목으로 분리해 표기하는 것**입니다. 02 문서와 `db/schema.sql`(`sensitive_agreed`) 은 이미 반영을 마쳤습니다.
