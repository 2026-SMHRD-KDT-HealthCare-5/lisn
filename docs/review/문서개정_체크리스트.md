# 산출물 문서 개정 체크리스트

> **최종 갱신** 2026.07.30 · **작성자** 이응균(PM) · **목표 완료일** 2026.08.11
> **대상** 01~05 HWP 5종 + 발표자료·화면설계서 PPTX 2종
>
> **이 문서에는 아직 반영되지 않은 항목만 남깁니다.**
> 완료된 결정과 그 근거 → [`작업이력.md`](작업이력.md) · 미결 안건 → [`회의안건_20260729.md`](회의안건_20260729.md)

---

## 1. 진행 현황

| 문서 | 남은 항목 | 담당 |
|---|---|---|
| 01 프로젝트 기획서 | 1 — `01-N-1` (오타 1글자) | 이응균 |
| 02 요구사항정의서 | **0 — 전부 완료** ✅ | — |
| 03 빅데이터분석정의서 | 1 — `03-F` (3건) | 김건영 |
| 04 DB요구사항분석서 | **0 — 전부 완료** ✅ | — |
| 05 테이블명세서 | 1 — `05-M` (비고 1칸) | 이응균 |
| 발표자료 PPTX | 4 — `PT-C` `PT-D` `PT-G` `PT-E` | 이응균 |
| 화면설계서 PPTX | 1 — `SD-A` (**보류** — 별도 작업 중) | 함은선 |

**총 8건.** 🔒 잠금 없음.

> **`03-F①` 이 가장 급합니다.** `raw_payload` 를 04·05·`schema.sql` 에서 지웠는데 **03 의 JSON 샘플에만 남아 있고**, 활동 시각 3컬럼도 빠져 있습니다. 없는 컬럼이 샘플에 있으면 **"이 필드는 어느 테이블에 들어가나요"** 에 답할 수 없습니다.

> **05 는 삭제할 컬럼이 없습니다.** `raw_payload` 를 지운 뒤로 남은 75개 컬럼 전부에 근거가 있습니다. `05-M` 은 비고 문구 조정 1건뿐입니다.

> **2026.07.30 완료** — `01-G`~`01-N` `04-E`~`04-H` `05-F`~`05-L` · `결정-1`(연락처 선택 입력)
> 근거는 [`작업이력.md`](작업이력.md) 로 옮겼습니다.

### 착수 순서

| 순서 | 항목 | 문서 | 이유 |
|---|---|---|---|
| **1** | `03-F` (3건) | 03 | **①이 실제 누락.** 같은 절 안에서 세 건 처리 |
| 2 | `PT-C` `PT-D` `PT-G` `PT-E` | 발표자료 | 슬라이드 순서대로 |
| 3 | `01-N-1` `05-M` | 01·05 | 각각 한 칸. 다른 작업 때 곁들여도 됨 |
| — | `SD-A` | 화면설계서 | 함은선 님 작업 대기 |

## 2. 표기 규칙

- **[교체]** 해당 문장/항목을 통째로 바꿈 · **[추가]** 기존 내용 뒤에 덧붙임 · **[확인]** 사실관계 확인 후 판단
- ⭐ 심사 방어에 직결 · ⭐⭐ 그중에서도 우선

> ### ⚠ 붙여넣을 때마다 확인하세요
> 실제로 다섯 번 다 사고가 났습니다.
> 1. **마크다운 기호** — `**굵게**` 의 별표, `` `코드` `` 의 백틱이 딸려 들어갑니다
> 2. **스마트 따옴표** — 한글이 `'FRIEND'` 를 `'FRIEND’` 로 바꿉니다. **SQL 이 실행되지 않습니다**
> 3. **고칠 때 한 글자 더 지우기** — 따옴표를 고치다 옆의 정상 따옴표까지 지워 `IN ('FRIEND,` 가 된 적이 있습니다
> 4. **앞뒤 줄까지 붙여넣기** — 이 문서의 코드 블록은 위치를 잡으려고 **앞뒤 줄을 함께 보여줍니다.** 그 줄들은 이미 문서에 있으니 빼고 붙여넣으세요
> 5. **글자 조각이 남기** — 비고를 고치다 `heart_rate` 의 조각인 `eart_rat` 가 남은 적이 있습니다(`05-F`). **고친 칸은 끝까지 읽으세요**

> ### ⚠ 컬럼을 지우면 언급된 곳을 전부 찾으세요
> `raw_payload` 를 지웠더니 **테이블 목록**과 **테이블설명**에 각각 남았습니다. 컬럼 하나가 문서 안 3~4곳에 등장합니다.
>
> **`Ctrl+F` 로 컬럼명을 검색해 0건이 될 때까지** 확인하세요. 대상은 ① 2쪽 테이블 목록 ② 테이블설명 ③ 컬럼 정의표 ④ CREATE 스크립트 ⑤ 04 객체 정의서입니다.

> ### ⚠ PPTX 저장 시
> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.5MB → 0.3MB). `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.

---

# 01. 프로젝트 기획서

## 01-L. 15쪽 협업 툴 GitHub — 저장소 구조가 실제와 다름 **[교체]** ⭐⭐ 🔥 *(2026.07.30 신규)*

**위치** 15쪽 「□ 협업 툴 활용」 표 → **`GitHub`** 행의 첫 번째 항목

**현재**
> - 시스템 구조상 일반 비즈니스 로직 서버와 무거운 AI 추론 서버가 분리되어 있으므로, 메인 서버와 AI 분석 서버의 **레포지토리를 각각 독립적으로 설계하여 관리**

**실제 저장소는 하나입니다.**

```
lisn/          ← 저장소 1개 (모노레포)
├─ ai/         ├─ backend/    ├─ frontend/
├─ db/         ├─ docs/       ├─ tools/
```

**하지 않은 일을 했다고 적혀 있습니다.** 심사위원이 GitHub 링크를 열면 즉시 드러납니다.

**교체** — 첫 번째 항목을 아래로 바꾸세요. 두 번째 항목(Git-Flow)은 그대로 둡니다.
> - 메인 서버·AI 분석 서버·앱·관리자 웹을 하나의 저장소에서 디렉터리로 분리하는 모노레포 구조로 관리한다. 서버는 논리적으로 분리되어 있으나 4인 팀 규모에서는 저장소를 나누면 스키마·API 규격 동기화 비용이 오히려 커지므로, 단일 저장소에서 `backend/` · `ai/` · `frontend/` · `db/` 로 구분한다.

> **"왜 하나로 합쳤나"에 대한 답이 되는 프레이밍입니다.** 소수 인원에서 모노레포를 택한 것은 정당한 판단이고, 그 이유를 적으면 오히려 설계 근거가 됩니다.

## 01-M. 15쪽 데이터 확보 방안 — 2곳 **[교체]** ⭐ *(2026.07.30 신규)*

**같은 표 안 두 칸입니다. 함께 처리하세요.**

### ① 「데이터 출처」 칸 — 기업 체성분의 `회원번호` 삭제

**현재**
> - 체성분 - **회원번호,** 나이, 성별, 키, 체중, 체수분, 체지방, 근육량 및 표준범위 상·하한, 골격근량, 기초대사량

**`회원번호,` 만 지우세요.** 두 가지 이유입니다.

1. **우리 스키마에 대응 컬럼이 없습니다.** `BODY_COMPOSITION_METRICS` 는 자체 발급 `user_id`(UUID)로 연결하므로 기업 회원번호를 저장할 곳이 없습니다
2. **04 문서의 주장과 어긋납니다.** 04 는 *"모든 측정 데이터는 실명·연락처와 분리되어 UUID 기반 식별자로만 연결된다"* 고 하는데, 외부 회원번호를 받는다고 적으면 **"그 번호로 개인을 특정할 수 있지 않나"** 가 나옵니다

> 남겨야 한다면 `가명 식별자(회원번호)` 로 성격을 밝히고, **「데이터 수집 방법」 칸 끝에** 아래를 덧붙이세요.
> > 기업 회원번호는 적재 시 자체 발급 UUID 로 대체하며 원본 식별자는 보관하지 않는다.

### ② 「데이터 종류」 칸 — 체성분 누락

**현재**
> 심박수, 수면 단계, 걸음 수·활동량 등의 웨어러블 라이프로그 시계열과, 우울 척도(BDI-II) 기반 위험 상태 라벨 데이터

바로 위 「데이터 출처」의 요약인데 **기업 제공 체성분 11항목이 빠졌습니다.** `01-E` 로 기업 데이터를 핵심 자산으로 올려놓고 종류 요약에서는 사라진 상태입니다.

**교체**
> 웨어러블 라이프로그 시계열(수면 단계·활동량·걸음 수·심박수), 체성분 측정치(체중·체지방·근육량·골격근량·기초대사량), 우울 척도(BDI-II) 기반 위험 상태 라벨

## 01-N. 14쪽 「방법론」 — 3장 되풀이 **[교체]** *(2026.07.30 신규)*

**위치** 14쪽 「□ 사용 기술 및 개발 방법」 표 → **`방법론`** 행. 항목 3개를 통째로 교체합니다.

`사용 기술` 행은 **그대로 둡니다.**

**문제** — `방법론` 3개 항목이 3장 「개발 내용」과 같은 내용입니다.

| 현재 방법론 항목 | 3장의 어디와 겹치나 |
|---|---|
| 비수동형 라이프로그 수집·전처리, 수동 기록 번거로움 배제 | 11쪽 프로젝트 목표 1 |
| 챗봇 성격 선택, 시스템 프롬프트 동적 주입 | 12쪽 핵심 기능 「페르소나 챗봇」 |
| 감정 스코어 → [감정 → 위험도 → 시스템 액션] 대응 로직 | 12쪽 핵심 기능 「위험도 단계별 대응」 |

**"무엇을 만드나"(3장)와 "어떻게 만드나"(4장)가 같은 말을 합니다.**

**교체** — 3개 항목을 아래 3개로 바꾸세요.
> - 3장에서 정의한 기능을 **데이터 수집 → 전처리 → 2단계 모델 추론 → 대응 액션** 순으로 구현한다. 각 단계를 독립 모듈로 설계해 앞 단계 실패가 뒤 단계를 막지 않도록 한다.
> - 라이프로그는 앱이 Health Connect 에서 조회해 백엔드로 전송하고, 백엔드가 이상치를 제거한 뒤 AI 서버가 개인별 최근 14일 기준으로 정규화하여 추론한다.
> - 위기 탐지는 공감 응답 생성과 **독립 호출**로 분리하여, 응답 생성이 실패하거나 지연되어도 안전 기능이 함께 실패하지 않도록 한다.

> **세 번째 항목이 새로 들어가는 내용입니다.** 안건 2-C(위기탐지·응답생성 병렬 호출) 결정이 01 에 반영돼 있지 않았습니다. **"왜 OpenAI 를 2번 호출하나"** 에 대한 답이 여기 있으면 비용 질문이 안전 설계 질문으로 바뀝니다.

---

# 03. 빅데이터분석정의서

## 03-F. 라이프로그 블록 「1. 데이터 준비」 — 3건 **[교체]** ⭐⭐ *(2026.07.30 재작성)*

**세 건 모두 같은 절 안에 있습니다. 한 번에 처리하세요.**

> **⚠ ①이 실제 누락입니다.** `05-E`(활동 시각 추가)·`05-G`(raw_payload 삭제)를 04·05·`schema.sql` 에 반영했는데 **03 의 JSON 샘플이 안 따라왔습니다.** ②③은 방어 논리 보강입니다.

### ① 「수집 데이터」 칸 JSON 샘플 — `raw_payload` 잔존 + 활동 시각 누락 🔥

**위치** PDF **3~4쪽** 「1. 데이터 준비」 → **`수집 데이터`** 칸. 칸 전체가 JSON 블록 하나입니다. **페이지가 갈려 있어 4쪽 첫 줄(`6" }` / `}`)까지가 이 칸입니다.**

**현재** — 마지막 두 줄이 문제입니다.
```
  "heart_rate": 72,
  "hrv": 45.3,
  "raw_payload": { "source_device": "Galaxy Watch 6" }   ← 삭제된 컬럼
}
```
**활동 시각 3개(`activity_start_at`·`activity_end_at`·`total_active_min`)도 빠져 있습니다.**

**교체** — 칸 내용을 **아래로 통째로 바꾸세요.** 컬럼 순서를 `db/schema.sql` 과 맞췄습니다.
```json
{
  "user_id": "c39a12e0-5712-42ba-822d-60603126f000",
  "collected_at": "2026-07-26T21:30:00Z",
  "steps": 3200,
  "distance": 2100,
  "calories": 180,
  "activity_start_at": "2026-07-26T18:20:00Z",
  "activity_end_at": "2026-07-26T19:05:00Z",
  "total_active_min": 45,
  "sleep_start_at": "2026-07-26T23:10:00Z",
  "sleep_end_at": "2026-07-27T06:50:00Z",
  "total_sleep_min": 280,
  "deep_sleep_min": 65,
  "light_sleep_min": 180,
  "rem_sleep_min": 35,
  "awake_min": 20,
  "sleep_onset_min": 15,
  "sleep_efficiency_pct": 71.0,
  "heart_rate": 72,
  "hrv": 45.3
}
```

> **`raw_payload` 는 반드시 지워야 합니다.** 04·05·`schema.sql` 에 없는 컬럼이 03 샘플에만 남아 있으면 **"이 필드는 어느 테이블에 들어가나요"** 에 답할 수 없습니다.
>
> 덧붙여 지워지는 `"source_device": "Galaxy Watch 6"` 는 `05-I` 확인 결과와도 맞지 않았습니다. Health Connect 는 `Galaxy Watch 6` 같은 표시명이 아니라 제조사+모델 코드(`Samsung` + `SM-R900`)를 줍니다.

### ② 「데이터 정의」 칸 — `distance`·`calories` 의 위치 **[추가]**

**위치** 같은 절 → **`데이터 정의`** 칸. 그 칸의 **마지막 문장 바로 뒤에 이어서** 적으세요.

**마지막 문장** (여기까지가 현재 내용입니다)
> …따라서 1차 모델은 수면·활동 지표를 공통 피처로 사용하고, 심박수·HRV 는 확보 가능한 구간에 한해 보조 피처로 활용한다.

**이어서 붙일 문장**
> 활동량 지표 중에서는 걸음 수(steps)를 주 피처로 사용하고, 이동거리·소모칼로리는 걸음 수와 상관이 높아 보조 피처로만 활용하여 피처 중요도 분산을 방지한다.

> 걸음 × 보폭 = 거리, 거리 × 체중 ≈ 칼로리입니다. **LightGBM 에 셋 다 넣으면 피처 중요도가 분산**됩니다.

### ③ 「데이터 정의」 칸 — 체성분의 역할 **[추가]**

**위치** ②에서 붙인 문장 **바로 뒤에 이어서** (같은 칸, 세 번째 문장)

**이어서 붙일 문장**
> 체성분 측정치는 수집 동의가 선택 항목이며 측정 빈도가 불규칙하므로 시계열 이상탐지 입력에서는 제외하고, 측정값이 존재하는 사용자에 한해 개인별 기준값 보정(체중 대비 활동량 정규화)에만 활용한다.

> `BODY_COMPOSITION_METRICS` 11컬럼이 **정서 위험도에 어떻게 기여하는지** 어느 문서에도 없고 `consent_scopes` 기본값도 `body_composition: false`(미동의)입니다.
>
> **"체성분은 왜 받나요"** 에 답이 됩니다. 기업 데이터와 8항목 완전 일치가 강점이므로 **컬럼은 유지**하고 역할만 밝히면 됩니다.

---

# 01. 프로젝트 기획서

## 01-N-1. 14쪽 「방법론」 — `13장` 오타 **[교체]** *(2026.07.30 신규)*

> **`01-N` 교체는 완료**되었습니다. 붙여넣는 과정에서 글자 하나가 딸려 들어갔습니다.

**위치** 14쪽 「사용 기술 및 개발 방법」 → **`방법론`** 행의 **첫 번째 항목 맨 앞**

| | 내용 |
|---|---|
| 현재 | `- `**`13장`**`에서 정의한 기능을 데이터 수집 → 전처리 → …` |
| 교체 | `- 3장에서 정의한 기능을 데이터 수집 → 전처리 → …` |

**`1` 한 글자만 지우면 됩니다.** 3장을 가리키는 문장인데 `13장` 이 되어 존재하지 않는 장을 참조합니다(문서는 6장까지).

> 「표기 규칙」 5번(**글자 조각이 남기**)과 같은 유형입니다. `05-F` 의 `eart_rat` 도 같은 원인이었습니다.

---

# 05. 테이블 명세서

> **삭제할 컬럼은 없습니다.** `raw_payload` 제거 후 남은 75개 컬럼 전부에 근거가 있으며, 더 줄이면 기업 제공 데이터를 받지 못하는 구조가 됩니다.

## 05-M. EMOTION_RISK_SCORES — `model_version` 비고 조정 **[교체]** *(2026.07.30 신규)*

**위치** 10쪽 EMOTION_RISK_SCORES 컬럼 정의표 → `모델 버전 / model_version` 행의 `비고` 칸

| | 내용 |
|---|---|
| 현재 | `MLOps 추적용 AI 모델 버전 (v1.0 등)` |
| 교체 | `추론 시점의 모델 버전 (v1.0 등), 재학습 전후 성능 비교용` |

**04 문서는 `created_at`·`updated_at` 감사 컬럼을 이렇게 빼놓았습니다.**

> 캡스톤 범위의 단일 배포 데모로 운영되어 **별도의 이력 추적·감사 로그 조회 기능을 제공하지 않기 때문**이며…

**감사는 안 한다면서 MLOps 는 한다고 적혀 있어 일관성이 어긋납니다.** 컬럼 자체는 유용하니(재학습 시 어느 버전이 낸 점수인지 구분, 평가셋 성능 측정) **비고 문구만 낮추세요.**

### 참고 — 파생 컬럼 3개는 유지가 맞습니다

*"이건 계산하면 되지 않나요?"* 가 나올 수 있는 자리입니다. **삭제하지 마세요.**

| 컬럼 | 계산식 |
|---|---|
| `total_sleep_min` | `deep + light + rem` |
| `sleep_efficiency_pct` | `total_sleep / (total_sleep + awake + onset) × 100` |
| `total_active_min` | `activity_end - activity_start` |

**답은 01 문서에 이미 있습니다.** 「데이터 출처」가 기업 데이터를 이렇게 기술합니다.

> **집계 지표** : 총 수면시간, 각성 시간, 입면 시간, **수면 효율(%)**, **총 활동시간**·거리·걸음수·소모칼로리

> **답변** — 기업이 집계값으로 제공하는 항목이라 받은 값을 그대로 저장합니다. 재계산하면 원본과 어긋날 수 있고, **활동 구간이 하루에 여러 번이면 종료−시작으로는 총 활동시간이 나오지 않습니다.**

---

# 발표자료 PPTX

> **아래 4건은 슬라이드 순서대로 배열했습니다. 파일을 한 번만 열면 됩니다.**
> `PT-A` `PT-B` `PT-F①②` 와 slide8 스택 표기는 **반영 완료**되었습니다.

## PT-C. slide5 SWOT — ST 전략 **[교체]** ⭐

`01-B` 로 **기획서는 고쳤는데 발표자료가 안 따라왔습니다.** 발표에서 그대로 노출되는 문구입니다.

**현재**
> ST (강점+위협) **기기 내 마스킹과 암호화 설계로** 규제 강화 및 감시 우려 선제 대응

**교체**
> ST (강점+위협) 위험도 기반 계층적 데이터 보호 설계로 규제 강화 및 감시 우려 선제 대응

> 실제 구현은 앱이 `user_id` 를 포함해 전송하고 PII 마스킹도 **백엔드 저장 시점**에 수행합니다. "기기 내"는 사실과 다릅니다.
>
> ⚠ **기획서 SWOT 은 `01-H` 로 갱신을 마쳤습니다.** slide5 의 WO·WT 에도 기획서와 같은 문제(IRB 승인 지연·정부 R&D 지원사업)가 남아 있는지 **함께 확인**하세요. 있으면 아래 문안으로 맞추면 됩니다.
> - WO → `무료 공개 데이터셋과 교내 공용 PostgreSQL 로 데이터·인프라 비용 절감`
> - WT → `승인 절차 없는 공개 데이터셋으로 학습 범위 한정, 공개 데이터 기반 검증`

## PT-D. slide6 · slide8 — 배치 스케줄러 · Node.js 위치 **[교체]**

**slide8 의 스택 표기(`앱 클라이언트(Flutter) · 관리자 웹(React)`)는 이미 반영됐습니다.** 남은 것은 두 가지입니다.

### ① slide8 시스템 아키텍처

| 현재 | 문제 | 교체 |
|---|---|---|
| `15분 주기 배치 스케줄러 (UPSERT 적재)` | `02-H` push 전환 미반영. 서버가 당겨오는 구조로 읽힘 | `앱 push 수신 → UPSERT 적재 (최소 15분 간격)` |
| `메인 백엔드 (Business Server) FastAPI/Node.js` | **Node.js 가 백엔드로 표기됨.** Node 는 React 관리자 웹 빌드용 | `메인 백엔드 FastAPI` · 관리자 웹은 별도 블록으로 분리 |

> **01·02·05 는 모두 앱 push 구조로 개정을 마쳤습니다. 발표자료만 남았습니다.** 지금은 **"그래서 누가 데이터를 가져오나요"** 에 문서와 발표가 다르게 답합니다.

### ② slide6 「핵심 기능」

**현재**
> 위험도 단계별 대응 **(FastAPI, Node.js)**

**교체**
> 위험도 단계별 대응 (FastAPI)

## PT-G. slide12 「학습 데이터셋 확보 계획」 — 오타 **[교체]**

`PT-F②` 로 데이터셋 블록을 교체하는 과정에서 **글자가 하나 끼어들었습니다.**

**현재** — GLOBEM 블록
> Source: **손** 14일 집계 수면·걸음 피처 + BDI-II 우울 척도 점수·우울 라벨

**교체**
> Source: 14일 집계 수면·걸음 피처 + BDI-II 우울 척도 점수·우울 라벨

> `손` 한 글자만 지우면 됩니다. 나머지 문안은 정확합니다.

## PT-E. 팀원 소개 슬라이드 — Apple Health 연동 **[교체]** ⭐

**현재**
> BACKEND / DB 윤일준 — API 서버, PostgreSQL 설계, Health Connect · **Apple Health** 연동

**안건 2에서 iOS/Apple Health 구현을 제외하기로 확정**했는데 담당 업무로 남아 있습니다. 02 요구사항정의서에 **"iOS 는 구현 대상에서 제외한다"** 를 이미 명시해두었으므로 정면으로 부딪힙니다.

**교체**
> BACKEND / DB 윤일준 — API 서버, PostgreSQL 설계, Health Connect 연동

---

# 화면설계서 PPTX

## SD-A. 변경이력 v0.5 + 연동 2건 **[확인]** — ⏸ 보류

> **별도 작업이 진행 중이라 보류합니다.** 해당 작업이 끝난 뒤 아래를 확인하세요.

### ① 변경이력 v0.5

- 작성일자가 `2026.07.2` / `8` 로 셀이 분리되어 깨져 있음 → `2026.07.28`
- **v0.5 행의 검수자 칸이 공란** → 다른 행과 맞춰 기입
- 문서 하단 「작성방법) …」 안내 블록은 **제출 전 삭제** (문서 자체에 "제출할 경우 작성방법 내용은 모두 지우고 제출해야 한다"고 명시되어 있음)

### ② `MAIN_JOIN_02` 에 연락처 선택 입력란 추가 — `결정-1` 후속

**연락처를 선택 입력으로 확정**했고 04 문서에는 반영했는데, **받는 화면이 없습니다.** 현재 `MAIN_JOIN_02` 는 이메일·비밀번호·이름·생년월일·성별만 받습니다.

`❷ 사용자 인적사항` 설명에 연락처를 추가하고 **선택 항목임을 명시**하세요.
> ❷ 사용자 인적사항: 이름, 생년월일, 성별 입력 (연락처는 선택 입력)

> **이 화면이 없으면 04 문서의 "선택 입력 항목인 연락처"가 근거를 잃습니다.** `phone` 컬럼을 유지하는 논리 전체가 여기에 걸려 있습니다.

### ③ `MAIN_JOIN_01` 민감정보 동의 항목 분리 표기 — `05-K` 연동

`MAIN_JOIN_01` 이 이미 **"서비스 이용약관 및 민감정보(생체/건강 데이터) 수집 동의 처리"** 로 되어 있고 필수(❷~❹)·선택(❺) 체크 구조를 갖추고 있습니다.

**남은 일은 그 필수 항목 중 민감정보 동의를 별도 항목으로 분리해 표기하는 것**입니다. 02 문서와 `db/schema.sql`(`sensitive_agreed`) 은 이미 반영을 마쳤습니다.
