# 산출물 문서 개정 체크리스트

> **최종 갱신** 2026.07.31 · **작성자** 이응균(PM) · **목표 완료일** 2026.08.11
> **대상** 01~05 HWP 5종 + 발표자료·화면설계서 PPTX 2종
> **현행성 점검** 2026.07.31 · 완료 항목은 작업이력으로 이동했고 현재 남은 범위만 기재
>
> **이 문서에는 아직 반영되지 않은 항목만 남깁니다.**
> 완료된 결정과 그 근거 → [`작업이력.md`](작업이력.md) · 미결 안건 → [`회의안건_20260729.md`](회의안건_20260729.md)

---

## 1. 진행 현황

| 문서 | 남은 항목 | 담당 |
|---|---|---|
| 01~05 HWP 5종 | **0 — 전부 완료** ✅ | — |
| 발표자료 PPTX | **0 — 전부 완료** ✅ | — |
| 화면설계서 PPTX | 개정안 15건 + 아래 `SD-A③`~`SD-A⑥` | 함은선 |

### 🎉 HWP 5종 + 발표자료 개정 완료 (2026.07.31)

**7/29 에 시작한 산출물 문서 개정이 끝났습니다.** 마지막으로 닫힌 3건은 `02 FR-DP-001` · `SD-E1`(04·05) · `05-N③` 이고, 반영 검증까지 마쳤습니다.

**앱 push 서술이 01·02·03·05·발표자료 전 산출물에서 일치합니다.** 안건 1-1 이 완전히 닫혔습니다.

> 개정 항목별 근거와 검증 결과, 그리고 과정에서 나온 **감사 패턴 4가지·비고 작성 원칙**은 전부 [`작업이력.md`](작업이력.md) 2026.07.31 (14차) 절로 옮겼습니다. **다음 문서를 검수할 때 그 절부터 읽으세요.**

---

## 2. 남은 작업 — 화면설계서 PPTX

**개정안 15건이 정본입니다.** → [`화면설계서_개정안.md`](화면설계서_개정안.md)

2026.07.30 전수 감사에서 **복붙 슬라이드 3장 · 누락 화면 6개 · 사라진 기능 2건 · `MLCM_500` 4중 부여**가 나와 분량이 커졌고, 화면이 **9개 → 15개**가 됩니다.

- **텍스트 교체형은 상당수 PPTX 에 반영**했습니다 (slide1·2·10·13·14·15, 단독 `-` 36개 제거)
- **신규 화면 시안 6장도 완성**돼 있습니다 → `docs/design/`

> **2026.07.31 갱신** — 전수 대조 결과 남은 작업이 「디자인 작업뿐」이 아닙니다.
>
> - **문안 규격이 확정됐습니다** — 화면설명 한 줄 50자·최대 5줄, 화면개요 80자.
>   현재 PPTX 에 **50자 초과 3줄**(slide10·13·15)이 있어 **문구 교체가 필요**합니다.
>   내용 문제가 없어 「유지」로 보던 `MAIN_HOME_01`(slide13)도 여기 걸립니다
> - slide14 `MAIN_LIFELOG_01` 은 **골격근량 누락** 1건
> - 나머지(신규 슬라이드 6장 추가, slide3 메뉴 구성도, 콜아웃 번호 재배치)는
>   와이어프레임 작업이라 코드로 처리할 수 없습니다
>
> 항목별 문안과 자수는 [`화면설계서_개정안.md`](화면설계서_개정안.md), 대조 근거는
> [`작업이력.md`](작업이력.md) 20차에 있습니다.

### SD-A⑤. `MAIN_HOME_01` ❶ 감정 이름 노출 제거 **[교체]** ⭐ — 안전 직결

**감정 마스터 9종에 「위기」·「절망」이 들어 있습니다.** 현재 앱은 이 이름을 홈 화면
헤드라인(27pt)으로 그대로 띄우고 있었습니다. 힘들어하는 사람 화면에 그 단어를 크게
박으면 데이터 관찰이 아니라 **사람에 대한 판정**으로 읽힙니다.

> **02 요구사항의 「진단 금지」에 걸립니다.** 챗봇 응답에만 적용되는 원칙이 아니라
> 화면 표기에도 같이 적용돼야 합니다.

**문서도 이렇게 하라고 한 적이 없습니다.** 현재 화면설명 ❶ 은
`대표 감정 이모지, 정서 점수 및 긍정/중립/부정 상태 분포` 입니다 — **이모지**를
쓰라고 했지 감정 이름을 텍스트로 띄우라고 하지 않았습니다. 구현이 문서에서 벗어난
것이었고, 이번에 문서 쪽 표기를 명확히 합니다.

**교체** — 화면설명 ❶ 한 줄

```
❶ 오늘의 마음 상태: 상태 안내 문구와 정서 점수 표시
```

| 항목 | 내용 |
|---|---|
| 개정 전 | `❶ 오늘의 감정: 대표 감정 이모지, 정서 점수 및 긍정/중립/부정 상태 분포` (49자) |
| 개정 후 | `❶ 오늘의 마음 상태: 상태 안내 문구와 정서 점수 표시` (27자) |

**바뀐 것과 이유**

- **「대표 감정 이모지」를 뺐습니다.** 위기 감정에 어떤 이모지를 쓸지가 문제입니다.
  😢 같은 것을 쓰면 캐릭터가 같이 괴로워하는 셈이라 감정을 더 키웁니다
  (마스코트 표정을 담담하게 바꾼 것과 같은 이유)
- **「긍정/중립/부정 상태 분포」를 뺐습니다.** 홈에는 구현돼 있지 않고, 기간별 분포는
  `MAIN_REPORT_01` ❸ 이 담당합니다. 두 화면에 같은 것을 두면 역할이 겹칩니다
- **정서 점수는 유지**합니다. 링 형태로 구현돼 있습니다

**구현은 완료**했습니다 (`home_screen.dart`, `test/home_no_label_test.dart` 6건으로 고정).
와이어프레임에서 감정 이름 자리를 **문구 두 줄**로 바꿔주세요.

> ⚠ **관리자 화면(`ADMIN_DASH_01`)은 그대로 둡니다.** 거기서 「위기」는 담당자가
> 판단하는 데 필요한 정확한 용어입니다. **본인이 보는 화면과 전문가가 보는 화면은
> 기준이 다릅니다.**

### SD-A⑥. 대화 성격을 고르는 곳을 하나로 **[교체]** — `MAIN_SETTING_01`·`MAIN_CHAT_01`

**설정과 챗봇 두 곳에서 성격을 바꿀 수 있었습니다.** 그런데 챗봇은 세션을 시작할 때
고른 값을 **항상 명시로 넘겨서** 서버의 `user.persona_type` 폴백을 타지 않았습니다.
즉 **설정에서 바꿔도 챗봇 동작이 바뀌지 않았습니다.**

문서 `MAIN_SETTING_01` ❸ 은 「현재 챗봇 성격 표시, 선택 시 `MAIN_CHAT_01` 이동」이라
설정에서 직접 바꾸는 지금 구현과도 달랐습니다. 그런데 **문서대로 고쳐도 이상합니다** —
이 앱은 성격 선택과 대화 시작이 한 동작이라(카드 버튼이 「이 성격으로 대화하기」),
설정에서 「바꾸기」를 누르면 결국 대화가 시작됩니다.

**해결 — 챗봇 탭이 확인 화면 역할을 하게 하고, 설정에서는 뺍니다.**

앱을 껐다 켜도 **최근에 고른 성격 카드가 먼저** 뜨고 **「최근 대화」 표시**가 붙습니다.
확인도 변경도 한 화면에서 됩니다.

---

**교체 ①** — `MAIN_SETTING_01` 화면설명 (❸ 삭제, 4줄)

```
❶ 웨어러블 연동: 기기명·연동 여부, 항목별 동의 개별 토글
❷ 알림 설정: 맞춤 케어 알림 수신 토글
❸ 계정 관리: 계정 관리·회원 탈퇴 화면(MAIN_SETTING_02) 이동
❹ 로그아웃: 세션 무효화 후 로그인 화면(MAIN_LOGIN_01) 복귀
```

> 5줄에서 **4줄**이 됩니다. 규격은 「최대 5줄」이라 줄어드는 것은 문제없습니다.
> 와이어프레임에서 페르소나 카드를 빼고 콜아웃 번호를 하나씩 당겨주세요.

**교체 ②** — `MAIN_CHAT_01` 화면설명 (❹ 추가, 4줄)

```
❶ [F] 따스한 공감형 : 감정 경청 및 따뜻한 위로 중심
❷ [T] 현실적인 조언형 : 객관적 상황 분석 및 문제 해결 중심
❸ 대화 시작: 실시간 대화 화면(MAIN_CHAT_02)으로 이동
❹ 최근 대화한 성격이 기본 표시되며 「최근 대화」 배지로 구분
```

> 기존 「-캐릭터 카드를 물리적으로(스와이프 형식) 선택하는 UI」 줄은 화면개요와
> 겹치므로 **화면설명에서 빼고** ❹ 를 넣습니다.

**서버·데이터는 그대로입니다.** `USERS.persona_type` 의 역할이 「설정값」에서
「마지막으로 대화한 성격」으로 바뀌었을 뿐, 스키마·API 변경은 없습니다.

구현 완료 — `chat_screen.dart`·`settings_screen.dart`,
`test/persona_single_source_test.dart` 5건으로 고정.

### SD-A④. `ADMIN_DASH_01` 대상자 검색 추가 **[교체]** — 화면설명

**2026.08.02 에 관리자 웹에 대상자 검색(이름·이메일)이 구현됐습니다.** 화면설명에 콜아웃을 추가합니다.

> **처음에 「5줄이 꽉 차서 못 넣는다」고 잘못 판단했습니다.** 제약은 **줄 수 5줄**이지
> 항목 수가 아닙니다. 위 「문안 규격」이 `❹~❼` 병합을 명시하고 있고, `MAIN_JOIN_01` 도
> 실제로 `❷ ~ ❹ 필수 동의` 로 **콜아웃 7개를 5줄 안에** 넣었습니다.

**교체** — 화면설명 (5줄 / 콜아웃 7개 / 최장 31자, 규격 50자)

```
❶ 위험도 분포 요약: 안정·주의·심각 인원수 집계
❷~❸ 조회 조건: 위험도 필터, 이름·이메일 검색
❹ 대상자 목록: 심각·주의 우선 정렬, 평가일시·점수
❺ 대상자 상세: 기간별 감정 스코어·라이프로그 차트
❻ 위기 사건 조회 · ❼ 관리자 권한 계정만 접근 가능
```

**바뀐 것**

| 개정 전 | 개정 후 | 이유 |
|---|---|---|
| — | `❷~❸ 조회 조건` 신설 | 위험도 필터와 검색창은 **화면에서 같은 줄에 있는 한 덩어리**입니다. 규격이 말하는 「연속된 입력 필드」에 해당하므로 한 줄로 묶습니다 |
| `❷ 대상자 목록` | `❹` 로 번호만 이동 | 문안 동일 |
| `❸ 대상자 상세` | `❺` 로 번호만 이동 | 문안 동일 |
| `❹ 위기 사건 조회` | `❻` · 뒷문장 생략 | 줄 길이를 벌어 ❼ 을 같은 줄에 둡니다 |
| `❺ 관리자 권한…` | `❼` 로 이동, 같은 줄 | **덜어내지 않습니다.** `02-F`(6) 접근통제 방어 논리라 화면설계서에 남아야 합니다 |

**와이어프레임 작업** — 콜아웃 마커가 5개에서 **7개**가 됩니다. 대상자 목록 위쪽 조회 조건 줄에 ❷(위험도 칩)·❸(검색창) 두 개를 새로 얹고, 기존 마커 번호를 밀어주세요.

> 검색은 **연락처로는 동작하지 않습니다.** `phone` 이 컬럼 암호화라 부분 일치가 원리상 성립하지 않습니다(`02-F`(3) · 안건 4). 화면설명에 넣기엔 길어서 **와이어프레임 주석**으로 남기시면 됩니다.

구현 위치는 `frontend/admin/src/App.jsx` 의 `PeopleTab` 입니다.

### SD-A③. `MAIN_JOIN_01` 민감정보 동의 항목 분리 표기 **[교체]** — `05-K` 연동

**개정안에 대응 항목이 없어 여기 남깁니다.**

`MAIN_JOIN_01` 화면개요는 이미 *"서비스 이용약관 및 민감정보(생체/건강 데이터) 수집 동의 처리"* 로 되어 있고 필수(❷~❹)·선택(❺) 체크 구조도 갖췄습니다. **남은 일은 그 필수 항목 중 민감정보 동의를 별도 항목으로 드러내는 것**입니다.

현재 화면설명이 `❷ ~ ❹ 필수 동의` 로 뭉뚱그려져 있어, **어느 항목이 민감정보 동의인지 화면설계서만 봐서는 알 수 없습니다.**

> 02 요구사항정의서와 `db/schema.sql`(`sensitive_agreed`·`sensitive_agreed_at`)은 이미 반영을 마쳤습니다. **개인정보보호법 제23조 제1항이 민감정보에 별도 동의를 요구**하므로, 화면에서 구분이 드러나야 근거가 성립합니다.

---

## 3. 표기 규칙

- **[교체]** 해당 문장/항목을 통째로 바꿈 · **[추가]** 기존 내용 뒤에 덧붙임 · **[확인]** 사실관계 확인 후 판단
- ⭐ 심사 방어에 직결 · ⭐⭐ 그중에서도 우선

> ### ⚠ PPTX 를 저장할 때
> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.99MB → 305KB, 폰트 6개 → 0개로 실제 발생). `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.
>
> 텍스트만 바꾸는 것이라면 `tools/patch-pptx-text.ps1` 처럼 **XML 을 직접 고치는 편이 안전합니다.** 서식을 건드리지 않아 폰트가 풀리지 않습니다.

> ### ⚠ 붙여넣을 때마다 확인하세요
> 실제로 다섯 번 다 사고가 났습니다.
> 1. **마크다운 기호** — `**굵게**` 의 별표, `` `코드` `` 의 백틱이 딸려 들어갑니다
> 2. **스마트 따옴표** — 한글이 `'FRIEND'` 를 `'FRIEND’` 로 바꿉니다. **SQL 이 실행되지 않습니다**
> 3. **고칠 때 한 글자 더 지우기** — 따옴표를 고치다 옆의 정상 따옴표까지 지워 `IN ('FRIEND,` 가 된 적이 있습니다
> 4. **앞뒤 줄까지 붙여넣기** — 코드 블록이 위치를 잡으려고 **앞뒤 줄을 함께 보여줄 때가 있습니다.** 그 줄들은 이미 문서에 있으니 빼고 붙여넣으세요
> 5. **글자 조각이 남기** — 비고를 고치다 `heart_rate` 의 조각인 `eart_rat` 가 남은 적이 있습니다. **고친 칸은 끝까지 읽으세요**

> ### ⚠ 축약 표기를 그대로 옮기지 마세요
> 표 안에서 자리를 아끼려고 `단위 + DEFAULT 0` 처럼 줄여 쓴 것이 문서에 그대로 들어간 적이 있습니다. 실제로 들어갈 값은 `단위: m, DEFAULT 0` 이었습니다.
>
> **작성 측 규칙** — 표의 「교체」 칸에는 **문서에 들어갈 문자열을 그대로** 적는다. 설명이 필요하면 표 밖에 쓴다.

> ### ⚠ 반영 여부를 문구 검색으로만 판정하지 마세요
> **grep 0건이 미반영을 뜻하지 않습니다.** `01-G`·`01-H②` 가 처음 0건으로 나왔는데 실제로는 반영돼 있었고 **줄바꿈이 문구를 쪼갠 것**이었습니다(`앱에서 최소 15분` / `간격 전송`). 반드시 절 전체를 읽어 확인하세요.
>
> **반대로 검색이 오탐을 낼 수도 있습니다.** 01 의 `혈압` 1건은 활용방안이 아니라 **시장 현황의 통계 인용**(한국미디어패널조사)이라 문제없었습니다.

---

## 4. 문서를 고친 뒤

1. **HWP 를 고쳤으면 PDF 로 다시 내보냅니다.** `tools/doc2txt.py` 는 HWP 를 읽지 않아서, PDF 를 새로 뽑지 않으면 추출본이 옛 내용 그대로입니다
2. `python tools/doc2txt.py` 로 재추출합니다
3. **원본과 추출본을 같은 커밋에** 넣습니다. 그래야 버전 간 diff 가 보입니다

> 실제로 PDF 는 최신본인데 추출본은 이전 판인 커밋이 나온 적이 있습니다(`7922122`). 재추출을 건너뛰면 다음 사람이 옛 내용을 근거로 판단하게 됩니다.
