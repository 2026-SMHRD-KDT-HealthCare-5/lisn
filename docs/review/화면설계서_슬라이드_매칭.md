# 화면설계서 슬라이드 매칭표

> **작성** 2026.08.02 · 목적: **어떤 슬라이드에 무엇을 넣어야 하는지** 한 장으로 보기
>
> 문안은 [`화면설계서_개정안.md`](화면설계서_개정안.md), 교체 항목은
> [`문서개정_체크리스트.md`](문서개정_체크리스트.md) 에 있습니다. 이 문서는 **배치표**입니다.

---

## 현재 15장 → 목표 21장

```
1~6   표지·변경이력·메뉴구성·플로우차트 3종      (화면 아님, 그대로)
7~15  화면 9장                                  (기존)
+6    화면 6장 신규                              ← 이번에 넣을 것
```

---

## 배치표

**「넣을 위치」는 그 슬라이드 바로 뒤를 뜻합니다.** 관련 화면끼리 붙여 두면 심사에서
흐름을 따라가기 쉽습니다.

| 넣을 위치 | 화면 ID | 화면이름 | 문안 | 시안 PNG | 왜 여기 |
|---|---|---|---|---|---|
| **7 뒤** | `MAIN_LOGIN_02` | 비밀번호 재설정 | `SD-N1` | `MAIN_LOGIN_02.png` | 로그인 화면에서 진입하는 흐름 |
| **14 뒤** | `MAIN_REPORT_01` | 정서 리포트 | `SD-N2` | `MAIN_REPORT_01.png` | 메뉴경로가 「라이프로그 / 정서 리포트」 |
| **15 뒤** | `MAIN_SETTING_02` | 계정 관리·회원 탈퇴 | `SD-N3` | `MAIN_SETTING_02.png` | 설정 화면 하단에서 진입 |
| **15 뒤** | `MAIN_EMERGENCY_01` | 긴급 상담 연결 | `SD-N4` | `MAIN_EMERGENCY_01.png` | 전역 화면. 앱 화면 마지막에 |
| **맨 뒤** | `ADMIN_LOGIN_01` | 관리자 로그인 | `SD-N6` | `ADMIN_LOGIN_01.png` | 관리자 웹은 따로 묶습니다 |
| **맨 뒤** | `ADMIN_DASH_01` | 관리자 관제 대시보드 | `SD-N5` | `ADMIN_DASH_01.png` | 로그인 다음 |

> 시안은 전부 `docs/design/` 에 있습니다. **원본 HTML 은 `docs/design/src/`** 에 있어
> 고쳐서 다시 구울 수 있습니다(방법은 `docs/design/README.md`).

---

## 기존 15장은 이렇게 되어 있습니다

| 슬라이드 | 내용 | 화면 ID | 이번에 손볼 것 |
|---|---|---|---|
| 1 | 표지 | — | — |
| 2 | 변경이력 | — | **v0.8 행 추가·검수자 기입** |
| 3 | 메뉴 구성 | — | 「감정 변화 기록」→「정서 리포트」, 관리자 웹 노드 |
| 4~6 | 플로우 차트 3종 | — | — |
| 7 | 로그인 | `MAIN_LOGIN_01` | 콜아웃 4→7 (비밀번호 찾기 진입점) |
| 8 | 약관 동의 | `MAIN_JOIN_01` | **`SD-A③`** 민감정보 동의 분리 |
| 9 | 계정 정보 입력 | `MAIN_JOIN_02` | 키(`height_cm`) 입력란 마커 |
| 10 | 가입 완료·수집 항목 | `MAIN_JOIN_03` | 문안 50자 초과 1줄 |
| 11 | 챗봇 성격 선택 | `MAIN_CHAT_01` | — |
| 12 | 챗봇 대화 | `MAIN_CHAT_02` | 콜아웃 4→8 · **마이크 버튼 제거** |
| 13 | 홈 | `MAIN_HOME_01` | 문안 50자 초과 1줄 |
| 14 | 라이프로그 | `MAIN_LIFELOG_01` | **골격근량 누락** · 문안 50자 초과 |
| 15 | 설정 | `MAIN_SETTING_01` | — |

---

## ⚠ 슬라이드 추가는 스크립트로 못 합니다

`tools/patch-pptx-text.ps1` 은 **텍스트 교체 전용**입니다. 슬라이드를 새로 넣으려면
slide XML · rels · `presentation.xml` · `[Content_Types].xml` **네 곳에 등록**해야 하고
이건 미검증입니다. **PowerPoint 에서 직접 추가하세요.**

> 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.99MB → 305KB, 폰트 6개 → 0개로
> 실제 발생). 저장 전에 `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.

기존 슬라이드의 **문안만** 바꾸는 것은 스크립트가 안전합니다(서식을 안 건드려 폰트가
풀리지 않음).

---

## 화면을 직접 보면서 작업하려면

세 가지 방법이 있고, 목적에 따라 다릅니다.

### 1. 시안만 보면 될 때 — 브라우저 (가장 빠름)

에뮬레이터도 서버도 필요 없습니다. **화면설계서에 들어갈 그림은 이 시안입니다.**

```powershell
start docs\design\MAIN_REPORT_01.png
```

원본 HTML 을 열면 확대해도 안 깨집니다.

```powershell
start docs\design\src\MAIN_REPORT_01.html
```

### 2. 실제 앱 화면을 볼 때 — 원하는 화면으로 바로 띄우기

#### 먼저 에뮬레이터를 켭니다

이 PC 에는 **`lisn` 이라는 에뮬레이터가 이미 만들어져 있습니다.**

```powershell
flutter emulators
```

```powershell
flutter emulators --launch lisn
```

40초~1분쯤 걸립니다. 뜨면 확인하세요.

```powershell
flutter devices
```

`sdk gphone64 x86 64 (mobile) • emulator-5554 • android-x64` 가 보이면 준비된 것입니다.

> **에뮬레이터가 없는 PC 라면** 한 번만 만들면 됩니다.
> ```powershell
> flutter emulators --create --name lisn
> ```
> Android Studio 를 설치했는데 위 명령이 안 되면 Android Studio 의
> **Device Manager** 에서 만드세요. `sdkmanager`·`avdmanager` 는 `JAVA_HOME` 을
> 요구하는데, Flutter 는 Android Studio 의 JDK 를 스스로 찾으므로 **`flutter` 명령
> 쪽이 더 잘 됩니다.**

#### 그다음 원하는 화면으로 띄웁니다

**매번 로그인하지 않아도 됩니다.**

```powershell
C:\LISN\tools\show-screen.ps1 report
```

에뮬레이터가 꺼져 있으면 알아서 켜고 기다립니다. **절대 경로로 적었습니다** —
`.\tools\...` 는 저장소 루트에서만 되고, `frontend/app` 안에서 치면 못 찾습니다. 화면 목록은 인자 없이 실행하면 나옵니다.

직접 치려면 **앱 폴더로 들어가야 합니다.** 저장소 루트에서 `flutter run` 하면
`No pubspec.yaml file found` 가 납니다.

```powershell
cd frontend\app
flutter run --dart-define=DEV_LOGIN=force --dart-define=SCREEN=report
```

> `report`·`emergency` 는 평소에 **다른 화면 위에 얹혀 열리는 화면**이라, 부모 화면을
> 깔고 그 위에 띄웁니다. 그래야 **뒤로가기·닫기가 실제와 같게** 나옵니다.
>
> ⚠ **`force` 를 쓰세요.** `DEV_LOGIN=true` 는 **이미 로그인돼 있으면 건너뜁니다.**
> 앞서 손으로 다른 계정에 로그인해 뒀다면 그 계정 데이터가 그려지는데, 화면은
> 멀쩡해 보여서 **캡처를 뜬 뒤에야 압니다.** 실제로 한 번 겪었습니다.
> `force` 는 기존 세션을 버리고 데모 계정으로 다시 로그인합니다.

| `SCREEN` 키 | 화면 |
|---|---|
| `login` | `MAIN_LOGIN_01` |
| `reset` | `MAIN_LOGIN_02` |
| `join` | `MAIN_JOIN_01`~`03` |
| `home` | `MAIN_HOME_01` |
| `chat` | `MAIN_CHAT_01`·`02` |
| `lifelog` | `MAIN_LIFELOG_01` |
| `setting` | `MAIN_SETTING_01`·`02` (하단으로 스크롤) |
| `report` | `MAIN_REPORT_01` |
| `emergency` | `MAIN_EMERGENCY_01` |

**데이터까지 보이게 하려면** 백엔드가 떠 있고 데모 시드가 들어가 있어야 합니다.

```powershell
.\tools\start-dev.ps1
```

```powershell
psql -U postgres -d lisn -f db\seed_demo_persona.sql
```

> ⚠ **인증을 우회하는 플래그입니다.** 컴파일 시점 플래그라 평소 빌드에는 영향이 없고
> 릴리스 빌드에서는 무시되지만, **제출·시연 빌드에 이 플래그를 주지 마세요.**
> 자세한 내용은 `frontend/app/lib/dev_screens.dart` 상단 주석.

### 3. 관리자 웹 — 브라우저에서 바로

```powershell
.\tools\start-dev.ps1
```

`http://localhost:5173` 에서 `admin@lisn.dev` 로 로그인합니다.

> **5173 포트여야 합니다.** 백엔드 CORS 가 그 주소만 허용해서, 이전 vite 가 5173 을
> 잡고 있으면 5174 로 뜨고 요청이 전부 막힙니다.

---

## 캡처를 뜰 때

- **앱은 390×844** 기준입니다(시안 캔버스와 같음)
- 에뮬레이터 화면 캡처는 `Ctrl+S`, 또는 명령으로

  ```powershell
  adb exec-out screencap -p > report.png
  ```

  `adb` 는 `%LOCALAPPDATA%\Android\Sdk\platform-tools` 에 있습니다
- 관리자 웹은 **1280×800**

시안과 실제 화면이 다르면 **시안 쪽을 고치는 게 맞습니다.** 화면설계서는 설계 문서라
구현 이전의 의도를 담아야 하고, 구현이 시안을 따라간 것이지 그 반대가 아닙니다.
