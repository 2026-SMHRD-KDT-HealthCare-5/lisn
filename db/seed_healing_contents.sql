-- =====================================================================
--  HEALING_CONTENTS 시드 — MLCM_400 · FR-CS-001
--
--  ⚠ 이 파일은 **틀만 있고 내용은 비어 있습니다.** URL 을 사람이 채워야 합니다.
--
--  04 문서 7항이 등록 조건을 못 박고 있습니다.
--    "자극적이거나 특정 감정에만 극단적으로 특화된 콘텐츠는 배제하고,
--     중립적이고 보편적으로 안정감을 주는 콘텐츠만 사전 검수 후 등록"
--
--  이 원칙이 있는 이유는 **AI 의 감정 분류가 틀려도 사용자에게 해가 되지 않게**
--  하기 위해서입니다. 추천 정확도로 안전을 보장하지 않고, 콘텐츠 풀 자체를
--  안전하게 만들어 정확도와 무관하게 안전을 확보하는 구조입니다.
--  따라서 URL 을 검수 없이 채우면 그 설계가 무너집니다.
--
--  선정 기준은 docs/review/힐링콘텐츠_큐레이션.md 참조.
--
--  적용:
--    psql -U postgres -d lisn -f db/seed_healing_contents.sql
-- =====================================================================


-- 감정 코드로 emotion_id 를 찾아 넣습니다. UUID 를 직접 쓰지 마세요 —
-- gen_random_uuid() 로 만들어져 환경마다 다릅니다.
--
-- 아래는 작성 예시입니다. 실제 URL 로 바꾸고 주석을 해제하세요.
--
-- INSERT INTO healing_contents (emotion_id, category, title, description, external_url)
-- SELECT emotion_id, 'MUSIC', '잔잔한 피아노 연주 모음',
--        '가사 없는 연주곡. 집중이나 휴식 어느 쪽에도 방해가 되지 않습니다.',
--        'https://...'
--   FROM emotions WHERE emotion_code = 'ANXIETY';


-- ---------------------------------------------------------------------
--  등록 현황 확인
-- ---------------------------------------------------------------------
-- 감정별로 몇 건씩 들어갔는지 봅니다. 0 인 감정이 있으면 그 감정으로
-- 판정된 사용자는 홈에서 추천이 비어 보입니다.
--
--   SELECT e.emotion_code, e.category, COUNT(h.content_id) AS 콘텐츠수
--     FROM emotions e
--     LEFT JOIN healing_contents h ON h.emotion_id = e.emotion_id
--    GROUP BY e.emotion_code, e.category
--    ORDER BY e.category, e.emotion_code;


-- ---------------------------------------------------------------------
--  CRITICAL 감정(DESPAIR · CRISIS)은 등록하지 않습니다
-- ---------------------------------------------------------------------
-- MLCM_510 2단계가 "진행 중이던 일반 콘텐츠 추천 로직을 즉시 자동 중단" 을
-- 규정합니다. CRITICAL 판정 시에는 긴급 상담 연결로 바로 전환되므로
-- 그 감정에 콘텐츠를 매핑해도 노출될 일이 없습니다.
--
-- 서버도 action != 'CONTENT' 이면 추천을 조회하지 않습니다(app/api/v1/home.py).
-- 등록해두면 "왜 안 나오지" 하고 헤매게 되므로 아예 넣지 마세요.
