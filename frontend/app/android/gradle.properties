org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# 프로젝트 경로에 한글이 있으면(예: ...\바탕 화면\...) AGP 가 빌드를 막습니다.
# 순수 Dart 앱이라 NDK 를 쓰지 않으므로 검사만 끄고 진행합니다.
#
# 이 플래그로도 안 되는 게 있었습니다 — flutter analyze 는 한글 경로에서 LSP 채널이
# 깨져 그냥 죽습니다. 그래서 2026.08.02 에 저장소를 C:\LISN 으로 옮겼습니다.
# 이 줄은 한글 경로를 쓰는 다른 팀원 PC 를 위해 남겨둡니다.
android.overridePathCheck=true
