# 마음이 Flutter 앱

기존 React 프로토타입을 Flutter 모바일 앱으로 옮긴 프로젝트입니다.

## 실행 준비

1. Flutter SDK를 설치합니다.
2. 이 폴더에서 플랫폼 프로젝트 파일을 생성합니다.

```bash
flutter create --platforms=android,ios .
flutter pub get
flutter run
```

`lib/`, `assets/`, `pubspec.yaml`은 이미 구성되어 있습니다.
