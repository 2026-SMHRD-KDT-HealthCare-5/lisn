[pytest]
asyncio_mode = auto

# asyncpg 커넥션 풀은 생성된 이벤트 루프에 묶인다. 테스트마다 루프를 새로 만들면
# 두 번째 테스트부터 "Event loop is closed" 로 죽는다. 세션 전체가 한 루프를 쓰게 한다.
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session

testpaths = tests
filterwarnings =
    ignore::DeprecationWarning