# CLAUDE.md

> **현행성 점검** 2026.08.06 · **문서·평가셋이 전부 닫혔습니다.**
> 남은 문서는 `PL-38`·`PL-39` 두 건(요구사항정의서 `MLCM_220` 이벤트
> 흐름 칸의 6번·2번)뿐이고, **같은 칸이라 한 번에 고칩니다.**
>
> **평가셋** — 211건 · 위기 111 · 비위기 100 · `team` 211/211 · 교차
> 라벨링·조정 완료. **재현율 0.793 · 정밀도 0.967 · F1 0.871** 입니다.
> ⚠ **`0.919` 를 쓰지 마세요** — AI 초안으로 잰 값이라 「사람이 작성」
> 요건을 못 채웁니다. 드러나면 방어할 수 없습니다.
>
> **선제 접촉(`MLCM_220`)이 끝에서 끝까지 붙었습니다** — 판정 → 조건 검사 →
> 세션 선생성 → 홈 카드·성격 선택 배너. **FCM 없이도 앱을 열면 닿습니다.**
>
> **지금 남은 것은 셋** — ① Health Connect **실기기** 검증(실기기 없이는 못
> 넘깁니다) ② **FCM 발송 경로**(Firebase 자격증명 선행 · 앱 토큰 발급과 서버
> 발송 양끝 모두 비어 있음) ③ `PL-38`·`PL-39` 반영.
>
> ⚠ **발표자료 PPTX 는 개정 범위에서 뺐습니다**(2026.08.04 PM 결정). 기획서
> 발표는 끝났고, `PL-20`~`PL-24` 는 **8/28 최종 발표 준비 시점으로 이월**합니다.
> 그래서 `tools/check_docs.py` 는 **발표자료를 검사하지 않습니다**(8/6).
> 지금은 전부 통과합니다. **8/28 준비를 시작하면 `EXEMPT` 에서 빼세요.**

이 저장소에서 작업할 때 알아야 할 맥락입니다.

## 프로젝트

귀기울임(LISN) — 멀티모달 라이프로그 감정 분석 기반 맞춤형 LLM 케어 및 모니터링 시스템.
스마트인재개발원 KDT 헬스케어 5팀 캡스톤, 기업주제(라라랩스). 4인 팀, 6주 일정(2026.07.17 ~ 08.28).

**현재 단계는 개발 착수 단계입니다.** 문서 검수·수정 마감 8/11, 최종 발표 8/28.

## 저장소에서 가장 먼저 볼 것

| 파일 | 용도 |
|---|---|
| `docs/SESSION-HANDOFF.md` | **여기부터.** 현재 상태·다음 할 일·킥오프 프롬프트 |
| `docs/학습자료.md` | 팀 학습용. 설계 이유 · **되돌리면 안 되는 것** · 실패 사례집 |
| `docs/extracted/*.txt` | 산출물 본문 추출본. **HWP 원본 대신 이걸 읽으세요** |
| `db/schema.sql` | 8개 테이블 DDL. **정본.** 개정 근거가 주석으로 병기됨 |

### `docs/` 는 성격별로 나뉘어 있습니다 (2026.08.02 정리)

전에는 `docs/review/` 한 폴더에 13개가 섞여 있었습니다. **무엇이 살아 있는
문서이고 무엇이 지나간 기록인지** 구분이 안 됐습니다.

| 폴더 | 담는 것 | 갱신 |
|---|---|---|
| `docs/진행/` | 지금 굴러가는 것 — 작업이력 · 문서개정 체크리스트 | **계속** |
| `docs/결정/` | 확정된 설계 결정 — API 사전결정 · 화면설계서 개정안 · API 명세 초안 | 거의 안 함 |
| `docs/검증/` | 재보고 남긴 기록 — 성능실측 · 문서↔구현 대조 · 감정라벨 조사 | 날짜별 누적 |
| `docs/평가셋/` | 위기 판정 평가셋 일체(문서 + CSV + 캐시) | 채점할 때 |
| `docs/가이드/` | 작업할 때 펴놓고 보는 것 — 콘텐츠 큐레이션 · 슬라이드 매칭 | 필요 시 |
| `docs/공지/` | 팀 공지 | 날짜별 |
| `docs/llm/` · `docs/design/` · `docs/extracted/` | 이전 그대로 | |

색인은 [`docs/README.md`](docs/README.md) 에 있습니다.

**역할이 갈립니다.** 완료된 건 `진행/작업이력.md` 로 옮기고 체크리스트에서
지웁니다. 그래야 "무엇을 해야 하는지"가 보입니다.

## LLM·프롬프트 작업 규칙

OpenAI API, LLM 프롬프트, 챗봇 대화 상태·요약, 페르소나, 위기 탐지, 안전장치와 관련된
코드나 문서를 변경하기 전에는 아래 절차를 따릅니다.

1. `docs/llm/PROMPT_REFERENCE.md`를 읽습니다.
2. `docs/llm/USAGE_LOG.md`에서 최신 관련 기록을 검색합니다.
3. OpenAI 기능·API 계약은 `openaiDeveloperDocs` MCP의 최신 공식 문서를 먼저 확인합니다.
   Cookbook은 구현 예제로 사용하고, DAIR.AI·Learn Prompting·prompts.chat은 보조 자료로만
   사용합니다. Claude 전용 동작은 Anthropic 공식 문서를 우선합니다.
4. 구현 전에 성공 조건과 대표 평가 사례를 정하고, 구현 후 같은 사례로 검증합니다.
5. 실제로 채택하거나 거부한 방법과 검증 결과를 `docs/llm/USAGE_LOG.md` 맨 위에 기록합니다.

커뮤니티 프롬프트를 검증 없이 복사하지 않습니다. 의료·위기 안전은 프롬프트만으로 보장하지
말고 서버 검증·정책·테스트를 함께 둡니다. 숨은 Chain-of-Thought 출력을 요구하지 않고,
필요한 결론·근거·검증 가능한 구조화 필드를 요청합니다.

## 산출물 본문 추출

산출물이 HWP·PPTX 바이너리라 직접 읽을 수 없습니다. 추출 경로가 두 개 있습니다.

### 1순위 — `tools/doc2txt.py` (PDF·PPTX 기준, 정확도 높음)

한글에서 **PDF 로 내보낸 뒤** 돌리세요. **표를 표 구조로 뽑기 때문에** 테이블명세서·요구사항 표·일정표 대조가 정확합니다. 페이지별 이미지 개수도 세어주므로 ERD·다이어그램 존재 여부를 열어보지 않고 확인할 수 있습니다.

```powershell
pip install pypdf pdfplumber
python tools/doc2txt.py
```

`documents/` 의 PDF·PPTX 를 전부 읽어 `docs/extracted/*.txt` 로 씁니다.

### 2순위 — `tools/hwp2txt.ps1` (HWP 직접 파싱, 설치 불필요)

CFB(OLE) 컨테이너를 파싱하고 raw deflate 를 풉니다. **Python 없이 동작**하므로 환경이 안 갖춰졌을 때 씁니다. 다만 **표 구조가 무너져** 행·열 관계를 알 수 없습니다.

```powershell
.\tools\hwp2txt.ps1 -Path ".\documents\요구사항정의서_귀기울임.hwp"
```

큰 문서(600KB+)는 2분 이상 걸리므로 백그라운드로 돌리세요.

### 공통 규칙

- **편집은 HWP, 대조는 PDF.** PDF 는 편집할 수 없으니 HWP 원본을 절대 지우지 마세요. 둘 다 `documents/` 에 둡니다.
  - `doc2txt.py` 는 **HWP 를 읽지 않습니다.** PDF 를 다시 내보내지 않으면 추출본이 옛 내용 그대로입니다. 문서를 고쳤으면 **PDF 내보내기 → 재추출** 을 반드시 거치세요.
- 문서가 갱신되면 **추출본도 다시 뽑아 같은 커밋에** 넣습니다. 그래야 버전 간 diff 가 보입니다.
- **반영 여부를 문구 검색으로 판정하지 마세요.** 칸이 좁은 셀은 글자가 칸 너비에 맞춰 쪼개지고 옆 칸 텍스트와 섞여 추출됩니다(`permission_gra nted`, `terms_agreed_a t`). 표 제목 칸은 특히 심해서 `정서 리포트 및 관리자 관제 대시보드` 가 다섯 조각으로 흩어집니다.
  - **긴 본문 문장으로 검색**하세요. 라벨은 쪼개지지만 본문은 칸이 넓어 온전히 남습니다.
  - grep 이 0건이어도 **절 전체를 읽어** 확인하세요. 실제로 반영된 항목을 미반영으로 잘못 보고한 사례가 두 번 있었습니다.
  - 부분 문자열이 다른 단어에 걸릴 수 있습니다. `eart_rat` 은 `heart_rate` 에 걸립니다 — 앞 문맥을 붙이거나 `(?<!h)` 같은 부정 룩비하인드를 쓰세요.
- 체크리스트 문안을 문서에 붙여넣을 때 **마크다운 기호(`**`, 백틱)가 딸려 들어가지 않게** 확인하세요. 실제로 기획서·요구사항정의서에 들어간 적이 있습니다.
- 발표자료 PPTX 를 다시 저장하면 **임베드 폰트가 풀릴 수 있습니다**(5.5MB → 0.3MB). 실제로 폰트 0개 상태로 커밋된 적이 있습니다. `파일 → 옵션 → 저장 → 파일에 글꼴 포함` 을 확인하세요.

## PPTX 를 코드로 고치기

`tools/patch-pptx-text.ps1` 참조. **PowerPoint 로 다시 저장하지 않으므로 임베드 폰트가 풀리지 않습니다.**

- PPTX 는 zip 입니다. `ppt/slides/slideN.xml` 의 `<a:t>` 안 글자만 바꾸고 **`<a:rPr>`(서식)은 건드리지 않습니다.** 폰트·크기가 그대로 유지됩니다
- **교체는 정확히 1건 매칭될 때만 적용**하도록 짜세요. 0건이면 건너뛰고 보고 → 재실행해도 안전합니다
- 적용 후 반드시 검증: 슬라이드 수 · 전체 파트 수 · 각 슬라이드 `[xml]` 파싱
- **한 줄의 텍스트가 여러 런으로 쪼개져 있을 수 있습니다.** 서식이 다른 부분(색 다른 단어 등)에서 갈립니다. 첫 런에 새 문장을 넣고 나머지를 빈 문자열로 만드세요
- **슬라이드 추가는 `tools/add-pptx-slide.ps1`** 을 쓰세요. 기존 슬라이드를 복제해 붙입니다(레이아웃·서식이 따라옵니다). 2026.08.02 에 **PowerPoint 로 열어 렌더링까지 검증**했습니다
  - 등록이 **네 곳** 필요합니다 — `ppt/slides/slideN.xml` · `slides/_rels/slideN.xml.rels` · `ppt/_rels/presentation.xml.rels` · `ppt/presentation.xml` 의 `<p:sldIdLst>` · `[Content_Types].xml` 의 `Override`. 스크립트가 전부 처리하고 **검증을 통과해야만 원본을 갈아치웁니다**
  - ⚠ **노트 슬라이드는 복제하지 않습니다.** 노트가 원본 슬라이드로 역참조(`notesSlideN.xml.rels` → `../slides/slideN.xml`)를 갖고 있어서, 같이 복제하면 PowerPoint 가 「복구가 필요합니다」를 띄웁니다
  - 이미지는 원본과 **공유**합니다. 화면 캡처는 PowerPoint 에서 직접 갈아끼우세요

## 화면 시안을 코드로 만들기

`docs/design/README.md` 에 방법과 팔레트 추출값이 있습니다. Figma REST API 는 읽기 전용이라 외부에서 노드를 못 만듭니다. 대신 기존 시안 PNG 에서 픽셀로 색을 뽑고, HTML 로 그린 뒤 **Edge 헤드리스**로 굽습니다(이 PC 에 Node·Python 이 없어도 됨).

- 아이콘은 **이모지 대신 인라인 SVG**. 이모지는 OS 가 자기 색으로 렌더해 팔레트 밖으로 튑니다
- `--virtual-time-budget` 을 주지 않으면 웹폰트가 안 붙은 채 찍힙니다
- 정신건강 위기 화면에 **경고색(빨강·주황)을 쓰지 마세요.** 불안을 키워 회피를 유발합니다. 주목도는 구조로 만듭니다

## 문서 수정 시 주의

**5개 문서 + 발표자료 2종이 서로 물려 있습니다.** 한 곳을 고치면 다른 곳도 같이 고쳐야 하는 경우가 많습니다. 수정 전에 반드시 체크리스트의 선행 의존성을 확인하세요.

- `[확인]` 표시 항목은 사실관계 확인이 필요한 것들입니다. **추정으로 채우지 마세요.** 법령 조문, 긴급상담 번호(1577-0199 유효성), AI Hub 데이터셋 접근 가능 여부 등이 포함됩니다.
- 개정안을 검토할 때는 인용된 "현재 문장"을 `docs/extracted/` 원문과 대조하세요. 실제로 어긋난 사례가 있었습니다.
- **`[확인]` 이라고 다 결정 대기가 아닙니다.** 문서가 이미 요구하는 것을 확인 항목으로
  잘못 분류한 적이 있습니다. `height_cm`은 데이터베이스요구사항분석서·테이블명세서에서 확정된 뒤 `MAIN_JOIN_02` 선택
  입력으로 구현까지 완료됐습니다.

### 확정된 결정

- **안건 1-1** — Health Connect 는 Android on-device 권한 모델이라 서버가 보유할 OAuth 토큰이 없습니다. **서버 pull → 앱 push 구조로 전환** 확정. `MLCM_200`·`NFR-DV-002` 의 수집·재시도 주체가 백엔드 스케줄러에서 앱 클라이언트로 이동했습니다. 기획서·요구사항정의서·데이터베이스요구사항분석서·테이블명세서 모두 반영 완료.
- **안건 2** — iOS/Apple Health 구현 제외. 단 `platform_type` enum 의 `APPLE_HEALTH` 값은 유지(빼면 컬럼에 값이 하나뿐이라 존재 이유가 사라짐).
  - ⚠ **「기술적으로 불가능」이 아닙니다.** `health` 패키지(13.3.1, **MIT**)는 **HealthKit 과 Health Connect 를 모두 감싸는 래퍼**입니다. 「Health Connect 가 Android 전용이라서」만 근거로 대면 **「그 패키지는 HealthKit 도 되는데요」에 답할 수 없습니다.**
  - **실제 사유는 장비·계정·일정입니다.** ① iOS 빌드는 **macOS·Xcode 필수**(윈도우에서 시뮬레이터·실기기 배포 모두 불가) ② 실기기 배포에 **Apple Developer Program** 필요(연회비 **[확인]**) ③ HealthKit 은 **실기기에 실제 건강 데이터**가 있어야 검증됨 ④ 6주 일정에 Android 실기기 검증도 남아 있음.
  - `frontend/app/ios` 폴더는 이 결정 뒤 **제거**됐습니다. 되살리려면 프로젝트 재생성 + Xcode 에서 HealthKit capability 추가부터입니다.
  - **요구사항정의서 원문은 이미 정확합니다** — 「iOS 는 HealthKit 기반의 **별도 연동 계층을 필요로 한다**」·「**확장 가능하도록 설계하되** 기간 내 제외」. **문서는 고칠 필요 없습니다.**
- **안건 3** — 위기 탐지는 KcBERT 파인튜닝이 아니라 **키워드 규칙 + OpenAI 프롬프트 2단계**.
- **안건 4** — 전 컬럼 AES-256 이 아니라 **위험도 기반 계층적 보호**. 라이프로그 측정치는 기간별 집계·복합 인덱스가 서비스 핵심 동작이라 컬럼 암호화 대상에서 제외.
- **프론트 방향** (2026.07.30) — Flutter 앱이 화면설계서를 참조하지 않고 만들어져 문서와 다릅니다. **앱이 아니라 문서가 정본**이며, 앱을 문서에 맞춰 재구축합니다. 앱 구현을 근거로 문서를 판단하지 마세요.
- **`USERS.role`** (`SD-E1`) — 관리자 기능이 요구사항 5곳에 있는데 데이터 모델에 관리자 개념이 없었습니다. `role VARCHAR(20) DEFAULT 'USER' CHECK IN ('USER','ADMIN')` 추가. `schema.sql`·데이터베이스요구사항분석서·테이블명세서 HWP 모두 반영 완료됐습니다.

## 환경

- Windows 11 / PowerShell. **작업 경로는 PC 마다 다릅니다.** OneDrive 동기화 경로 안에 두면 파일 잠금·지연 동기화 문제가 생길 수 있으니, 새 환경에서는 먼저 `(Get-Location).Path` 로 위치를 확인하세요.
- **PC 마다 런타임 설치 상태가 다릅니다.** `tools/check-env.ps1` 로 먼저 확인하세요. 팀 공통 세팅 절차는 `docs/SETUP.md` 에 있습니다.
  - Python 은 팀 재현성을 위해 **3.12로 고정**합니다. 최신 PyTorch의 지원 여부와 별개로
    프로젝트 환경은 검증된 버전을 통일합니다.
  - PostgreSQL 은 **17 로 고정**.
- Edge 는 어느 PC 에나 있습니다. 헤드리스 렌더링(`--headless=new --screenshot`)에 쓸 수 있어 Node 없이도 HTML → PNG 가 됩니다.
- **`flutter analyze` 는 경로에 한글이 있으면 죽습니다.** LSP 채널이 메시지를 잘라먹고 `FormatException: Unterminated string` → 분석 서버 exit 255 가 납니다. `바탕 화면` 아래에 뒀을 때 실제로 겪었습니다. **`dart analyze` 를 쓰면 우회됩니다** — 같은 규칙으로 같은 결과가 나옵니다. `flutter test` 는 영향 없습니다.
  - 2026.08.02 에 저장소를 `C:\LISN` 으로 옮겨 **지금은 `flutter analyze` 도 정상입니다.** 한글 경로로 되돌리지 마세요.
- git 인증은 GCM(Git Credential Manager)을 씁니다. Windows 자격 증명에 토큰이 저장돼 있으면 **에이전트 도구에서도 프롬프트 없이 push 가 됩니다.** 새 PC 에서는 자격 증명이 없으므로, 대화형 터미널에서 `git push` 를 한 번 실행해 브라우저 로그인으로 등록한 뒤 이어가세요.
  - 확인 방법: `cmdkey /list` 에 `git:https://github.com` 항목이 있으면 등록된 상태입니다.
  - 실제 인증 여부는 `git push --dry-run` 으로 확인하세요. 아무것도 올리지 않으면서 쓰기 권한까지 검증됩니다. 공개 저장소라 `fetch` 성공만으로는 인증 여부를 알 수 없습니다.

### PowerShell 주의

- **`&&` 와 `||` 를 쓸 수 없습니다.** PowerShell 5.1 에는 파이프라인 체인 연산자가 없어 파서 에러가 납니다. 사용자에게 명령을 안내할 때 특히 조심하세요 — bash 습관으로 `cd x && pip install ...` 를 주면 그대로 실패합니다.
  - 순차 실행은 `;`, 조건부는 `A; if ($?) { B }`
  - **여러 줄로 나눠 주는 편이 가장 안전합니다.**
- 커밋 메시지에 큰따옴표가 들어가면 네이티브 인자 전달에서 깨집니다. **메시지를 파일에 쓰고 `git commit -F` 를 쓰세요.**
- here-string 은 `@'...'@` 를 쓰고 닫는 `'@` 는 반드시 열 0 에 두세요.
- **한글이 들어간 `.ps1` 파일은 UTF-8 BOM 으로 저장해야 합니다.** PowerShell 5.1 은 BOM 이 없으면 스크립트를 ANSI 로 읽어서 한글이 깨지고 파서 에러가 납니다. 편집 후 아래로 BOM 을 다시 붙이세요.
  ```powershell
  $noBom = New-Object System.Text.UTF8Encoding($false)
  $withBom = New-Object System.Text.UTF8Encoding($true)
  [IO.File]::WriteAllText($p, [IO.File]::ReadAllText($p, $noBom), $withBom)
  ```
- Windows 의 `python` / `python3` 은 실제 설치 없이도 Microsoft Store 스텁이 PATH 에 잡힙니다. `Get-Command` 로 존재 여부만 확인하면 오탐이 나므로, 버전 출력까지 확인해야 합니다. `tools/check-env.ps1` 이 이 케이스를 구분해 줍니다.
- **원소가 하나인 배열은 펼쳐집니다.** `@( @('a','b') )` 를 해시테이블 값으로 넣으면 안쪽 배열이 그대로 꺼내져 `$_[0]` 이 문자가 됩니다. 앞에 쉼표를 붙여 `,@('a','b')` 로 강제하세요.
- 문자열에 `Remove-Item` 같은 위험 명령어가 **정규식 안에라도** 들어가면 샌드박스가 실행을 막을 수 있습니다.

### .gitignore

GitHub Python 템플릿 기반입니다. 원본의 `lib/` 규칙은 **`/lib/` 로 좁혀놨습니다** — 그대로 두면 Flutter 소스가 있는 `frontend/lib/` 전체가 커밋에서 누락됩니다. 되돌리지 마세요.

`.claude/settings.json` 은 커밋되어 **팀 전체가 공유**하고, `.claude/settings.local.json` 은 PC 별이라 제외됩니다. 개별 승인이 local 에 쌓이는데 **한글이 들어간 명령은 인코딩이 깨져 저장돼 매칭이 안 됩니다.** 반복해서 물어보면 ASCII 패턴으로 `settings.json` 에 넣으세요.

## 브랜치

**브랜치를 씁니다.** 팀원별로 나눠 작업하고 main 에 병합합니다.

| 브랜치 | 용도 |
|---|---|
| `main` | 통합본. 여기가 기준입니다 |
| `함은선` | 프론트엔드(Flutter 앱·화면 시안) |
| `backend` | FastAPI 서버 |
| `docs/revision-20260729` | 문서 개정 |

2026.07.31 에 네 브랜치를 **한 번 전부 main 에 병합**했습니다. 그 시점에 브랜치가 갈려 "이 파일이 어느 쪽에 있더라"가 반복됐기 때문입니다. **그건 1회성 정리였고 브랜치 폐지가 아닙니다.** 이후로도 `함은선` 브랜치에서 계속 작업이 올라오고 있습니다.

- **작업 전에 `git branch -r` 로 어디에 있는지 먼저 보세요.** 브랜치가 여럿이라 최신본 위치가 갈립니다
- 병합 여부는 `git log --oneline origin/main..<브랜치>` 로 확인합니다. 0 이면 이미 main 에 들어간 것입니다
- **문서 작업은 main 에서 하고 바로 push 합니다.** 산출물 HWP·PPTX 는 바이너리라 브랜치가 갈리면 병합할 수 없습니다 — 실제로 화면설계서 PPTX 가 충돌난 적이 있습니다

## backend

```powershell
cd backend
uvicorn app.main:app --reload
```

### ⚠ 테스트는 로컬 DB 로 돌립니다

`.env` 기본값은 **캠퍼스 공용 DB** 입니다(2026.08.05). 테스트는 계정을
만들고 지우므로 공용에서 돌리면 남의 개발 중에 행이 생겼다 사라집니다.
`conftest.pytest_configure` 가 **로컬이 아니면 실행 자체를 막습니다.**

```powershell
$env:LISN_TEST_DATABASE_URL = "postgresql+asyncpg://postgres:비밀번호@localhost:5432/lisn"
python tools/check_docs.py --with-tests
```

- 검사기가 **대신 로컬을 골라주지 않습니다.** 어느 DB 로 돌릴지는 사람이
  정할 일이고, 조용히 바꿔치면 정본이 어디인지 흐려집니다
- PowerShell 은 `VAR=x 명령` 을 못 써서 `$env:` 로 받습니다
- `flutter` 가 PATH 에 없으면 `$env:FLUTTER_BIN` 으로 지정하세요

**⚠ 시각·난수에 의존하는 테스트를 만들지 마세요.** 선제 접촉 테스트가
벽시계를 그대로 써서 **09시 이전에만 8건이 깨졌습니다.** 실패 메시지는
시각과 무관해 보이고 아홉 시가 지나면 저절로 낫습니다
(→ `docs/학습자료.md` 사례 20).

`backend/.env` 는 gitignore 대상입니다. 새 PC 에서는 `.env.example` 을 복사해 `DATABASE_URL` 과 `JWT_SECRET` 을 채우세요.

- **`db/schema.sql` 이 스키마 정본**입니다. 모델은 그 DDL 의 파이썬 매핑일 뿐이고 `Base.metadata.create_all()` 을 쓰지 않습니다. alembic 도 안 씁니다 — 정본이 둘이 되면 데이터베이스요구사항분석서·테이블명세서와 대조할 기준이 파이썬 코드로 옮겨갑니다
- 스키마를 바꿀 때는 `schema.sql` 을 고치고 **DB 를 다시 만듭니다**
- **시각 컬럼에는 `TimestampTZ` 를 반드시 명시하세요.** `Mapped[datetime]` 만 쓰면 SQLAlchemy 가 `timezone=False` 로 추론해 `TIMESTAMP WITHOUT TIME ZONE` 을 만들고, tz-aware 값을 넣는 순간 asyncpg 가 죽습니다. 데이터베이스요구사항분석서는 전 컬럼 `TIMESTAMPTZ` 를 규정합니다
- **DB 연결 오류는 PostgreSQL 로그를 먼저 보세요.** asyncpg 는 `connection was closed in the middle of operation` 처럼 원인을 감추는 메시지를 던집니다. 실제 원인(`"lisn" 데이터베이스 없음`)은 로그에 명확히 찍혀 있습니다
  - `C:\Program Files\PostgreSQL\17\data\log\`

## LLM 공급자 — Gemini(임시) / OpenAI(정본)

`.env` 의 `LLM_PROVIDER` 로 갈아탑니다. 평소 개발은 **Gemini**(무료 한도), 정확도 검사·시연은 **OpenAI**.

- **Gemini 는 임시 수단입니다.** 산출 문서(기획서 · 요구사항정의서 `MLCM_310`/`MLCM_320`)의 **"외부 OpenAI API" 가 정본**이고 **문서는 고치지 않습니다.** 그러니 **OpenAI 경로를 죽이지 마세요** — 죽이면 문서와 실제가 어긋납니다
- Gemini 는 OpenAI 호환 엔드포인트라 **SDK 는 `openai` 를 그대로** 씁니다. 다만 **Responses API 미지원**이라 양쪽 다 Chat Completions 로 통일했습니다
- **작업마다 다른 모델을 씁니다.** 무료 한도가 **모델별로** 잡혀서 한 모델에 몰면 병목이 됩니다. 특히 `reply`·`crisis` 는 `analyze_and_reply` 에서 동시에 호출되므로 반드시 갈라놔야 합니다
- **`max_retries=0` 을 되돌리지 마세요.** 무료 티어는 모델이 통째로 503 이 되는 일이 있고, 기본 재시도(2회)면 죽은 모델 하나가 `NFR-DV-001` 3초 예산을 통째로 먹습니다(실측 13.95초)
- **쓰면 안 되는 모델** — `gemini-3.5-flash`(503 UNAVAILABLE) · `gemini-2.5-pro`(429, 무료 한도 없음)
- 모델 가용성·검증 결과는 `docs/llm/USAGE_LOG.md` LLM-004 에 실측값으로 있습니다

## ai/server

```powershell
cd ai\server
uvicorn main:app --reload --port 8001
```

`MLCM_210` 정서 위험도 분석. backend 의 `AI_SERVER_URL` 기본값(8001)과 맞춰져 있습니다.

**DB 설정은 `backend/.env` 를 직접 읽습니다.** 환경변수를 따로 넣을 필요가 없습니다.
`AI_DATABASE_URL` 로 갈라 쓸 수도 있고, `postgresql+asyncpg://` 접두사는 자동으로
벗겨냅니다.

- ⚠ 전에는 `.env` 를 안 읽어서 **문서대로 띄우면 DB 를 못 붙었습니다.** 하드코딩
  기본값으로 접속해 PostgreSQL 이 인증 실패로 끊었습니다(2026.08.02 수정)
- ⚠ 그때 asyncpg 는 `connection was closed in the middle of operation` 만 던집니다.
  **원인은 PostgreSQL 로그에 있습니다** — 「사용자 postgres 의 password 인증을
  실패했습니다」

- **판정은 개인 기준선 이탈 탐지입니다.** `model_version` 이 `rule-` 로 시작하면
  모델 결과가 아닙니다. 임계값도 선행연구값이 아니라 임의값이니 **성능 근거로 쓰지 마세요**
- 모델 교체는 **`_predict()` 하나만** 바꿉니다. 반환 6필드는 비즈니스 서버와의 계약입니다
- **`risk_level_of()` 는 모델이 아니라 정책**(데이터베이스요구사항분석서 6항)입니다. `_predict()` 를 교체할 때
  같이 들어내지 마세요. 비즈니스 서버가 이 계산을 다시 하면 규칙이 두 곳에 생겨 어긋납니다
- `EMOTION_CATEGORY` 는 `schema.sql` 감정 마스터 9종의 복제본입니다. **스키마를 고치면 여기도 고칩니다**
- **데이터가 없을 때 `NORMAL` 을 쓰지 않습니다.** 실측치가 3일치 미만이면 422 로 끊습니다.
  편차 0 을 "정상"으로 적재하면 관제 대시보드에서 위험을 놓칩니다
- 위기 문맥 탐지는 **여기 없고 backend 에 있습니다.** `NFR-DV-003`(외부 API 장애 시에도
  키워드 필터 단독 동작) 때문입니다. API 명세 초안의 `/internal/analyze/crisis` 는 철회됐습니다

## frontend 구조

기본 구조, Flutter 인증·긴급 상담, 관리자 웹 인증 기반은 main에 반영됐습니다(2026.07.31).

```
frontend/
├── app/        Flutter 사용자 앱 — lib/{config,models,services,screens,theme,widgets}
└── admin/      Vite + React 관리자 관제 웹 — 로그인·ADMIN 역할 가드 완료
```

**화면 시안은 `docs/design/` 에 있습니다.** 전에 `frontend/design/` 에도
있었는데, 두 곳에 흩어지면 어느 쪽이 최신인지 알 수 없어 합쳤습니다
(옛것은 `docs/design/구버전_20260731/`).

**앱 화면 14개와 관리자 웹 2개가 전부 실제 API 에 붙어 있습니다.** 목업 데이터는
없습니다. 관리자 웹은 앱과 같은 로그인 API 를 쓰고 `ADMIN` 역할만 세션을 허용합니다.

- 라이프로그 수집(`MLCM_200`)은 **앱이 Health Connect 에서 읽어 push** 합니다.
  플랫폼에 닿는 곳은 `services/health_reader.dart` 하나이고 집계·동기화는
  주입만 받아 **실기기 없이 테스트됩니다** → `frontend/app/README.md`
- **화면설계서가 정본입니다.** 앱이 문서와 어긋난 6건을 2026.08.02 에 맞췄습니다
  → [`docs/검증/구현_갭_20260803.md`](docs/검증/구현_갭_20260803.md)
  - 특히 **챗봇 성격 이름**은 앱만 달랐습니다. `test/persona_label_test.dart` 가
    화면설계서 추출본을 직접 읽어 대조하니, 바꾸려면 **문서를 먼저** 고치세요

## ⚠ 릴리스 빌드는 디버그와 다릅니다 (2026.08.02)

**에뮬레이터·디버그로는 절대 안 보이는 결함이 셋 있었습니다.** 고쳐뒀지만
같은 자리를 다시 밟기 쉬우니 알아두세요.

- **평문 HTTP** — `targetSdk` 36 은 평문을 기본 차단하고, Flutter 는
  `src/debug/AndroidManifest.xml` **에만** 허용을 넣어둡니다. 그래서
  **릴리스 APK 만 서버에 못 붙습니다.** 화면에는 「서버에 연결할 수
  없습니다」만 떠서 네트워크·서버 문제로 보입니다
  - 허용 범위는 `android/app/src/main/res/xml/network_security_config.xml`
  - `usesCleartextTraffic` 을 통째로 켜지 마세요. 기본은 막고
    **로컬 주소만** 예외로 둔 상태입니다(02-F 가 HTTPS/TLS 를 규정)
  - ⚠ **실기기는 주소를 두 곳에 넣어야 합니다.** `--dart-define=API_BASE_URL`
    만으로는 안 되고 위 XML 의 `<domain>` 에도 같은 IP 를 넣어야 합니다.
    Android 네트워크 보안 설정은 **CIDR 을 지원하지 않습니다**
- **패키지 가시성** — Android 11+ 는 `<queries>` 에 선언하지 않은 앱을
  못 봅니다. `tel`(109 긴급 전화)·`https`(콘텐츠 열기)가 여기 걸립니다.
  **에뮬레이터에는 전화 앱이 없어 어차피 실패하므로 여기서는 안 보입니다**
- **제출 전에 `flutter build apk --release` 로 한 번 굽고 켜보세요.**
  빌드가 되는 것과 동작하는 것은 다릅니다

## 앱에 인증 우회 경로가 있습니다 (2026.08.02)

화면설계서 캡처용으로 `frontend/app/lib/dev_screens.dart` 에 **자동 로그인**과
**화면 바로 띄우기**를 넣었습니다.

```bash
flutter run --dart-define=DEV_LOGIN=force --dart-define=SCREEN=report
```

- 에뮬레이터는 `flutter emulators --launch lisn` 으로 켭니다(이 PC 에 이미 있음)
- `DEV_LOGIN=true` 는 **이미 로그인돼 있으면 건너뜁니다.** 캡처용으로는 `force` 를
  쓰세요 — 안 그러면 손으로 로그인해 둔 다른 계정 데이터가 조용히 찍힙니다

- **컴파일 시점 플래그**라 평소 빌드에는 영향이 없습니다
- **릴리스 빌드에서는 무시**합니다(`kReleaseMode`). 이 검사를 빼지 마세요
- 인증 검사를 끄는 게 아니라 **실제로 로그인**합니다. 그래야 데이터까지 그려집니다
- 제출·시연 빌드를 만들 때 **이 플래그를 주지 않는지 확인**하세요

## 커밋

- 메시지는 한국어. `chore:` `docs:` `feat:` `fix:` 접두사.
- 무엇을 바꿨는지보다 **왜 바꿨는지**를 적습니다. 특히 문서 개정은 근거가 중요합니다.
- 문서 원본(HWP)을 수정했으면 추출본도 같은 커밋에 포함합니다.
