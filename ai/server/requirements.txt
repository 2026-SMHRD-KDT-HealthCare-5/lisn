# AI 추론 서버 — 비즈니스 서버(backend/requirements.txt)와 별개로 관리합니다.
# 모델 라이브러리(torch·lightgbm)가 무거워 같이 두면 백엔드 설치가 느려집니다.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
asyncpg>=0.30
python-dotenv>=1.0   # backend/.env 를 읽는다

# 학습된 집계(model_score.py) — 2026.08.25 도입.
#
# 규칙의 z 계산은 그대로 두고 「상위 3개 평균 / 4.0」이라는 임의 집계만
# 로지스틱 회귀로 바꿉니다. 입력이 늘지 않아 새 센서가 필요 없습니다.
# 근거: 참가자 내부 AUC 0.491(규칙) -> 0.609, 이득 +0.115
#       (95% +0.056~+0.176) · ai/train/eval_rule_features.py
#
# ⚠ scikit-learn 은 **학습 때와 같은 계열**이어야 pickle 이 열립니다.
#   버전이 크게 어긋나면 model_score 가 경고를 남기고 규칙으로 돕니다.
numpy>=2.0
pandas>=2.2
scikit-learn>=1.5
joblib>=1.4

# 아직 안 씁니다.
# torch>=2.5           1차 LSTM Autoencoder (설계에서 철회)
# lightgbm>=4.5        학습 실험용 — ai/train/ 에서만 씁니다
